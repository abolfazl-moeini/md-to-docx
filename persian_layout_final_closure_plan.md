# پلن نهایی بستن پروژهٔ تبدیل Markdown فارسی به Word

تاریخ ممیزی: ۲۰۲۶-۰۹-۰۷. وضعیت این سند: **فقط پلن؛ تسک‌های زیر هنوز اجرا و تأیید نهایی نشده‌اند.**

این سند پس از خواندن پیاده‌سازی، `persian_layout_quality_plan.md` و `persian_layout_gap_review_plan.md` و اجرای بررسی مستقل نوشته شده است. هدف، جمع‌کردن کار باقی‌مانده در یک فهرست اجرایی برای عامل بعدی است. ادعای «هیچ باگی در هیچ ورودی ممکن وجود ندارد» قابل اثبات نیست؛ معیار اتمام این پلن، پوشش کامل قرارداد محصول، corpus تعیین‌شده، رگرسیون‌های شناخته‌شده و دروازه‌های واقعی Word است.

درخواست جدید کاربر دربارهٔ **`Google Chrome quit unexpectedly`** در تسک F01 با اولویت P0 آمده و پیش‌نیاز هر اجرای جدید Mermaid است. پنهان‌کردن پنجرهٔ crash یا خاموش‌کردن گزارش خطای macOS رفع مشکل محسوب نمی‌شود.

ریشهٔ پروژه: `/Users/moeini/Downloads/md-to-docx`. مسیرهای کوتاه این سند نسبت به آن‌اند. نام مسیرها و فرمان‌های دارای برچسب «پیشنهادی» بخشی از کار آینده هستند، نه قابلیت موجود.

## ۱. قرارداد تحویل و مرز کار

- ورودی: Markdown فایل/متن/stdin همراه نام یا مسیر پوشهٔ قالب معتبر.
- خروجی: **DOCX** با متن فارسی درست، Vazirmatn پیش‌فرض و قابل تنظیم، محتوای فنی دقیق، کد رنگی، تصویر و Mermaid خوانا، و طراحی یک‌دست در تمام صفحات.
- قالب‌ها: چهار قالب موجود `purple_book`، `persian_book`، `persian_compact` و `persian_report`. هیچ‌کدام برای سبزکردن گزارش کنار گذاشته نشوند.
- corpus پایه: **۶۱ ورودی × ۴ قالب = ۲۴۴ زوج**. ساختار آن همان یک کتاب کامل، چهار فصل، چهل برش و شانزده fixture مصنوعی است. محتوای fixture مصنوعی ناقص باید تکمیل شود؛ عدد ۶۱ به‌تنهایی معیار کفایت نیست.
- پوستهٔ اجرایی همچنان یک پوشه با YAML و دارایی‌ها و در صورت نیاز `shell.docx` تک‌بخشی است. تبدیل هر عکس یا Word دلخواه به template، پوستهٔ چندبخشی، خروجی قدیمی `.doc`، GUI و وب‌سرویس وارد این دور نشوند.
- Python و ابزارهای عمومی Node حفظ شوند. تغییر رفتار با TDD انجام شود؛ بازنویسی کل موتور یا مهاجرت به Haskell لازم نیست.
- انتخاب فعلی **Courier New برای کد حفظ شود**؛ برای تعویض اجباری به DejaVu تسک دوباره نساز. موجودبودن Courier New در محیط مشاهده باید اندازه‌گیری شود؛ نام فونت به‌تنهایی تضمین نصب آن نیست.
- اصل کتاب SQL، خروجی‌های `raw/`، بستهٔ مرور Word و تغییرات عامل‌های قبلی بازنویسی یا پاک نشوند. اصلاح دستی Word نهایی جای اصلاح renderer و بازتولید را نمی‌گیرد.

## ۲. شواهد تازه؛ چه چیزی واقعاً بررسی شد؟

### ۲.۱. اجرای تست و وضعیت خروجی‌ها

فرمان این ممیزی:

`PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest tests -q -rs -p no:cacheprovider -k 'not test_smoke_wheel_build_and_template_assets'`

نتیجهٔ واقعی: **۲۸۸ passed، ۲ failed، ۲ skipped و ۱ deselected** در حدود ۲۶٫۸ ثانیه. شکست‌ها مربوط به راه‌اندازی مرورگر در `test_mermaid_svg_label_verification` و `test_representative_fixtures_structural_counts` بودند. دو skip نیز به همان خرابی محیط Mermaid مربوط بودند. تست ساخت wheel عمداً در این نوبت اجرا نشد؛ نتیجهٔ بستهٔ نصب از روی آن ادعا نمی‌شود.

پس از مشاهدهٔ پیام‌های crash توسط کاربر، هیچ تست دیگری که مرورگر را راه‌اندازی کند اجرا نشد. بررسی بعدی از روی کد، DOCX و PNGهای موجود، گزارش crash و آزمایش‌های بدون مرورگر در حافظه/پوشهٔ موقت بود. کد محصول و تست‌های مخزن تغییر نکردند.

آخرین artifact کامل مشاهده‌شده: `artifacts/persian-layout/run_20260907_182555/`. گزارش آن ۲۴۴ زوج پذیرفته‌شده توسط کنترل‌های فعلی دارد، ولی `visual_pages_reviewed=0` است. در `artifacts/` هیچ PDF دیده نشد؛ در آخرین run هیچ PNG صفحه نیز وجود ندارد. وجود `word-windows-review/` و ZIP همراه آن، بدون نتیجهٔ مرور یا export، اثبات بررسی Word نیست.

این ممیزی اجرای تازهٔ کل ماتریس یا تأیید ظاهری تمام صفحات نیست. موفقیت یک تست LibreOffice در مجموعهٔ واحد نیز معادل رندر تمام ۲۴۴ زوج نیست.

### ۲.۲. نسخهٔ کد مورد بررسی

working tree تغییرات commitنشدهٔ فراوان دارد. صرف HEAD برای بازتولید کافی نیست. hashهای زیر نقطهٔ مرجع این بازبینی‌اند:

| فایل | SHA-256 |
| --- | --- |
| `src/md_to_docx/renderer.py` | `794467010b144a36f0ed909ed7ca501d855eeb505803478610026bf59e4c49b6` |
| `src/md_to_docx/pandoc_json.py` | `0fa1fccb97f8ecab3e871e7fe427b5742bc3dff018359f43c8f6d4c5ef3d7240` |
| `src/md_to_docx/mermaid.py` | `e9bac45c08bab1d5e1ac4f6bb7baa52017d2e8a04e295455379c9691c029e245` |
| `scripts/matrix_runner.py` | `6d0ce37813a410efd3893993f26bd9b0073decb8947ebecce22909bf61fd519b` |
| `tests/fixtures/persian_layout/manifest.yaml` | `283f03b5a350e86667b8ed26598cb7cb60018ffedf82366ce4963e1cbb55e65f` |

کتاب اصلی و B00 همچنان باید hash `525a3e92016a946bf36fdd5b3a2a8c8829a6e71e3e433fe52b6d9e9f3352443b` داشته باشند. پنج caption بدون تصویرِ این کتاب کمبود منبع‌اند؛ تصویر یا نمودار برای آن‌ها جعل نشود.

### ۲.۳. اصلاحاتی که نباید بی‌دلیل دوباره پیاده شوند

| اصلاح موجود | نتیجهٔ این بررسی | ادامهٔ لازم |
| --- | --- | --- |
| چهار قالب و کپی داخل بسته | فایل‌های دو مسیر برای هر چهار قالب، به‌جز فایل‌های سیستم، همسان‌اند | تست wheel واقعاً نصب‌شده و بررسی ظاهر |
| custom-style تنظیم‌پذیر و strict | در Template و adapter وجود دارد | اعتبارسنجی نقش مقصد و پوشش Span؛ F08 |
| `paragraph_align`، فاصلهٔ بدنه و اندازهٔ caption | بارگذاری/مصرف و تست دارند | یک‌دستی همهٔ نقش‌ها و ظرف‌ها؛ F06 |
| header عمومی و هویت T04 | عنوان اختصاصی کتاب حذف و لوگو/PAGE و تست خروجی اضافه شده‌اند | مرور همهٔ صفحات و فونت در Word |
| وضعیت مرور pending | runner دیگر هر تبدیل را خودکار visually accepted نمی‌نویسد | هنوز هیچ مسیر اجرای رندر/مرور ندارد؛ F13/F14 |
| کنترل شمار عناصر و warnings | اضافه شده است | برای اثبات وفاداری محتوا کافی نیست؛ F03/F04/F07 |
| پاورقی غنی | `FootnoteContainer` و dispatch بلوک‌ها وجود دارد؛ دیگر صرفاً متن flattenشده نیست | اندازهٔ نقش، تصویر/کد/جدول و oracle storyهای فرعی |
| مسیر تصویر | fallback نام‌فایل/cwd از resolver اصلی حذف شده است | تست رگرسیون حفظ شود؛ توضیح قدیمی AGENTS اصلاح شود |
| فایل Bold و دارایی‌های تصویری دارای نوشته | وجود دارند | بارگذاری واقعی وزن Bold و تکمیل عکس/provenance |

متن ابتدای پلن‌های قبلی که «همهٔ موارد انجام شد» می‌گوید نباید معیار پذیرش قرار گیرد. به‌خصوص G14 و مرور نهایی با شواهد موجود بسته نیستند.

## ۳. یافته‌های باز و قابل بازتولید

| ID | شدت | یافته و شاهد دقیق | تسک رفع |
| --- | --- | --- | --- |
| A01 | P0 | Mermaid بعد از شکست Chrome مدیریت‌شده، Chrome/Edge نصب‌شدهٔ کاربر را هم امتحان می‌کند؛ هر تست/شکل می‌تواند زنجیره را تکرار کند. crashهای ۱۸:۳۲ همین روز موجودند. | F01 |
| A02 | P0 | lockfile، Puppeteer `25.10.0` با `engines.node >=22.12.0` دارد، ولی CI از Node 20 استفاده می‌کند. | F01/F15 |
| A03 | P0 | `--fixtures NO_SUCH_FIXTURE` صفر زوج اجرا می‌کند و با کد خروج ۰ تمام می‌شود. `main` برای fail/error زوج‌ها نیز خروج غیرصفر ندارد. | F02 |
| A04 | P0 | حذف پاراگراف عادی B00 از DOCX روی کپی موقت، با کنترل محتوایی و ساختاری هر دو pass شد. | F03 |
| A05 | P0 | جابه‌جایی دو سلول اولین ردیف دادهٔ R30، با حفظ تعداد و رشته‌ها، از هر دو oracle عبور کرد. | F03/F10 |
| A06 | P0 | تغییر Target تصویر S09 به `media/does_not_exist.png` با XML معتبر، از هر دو oracle عبور کرد. | F04 |
| A07 | P0 | `render_block(Header)` شماره را از inlineها جدا می‌کند؛ در سه قالب دارای `badge: false` مسیر ساده شماره را بازنمی‌گرداند. در خروجی R01 این سه قالب، `۱.۱` و `۱.۱.۱` وجود ندارند؛ T01 آن‌ها را در badge دارد. | F05 |
| A08 | P0 | فونت heading مستقل در مسیر AST مصرف نمی‌شود: با `fonts.heading=HEADING_FONT_PROBE` و body متفاوت، فونت cs عنوان `BODY_FONT_PROBE` بود. | F06 |
| A09 | P1 | پنج warning مربوط به «caption اشتباه در block 999» به‌جای پنج محل صحیح B00 به `verify_warnings` داده شد و issues خالی برگشت. محل/هویت warning سنجیده نمی‌شود. | F07 |
| A10 | P1 | `custom_styles: {strict: true, My Note: nonexistent_role}` پذیرفته می‌شود؛ کادر با ظاهر fallback و بدون warning ساخته می‌شود. | F08 |
| A11 | P1 | metadata `dir: ltr` روی پاراگراف عددی، در قالب RTL هنوز `bidi=1` می‌دهد؛ lang/dir ورودی در adapter اعمال نمی‌شود. قطعه‌بندی bidi همچنان در بسیاری مسیرها روی Strهای جدا انجام می‌شود. | F09 |
| A12 | P0 | `_fit_image_size(2000,10,...)` نسبت ۲۰۰:۱ را به حدود ۳۳٫۵:۱ تبدیل می‌کند؛ clamp جداگانهٔ حداقل ۰٫۲in برای هر ضلع تصویر را می‌کشد. | F11 |
| A13 | P1 | helper بررسی label SVG وجود دارد، ولی pipeline PNG و runner آن را به نتیجهٔ هر Mermaid متصل نکرده‌اند. SVG خوب هم clipping یا font fallback در PNG را رد نمی‌کند. | F12 |
| A14 | P1 | `mermaid.json` و CSS هر چهار قالب byte-identical و بنفش‌اند؛ نمودار T03 خاکستری یا T04 سبزآبی واقعاً theme قالب را دنبال نمی‌کند. | F12 |
| A15 | P1 | Bold در `font_files` هست ولی `_effective_mermaid_css` فقط وزن normal را بارگذاری می‌کند. پاورقی دوپاراگرافی S13 نیز در خروجی persian_book اندازه‌های ۹٫۵ و ۱۰٫۵pt دارد. | F06/F12 |
| A16 | P0 پذیرش | runner رندر صفحه ندارد، خروجی مرور هر زوج فقط pending است و `summary/index` حتی هنگام وجود soffice متن ثابت absent دارند. | F02/F13/F14 |
| A17 | P1 | fixtureهای S06/S12/S13/S14/S15 بخشی از سناریوهای متن پلن را ندارند؛ عنوان fixture جای ساختار واقعی را گرفته است. | F00 |
| A18 | P1 | S15 تنها تیتر «فهرست مطالب» و کامنت دارد؛ در خروجی هیچ field مربوط به TOC وجود ندارد. | F16 |
| A19 | P1 | تست wheel فایل ZIP را بررسی می‌کند، سپس همان پکیج importشده از checkout را با ورودی بیرون مخزن اجرا می‌کند؛ wheel را نصب و از آن import نمی‌کند. | F15 |
| A20 | P1 | تست `test_representative_fixtures_structural_counts` بدون marker Mermaid، S10 را تبدیل می‌کند؛ job واحد بدون Node/مرورگر نیز آن را انتخاب می‌کند. job wheel نیز Pandoc نصب نمی‌کند ولی تست جدید تبدیل واقعی دارد. | F15 |
| A21 | P1 | «عکس» S09 یک تصویر کوچک از صفحات مرجع است؛ دو عکس واقعی افقی/عمودی هنوز نیستند. PROVENANCE بدون شاهد منشأ مجوز، crop مرجع را CC0 نامیده است. | F00/F17 |
| A22 | P1 | تولید counts در `generate_fixtures.py` فرزندان Table، Note، DefinitionList و inlineهای تو‌در‌تو را کامل نمی‌پیماید؛ هر Div دارای class را callout می‌شمارد. | F00/F03 |

نمونه‌های دستکاری‌شده صرفاً در پوشهٔ موقت ساخته شدند و فایل‌های artifact اصلی سالم ماندند. محل موقت ممیزی `/private/tmp/md2docx-final-audit-vsc37nyf/` است؛ برای اجرای آینده به باقی‌ماندن آن تکیه نکن، دستور بازتولید آزمون‌ها در تسک‌ها آمده است.

## ۴. روش اجرای عامل بعدی

هر تسک F باید یک خروجی قابل مرور، تست شکست‌خورده قبل از اصلاح، تست موفق بعد از اصلاح و سند شواهد داشته باشد. اگر رفتار از قبل درست بود، دوباره تغییرش نده؛ آن را با دلیل و رگرسیون محافظ ببند. «افزودن helper بدون اتصال به مسیر واقعی»، «assert فقط وجود فایل» و «تغییر expected به مقدار خراب فعلی» تکمیل تسک نیست.

ترتیب اصلی: **F00 و F01 → F02–F04 → F05–F12 → F15 و F16 → F13 و F14 → F17 و F18**. زیرساخت رندر F13 می‌تواند پس از آماده‌شدن محیط زودتر نوشته شود، ولی baseline نهایی بعد از اصلاح layout تصویب شود. تا حل F01، آزمون‌های منجر به راه‌اندازی Chrome/Edge تکرار نشوند.

## ۵. تسک‌های نهایی

### F00 — تثبیت corpus، نسخه و پوشش واقعی fixtureها

**اولویت:** P0 برای اعتبار نتیجه. **وابستگی:** ندارد. **مسیرها:** manifest، اسکریپت‌های fixture، تست manifest.

- [ ] snapshot کد، diff و hash ورودی/قالب/دارایی/فونت/lockfile را ثبت کن؛ از reset، stage کردن همهٔ تغییرات یا commit اجباری تغییرات دیگران استفاده نکن.
- [ ] ۶۱ ID یکتا، دسته‌بندی `1+4+40+16`، hash منبع و تمام فایل‌ها را بررسی کن؛ runner هم قبل از تبدیل همین validation را اجرا کند، نه فقط pytest.
- [ ] extraction را با reader دقیق pipeline و بلوک‌های AST بسنج؛ شمارش زوج‌بودن رشتهٔ ``` یا ::: به‌تنهایی validator صحیح Markdown نیست. تغییر renderer نباید fixture را خودکار بازنویسی کند.
- [ ] شمارش AST در تمام containerها، Table/Note/DefinitionList/Span/Link/Strong و captionهای ساختاری کامل شود. count ورودی و count خروجی تعریف مشترک معنایی داشته باشند، ولی تابع renderer oracle خودش نباشد.
- [ ] expected semantics هر fixture را ثبت کن: شماره و متن heading، توالی نثر، کد دقیق، cellها، مقصد لینک، occurrenceهای image و Mermaid، پاورقی و فرمول. صرف چند sensitive string کافی نیست.
- [ ] جدول زیر را روی همان S01–S16 تکمیل کن؛ B00 و ۴۴ fixture واقعی بدون دستکاری منبع بمانند. hash/manifest نسخهٔ تازهٔ syntheticها با diff صریح عوض شود و baseline قبلی invalidate شود.

| fixture | تکمیل لازم برای پوشش قرارداد قبلی |
| --- | --- |
| S01 | علامت‌گذاری و عبارت چندکلمه‌ای لاتین در مرز bold/link/italic؛ عدد و Windows path؛ انتظار متن منطقی و بصری |
| S02 | شماره با badge روشن/خاموش و extraction روشن/خاموش؛ heading داخل cell/Div؛ heading font مستقل و literal `{#id}` در code |
| S03 | نام style دلخواه، role مقصد ناموجود، strict، nested class/attribute و Span دارای style |
| S04 | list چندسطحی tight/loose با continuation؛ ordered با start متفاوت و alpha/roman؛ نشانگر انگلیسی کنار فارسی |
| S05 | جدول حداقل ۶۰ردیفی، header تکراری، cell چندپاراگرافی؛ oracle ترتیب/معنای ستون |
| S06 | تصویر و کد بلوکی و فهرست واقعی داخل سلول و جدول تو‌در‌تو؛ متن `* ... <br>` داخل pipe cell معادل list AST نیست |
| S07/S08 | exact code و tab/newline، خطوط بلند، زبان ناشناخته، code بیش از دو صفحه و tree متنی؛ مقایسهٔ انتهای هر بلوک |
| S09 | دو عکس واقعی با منشأ مشخص، تصویر فنی، alpha، EXIF orientation، شکل خیلی باریک و خیلی کوچک، reference image و inline image |
| S10/S11 | label بلند/چندخطی و ساختار ثابت در تمام خانواده‌های نمودار موجود؛ خوانایی A5، رنگ قالب و markerهای قابل تطبیق |
| S12 | Mermaid در cell/definition/footnote علاوه بر list/Div فعلی؛ تصویر در nesting واقعی؛ تمام رسانه‌ها در story صحیح |
| S13 | پاورقی چندپاراگرافی با bold، link، list، code و image، و OMML زیرمجموعهٔ پشتیبانی‌شده؛ فونت نقش یکسان |
| S14 | طول متن قبل از تیتر/کادر/تصویر در چند variant ثابت و ثبت‌شده؛ caption و code واقعاً از مرز صفحه عبور کنند |
| S15 | TOC به‌صورت قابلیت صریح فعال شود؛ metadata، anchor، field update و شروع فصل آزموده شود |
| S16 | کتابچهٔ تلفیقی واقعاً چندصفحه‌ای با عکس و Mermaid و chapter transitions؛ صفحهٔ اول تا آخر بررسی شود |

**تست قرمز:** Table دارای Image در cell یا Note دارای Mermaid باید در شمار ورودی دیده شود؛ حذف دارایی، تغییر hash یا reference خراب باید suite را متوقف کند. **پذیرش:** هیچ ردیف capability صرفاً با عنوان فایل پوشش‌داده‌شده تلقی نشود. variantها و تست‌های منفی جدا از عدد پایهٔ ۲۴۴ گزارش شوند.

### F01 — رفع ریشه‌ای `Google Chrome quit unexpectedly` و پایداری Mermaid

**اولویت:** P0؛ اولین اصلاح اجرایی. **مسیرها:** `mermaid.py`، bootstrap، تنظیمات runtime، fixtureهای خارجی pytest، matrix runner و lockfile/CI.

#### شواهد علت و حدود آن

گزارش `/Users/moeini/Library/Logs/DiagnosticReports/Google Chrome-2026-09-07-183214.ips` فرایند والد `node`، خطای `EXC_CRASH / SIGABRT` و `Abort trap: 6` را نشان می‌دهد. پشتهٔ thread معیوب شامل `_RegisterApplication` و `TransformProcessType` است. Google Chrome for Testing و Edge نیز در همان بازه الگوی مشابه دارند. این شواهد شکستِ راه‌اندازی/ثبت برنامه در محیط macOS را نشان می‌دهد؛ خرابی syntax Mermaid یا خود فایل Markdown علت اثبات‌شدهٔ این crashها نیست. از این پشته به‌تنهایی نمی‌توان عامل دقیق sandbox/مجوز macOS را قطعی نامید.

کد فعلی در `_iter_browser_candidates` مسیر env، چند مرورگر cache، PATH و سپس برنامه‌های `/Applications/` را جمع می‌کند؛ `render_mermaid_to_png` هنگام launch error همه را یکی‌یکی می‌آزماید. این انتخاب برای هر فراخوانی دوباره رخ می‌دهد؛ سلامت شکست‌خوردهٔ runtime در سطح run حفظ نمی‌شود. بنابراین یک مشکل محیطی به پیام‌های متعدد تبدیل می‌شود.

#### قرارداد جدید runtime

- [ ] یک runtime مدیریت‌شده و نسخه‌ثابت برای تولید Mermaid تعریف کن؛ انتخاب پیشنهادی نخست، `chrome-headless-shell` سازگار با Puppeteer قفل‌شده است. `headless: "shell"` و full Chrome/headless جدید دو حالت متفاوت‌اند و باید با binary متناظر تنظیم شوند. موفقیت shell در این محیط هنوز آزمایش نشده است و نباید فرض شود. [راهنمای رسمی Headless در Puppeteer](https://pptr.dev/guides/headless-modes)
- [ ] اولویت resolution روشن باشد: override صریح API/CLI یا config منتخب، سپس env صریح، سپس runtime مدیریت‌شدهٔ lockشده. تنظیم متعارض diagnostic بدهد. override صریح نامعتبر به مرورگر دیگری fallback نکند.
- [ ] مرورگر شخصی Chrome/Edge/Brave از جست‌وجوی خودکار پیش‌فرض حذف شود. استفاده از system browser فقط با انتخاب صریح و profile اختصاصی باشد؛ profile روزمره، تب‌ها و session کاربر دست‌نخورده بمانند.
- [ ] سازگاری Node و Puppeteer رفع شود: نسخهٔ Node در محیط توسعه و CI با حداقل `22.12.0` ثبت‌شده در lockfile فعلی سازگار و نسخهٔ دقیق آزموده‌شده ثابت باشد. گرفتن خودکار «جدیدترین مرورگر موجود در cache» جای انتخاب نسخهٔ سازگار نیست.
- [ ] نصب runtime یک مرحلهٔ setup آشکار باشد. مسیر تبدیل هنگام نبود mmdc/runtime، از `npx -y` با دانلود نسخهٔ شناور استفاده نکند؛ خطای روشن همراه راه آماده‌سازی بدهد.
- [ ] قبل از اجرای دسته‌ای، نسخه/معماری/اجراپذیری binary و دسترسی temp/profile بررسی شود؛ `--version` به‌تنهایی سلامت render نیست. پس از کنترل‌های بدون launch، یک smoke کوچک واقعی فارسی با زمان محدود اجرا شود.
- [ ] preflight تنها وقتی ورودی Mermaid دارد انجام شود؛ تبدیل بدون Mermaid حتی در محیط بدون browser باید کار کند.
- [ ] شکست launch در سطح کل suite/run ثبت شود. پس از نخستین failure، زوج‌های وابسته blocked/error بگیرند و **launch مجدد نداشته باشند**؛ زوج‌های بدون Mermaid ادامه یابند و نتیجهٔ نهایی release ناموفق بماند.
- [ ] برای shardهای هم‌زمان health state/lock مشترک همان run داشته باش تا هر worker یک crash جدید نسازد. کلید health شامل binary/version و config مؤثر باشد؛ پاک‌شدن failure تنها با اجرای تازهٔ صریح یا تغییر runtime کنترل‌شده باشد.
- [ ] retry syntax error یا crash launch حذف شود؛ در صورت تعریف retry خطای transient، تعداد و علت آن محدود و جدا ثبت شود. fallback زنجیره‌ای میان برنامه‌های دسکتاپ ممنوع باشد.
- [ ] process و profile اختصاصی، timeout و cancel درست مدیریت شوند. فقط childهای همان اجرای mmdc متوقف شوند؛ `killall Chrome`، پاک‌کردن profile کاربر، خاموش‌کردن CrashReporter/Gatekeeper یا تغییر عمومی تنظیمات macOS راه‌حل نیست.
- [ ] flags امنیتی و `headless` به‌صورت بی‌قید بر config تحمیل نشوند. شکست ناشی از محدودیت host باید با راه‌حل پشتیبانی‌شدهٔ همان محیط یا اجرای renderer در محیط مجاز رفع شود؛ تغییر flag بدون اثبات، درمان `_RegisterApplication` فرض نشود. [راهنمای عیب‌یابی Puppeteer](https://pptr.dev/troubleshooting)

#### آزمون و معیار اتمام F01

۱. unit test با subprocess شبیه‌سازی‌شده ثابت کند انتخاب خودکار به `/Applications/Google Chrome.app` و Edge نمی‌رسد و override غلط fail می‌شود. این تست برای سنجش سیاست launch است؛ جای render واقعی را نمی‌گیرد.
۲. launch error مصنوعی را در ابتدای run با تعداد زیادی زوج Mermaid تزریق کن؛ شمار تلاش راه‌اندازی پس از failure باید صفر بماند و شمار preflight حداکثر یک باشد، حتی با workerهای متعدد.
۳. تبدیل بدون Mermaid در همان runtime معیوب موفق باشد؛ انتشار نیمه‌کاره یا PNG خالی به‌جای نمودار رخ ندهد.
۴. پس از آماده‌سازی محیط مناسب، یک smoke واقعی، سپس S10–S12 روی چهار قالب، سپس ماتریس کامل اجرا شود. stdout/stderr، binary/version، exit/signal، مدت، تعداد attempt و cleanup ثبت شوند.
۵. در اجرای سالم، پنجرهٔ خطای Chrome/Edge باز نشود، crash report تازهٔ مرتبط ایجاد نشود، پردازش یتیم نماند و PNGها واقعاً درست باشند. این مرحله نیازمند مشاهدهٔ همان macOS مشکل‌دار است؛ سبزشدن Linux جای آن نیست.
۶. اگر host همچنان launch را رد می‌کند، fail-fast و نبود مزاحمت فقط بخش تاب‌آوری را می‌بندد؛ **قابلیت Mermaid تا اجرای موفق واقعی بسته نیست**. setup پشتیبانی‌شدهٔ لازم تکمیل شود، بدون شروع دوبارهٔ زنجیرهٔ crash.

### F02 — runner قابل اتکا، خروج غیرصفر و گزارش صادقانه

**اولویت:** P0. **وابستگی:** F00؛ اجرای Mermaid وابسته به F01.

- [ ] نام fixture/template نامعتبر، انتخاب خالی، ID تکراری، manifest ناسالم و زوج گم‌شده پیش از اجرا خطا باشند. آزمایش `NO_SUCH_FIXTURE` باید از exit 0 فعلی به خطای کاربرد روشن برسد.
- [ ] وضعیت `conversion`، `content`، `structure`، `warnings`، `render` و `review` جدا ذخیره شود. `conversion_success` از پایان ساخت DOCX بیاید، نه از جمع تمام oracleها.
- [ ] برای fail/error/blocked مورد الزامی کد خروج غیرصفر بده؛ mode «فقط تبدیل» و «پذیرش release» صریح باشند. صفر زوج یا skip الزامی هرگز release موفق تولید نکند.
- [ ] set کامل زوج‌های انتخاب‌شده با manifest تطبیق داده شود؛ عنوان گزارش و تعداد قالب‌ها از انتخاب واقعی بیاید، نه متن ثابت ۲۴۴ یا لیست همیشگی چهار قالب در run انتخابی.
- [ ] snapshot کد، فرمان، hash ورودی/دارایی/فونت، نسخهٔ Node/Puppeteer/browser و مسیر واقعی ابزار رندر در ابتدای run ثبت شود؛ config/shell به‌تنهایی fingerprint قالب نیست.
- [ ] run ID و output directory تصادم نکنند؛ resume تنها با hash سازگار و گزارش صریح باشد. overwrite بی‌قید روی artifactهای run قبلی پذیرفته نیست.
- [ ] پیام «LibreOffice absent» و «Word absent» بر اساس مشاهدهٔ واقعی و وضعیت مرحله تولید شود؛ وجود executable، execution موفق و review انسانی سه چیز متفاوت‌اند.
- [ ] HTML گزارش escape شود؛ متن ورودی/خطا که `<...>` دارد به HTML اجرایی تبدیل نشود. لینک‌های گزارش قابل حمل و به artifact درست اشاره کنند.
- [ ] issues مربوط به افت محتوا/relationship شکسته severity مسدودکننده بگیرند؛ برچسب عمومی warning برای تمام oracle failures کافی نیست.

**تست قرمز:** انتخاب نامعتبر، missing pair، یک fail شبیه‌سازی‌شده، environment حاضر ولی رندر اجرا‌نشده و issue حاوی HTML. **پذیرش:** exit/status/summary همگی واقعیت یکسانی را بیان کنند و run قبلی دست‌نخورده بماند.

### F03 — oracle مستقل حفظ متن، کد، تیتر و جدول

**اولویت:** P0. **وابستگی:** F00/F02. **مسیرها:** oracle پیشنهادی جدا از renderer، manifest و matrix runner.

- [ ] نمای معنایی مستقل از ورودی بساز: توالی blockها و inlineها، شماره/متن heading، متن کد، row/cell و لینک‌ها، تصویر و footnote reference. شمارش و sensitive string کنترل کمکی بمانند.
- [ ] DOCX با story و نقش parse شود؛ متن بدنه با header/footer یا TOC تولیدی مخلوط نشود. footnote، field result، hyperlink و table layout شناخته شوند.
- [ ] `w:tab` و `w:br` و paragraph boundary و خط خالی decode شوند؛ extractor فعلی فقط `w:t` می‌خواند و خطوط خالی را می‌اندازد. normalization مجاز به تفکیک نقش تعریف شود؛ برای کد به جز توافق روشن line-ending، بایت/نویسهٔ منطقی حفظ شود.
- [ ] شمار heading با متن، سطح، شماره و مقصد آن کنترل شود؛ تعداد صحیح با شمارهٔ حذف‌شده pass نباشد.
- [ ] table oracle ترتیب ردیف/ستون و متن غنی هر cell را کنترل کند؛ جابه‌جایی دو سلول با تعداد ثابت شکست باشد. markerهای `tblDescription` به طبقه‌بندی کمک کنند، اما صحت semantic از برچسب خود renderer نتیجه گرفته نشود.
- [ ] تصویر و Mermaid به occurrence منبع، hash media و story مقصد وصل شوند؛ تصویر تکرارشدهٔ قانونی لوگو با اشتباه reuse یک نمودار برای پنج منبع فرق دارد.
- [ ] شمار nodeهای فرعی از Table/Note/DefinitionList و inlineهای nested هم تطبیق داده شود؛ story خارج document.xml پنهان نماند.
- [ ] parser اوراکل exception را نبلعد؛ خطای parse، ساختار ناشناخته یا نگاشت مبهم issues/blocked روشن ایجاد کند.

**آزمون‌های منفی الزامی:** حذف یک پاراگراف غیرحساس B00؛ حذف/تکرار جملهٔ میانی؛ تغییر کلمه یا عدد SQL بدون تغییر count؛ truncation آخر کد؛ جابه‌جایی دو cell؛ حذف شمارهٔ heading؛ تغییر target لینک؛ انتقال متن حساس از body به footnote/header؛ حذف media یک occurrence. هرکدام باید شکست مشخص با محل منبع و خروجی بدهند.

**پذیرش:** هر زوج ۲۴۴گانه گزارش تطبیق کامل داشته باشد. وضعیت «semantic pass» از سه substring یا شمار جدول تولید نشود.

### F04 — اعتبارسنجی کامل package و relationshipهای DOCX

**اولویت:** P0. **وابستگی:** F02/F03.

- [ ] علاوه بر XML well-formed، target تمام relationshipهای داخلی نسبت به part مالک resolve و وجود/نوع part کنترل شود؛ `../` و مسیر نسبی package درست normalize شوند.
- [ ] وجود rIdهای ارجاع‌شده، یکتایی ID در scope، content type و رابطه‌های image/hyperlink/footnote/header/footer بررسی شوند. external link طبق TargetMode جدا باشد و برای اعتبارسنجی لازم نیست از اینترنت باز شود.
- [ ] media معتبر ولی unreferenced نتواند missing drawing را پنهان کند؛ orphan part و dangling relationship با severity مناسب ثبت شود.
- [ ] schema/order عناصر OOXML و namespaceهای مورد استفاده با validator مناسب یا تست سازگاری Word کنترل شوند؛ صرف `ElementTree.fromstring` اثبات سند سالم برای Word نیست.
- [ ] Word خروجی را بدون Repair باز کند و save/reopen کپی آن، متن/تصویر/fields را خراب نکند؛ اصل تولیدی حفظ شود.

**تست قرمز:** S09 با target معتبرِ XML ولی ناموجود، duplicate rId، footnote image relation در part اشتباه و reference مفقود. **پذیرش:** همه رد شوند و خروجی سالم چهار قالب بدون repair باز شود.

### F05 — حفظ شماره و هویت تیتر در تمام طرح‌ها

**اولویت:** P0. **وابستگی:** F03.

- [ ] «استخراج شماره» فقط جداسازی برای طراحی باشد، نه حذف از محتوا. با `badge: true` شماره در badge و با `badge: false` در متن عنوان باقی بماند؛ duplication ایجاد نشود.
- [ ] قرارداد `extract_number: false` حفظ کل متن heading است. چهار حالت دو boolean، شماره‌های فارسی/عربی/لاتین و عنوان rich با bold/link/code آزموده شوند.
- [ ] heading داخل cell/callout/footnote نیز شماره و سطح را حفظ کند؛ مسیر nested اکنون عنوانِ stripped را emit می‌کند و باید جدا بررسی شود.
- [ ] anchor واقعی از AST مصرف شود؛ regex عمومی literal `{#id}` را از مثال کد یا تیتر دارای متن literal حذف نکند. نام bookmark معتبر و پایدار باشد.
- [ ] `heading bold=false`، اندازه و فونت نقش header واقعاً اثر کنند؛ hardcode `bold=True` مسیر root با تنظیمات تعارض نداشته باشد.
- [ ] overflow badge و تیتر چندخطی روی A5 با شمارهٔ چندبخشی بلند کنترل شود؛ مجموع عرض badge و title از ظرف تجاوز نکند.

**تست قرمز:** R01 در سه قالب بدون badge باید قبل از اصلاح فقدان `۱.۱` و `۱.۱.۱` را نشان دهد. **پذیرش:** شماره و عنوان ۱۸۹ heading کتاب با منبع تطبیق کند و navigation/anchor در Word سالم باشد.

### F06 — فونت و typography یک‌دست در همهٔ نقش‌ها

**اولویت:** P0 برای هدف قالب. **وابستگی:** F05 برای تیتر؛ F01 برای بخش فونت Mermaid.

- [ ] نقش font از dispatcher تا emit_inlines/append_text منتقل شود؛ heading با inline غنی نباید از body font استفاده کند. تست marker-font این ممیزی به رگرسیون واقعی تبدیل شود.
- [ ] نقش‌های body، heading1–6، code، table/header، list، quote، callout، caption، footnote و header/footer تعریف روشن داشته باشند؛ اندازه‌های ثابت پراکنده یا default مستند باشند یا از قالب resolve شوند.
- [ ] پاورقی اول و دوم S13 با یک نقش و اندازه رندر شوند؛ direct formatting پاراگراف اول و مسیر render_block بعدی باعث دو اندازه نشود. متن انگلیسی/فارسی پاورقی از سیاست direction همان نقش پیروی کند.
- [ ] فونت cs، اندازهٔ `szCs`، `bCs/iCs` و bold/italic در کنار font لاتین اعمال شوند؛ نام فونت در XML و استفادهٔ واقعی آن در Word/LibreOffice/Chromium جدا کنترل شوند.
- [ ] `Vazirmatn-Regular` و Bold با family/weight واقعی صحیح در محیط‌های renderer در دسترس باشند. CSS وزن ۷۰۰ باید به فایل Bold همان family اشاره کند؛ صرف کلید `Vazirmatn-Bold` که مصرف نمی‌شود کافی نیست.
- [ ] اندازهٔ line height فارسی exact نشود مگر با دلیل و تست glyph/اعراب. فاصلهٔ before/after در nested role و header/footer نیز قابل پیش‌بینی باشد.
- [ ] font override با فونت دومِ نصب‌شده روی بدنه و تیتر و caption و پاورقی اجرا شود؛ code Courier New نقش مستقل داشته باشد. ادعای نصب همگانی Courier New از مستندات حذف و جای آن preflight محیط باشد.

**تست قرمز:** heading/body دو فونت متفاوت، پاورقی دوپاراگرافی، nested callout با اندازهٔ متفاوت و وزن Bold. **پذیرش:** تغییر هر نقش در تمام کاربردهای همان نقش اثر کند؛ font fallback ناشناخته در محیط مرجع صفر باشد.

### F07 — هشدار دارای هویت، محل و شمار دقیق

**اولویت:** P1. **وابستگی:** F00/F02.

- [ ] warning در داخل سیستم ساختار داشته باشد: code، source/AST path، شناسهٔ عنصر، متن، severity و مرحله. لیست رشتهٔ عمومی `warnings` برای سازگاری API حفظ یا مهاجرت آن صریح مستند شود.
- [ ] expected warning با **نوع و هویت منبع** تطبیق کند؛ پنج warning اشتباه در پنج محل دیگر با پنج caption درست برابر نیستند. تطبیق multiset دقیق باشد تا تکرار و missing مشخص شود.
- [ ] line range کتاب اصلی با offset/wrapper برش‌ها اشتباه نشود؛ anchor/شناسهٔ منبع پایدار از شماره‌خط تنها معتبرتر است.
- [ ] absent بودن `expected_warnings` با «هر هشداری مجاز است» یکسان نباشد؛ قرارداد manifest آن را صریح کند و defaults نتیجه را پنهان نکنند.
- [ ] CLI/API/report هشدارها را بدون تکرار و با زبان قابل فهم نشان دهند؛ stderr تنها محل نگهداری شواهد نباشد.

**تست قرمز:** warnings ساختگی block 999، هشدار تکراری، یکی کم/زیاد و برش با wrapper. **پذیرش:** B00 دقیقاً پنج caption شناخته‌شده را گزارش دهد؛ warning جدید در release به بررسی منجر شود.

### F08 — قرارداد کامل template و custom-style

**اولویت:** P1. **وابستگی:** F06/F07.

- [ ] role مقصد mapping باید در `callouts` تعریف شده باشد یا نقش داخلیِ صریح و مستند باشد؛ `nonexistent_role` در validation رد شود، حتی وقتی strict خاموش است. strict مربوط به style ناشناختهٔ ورودی است، نه مجوز config غلط.
- [ ] کلیدهای style رشتهٔ غیرخالی، roleها canonical و collision پس از lower/strip بررسی شوند؛ نوع نامعتبر به AttributeError مبهم تبدیل نشود.
- [ ] شکل direct mapping و `mappings` یکی به‌عنوان canonical مستند و ترکیب مبهم آن‌ها رد شود. fallbackهای شش نام قدیمی فقط با قرارداد سازگاری روشن بمانند، نه اینکه تنظیم صریح template را خنثی کنند.
- [ ] برای style ناشناختهٔ Div و Span سیاست یکنواخت داشته باش؛ داخل code fence نمونهٔ style literal نباید تفسیر شود.
- [ ] normalize کلیدهایی مانند paragraph_align در validator و مصرف‌کننده هماهنگ باشد؛ `justify`/حروف بزرگ/فاصله یا canonical شوند یا با پیام واحد رد شوند. validator مقداری را نپذیرد که renderer متفاوت تفسیر می‌کند.
- [ ] nested keyهای مهم و نوع boolean مثل `page_break_before` اعتبارسنجی شوند؛ کلید تایپیِ بی‌اثر با ظاهر پیش‌فرض پنهان نشود.
- [ ] تقدم YAML و shell، missing asset، یک section، و فضای قابل استفاده آزموده شود. shell دلخواه جدید به‌خاطر body cleanup روابط مورد نیاز header/footer را از دست ندهد.

**تست قرمز:** role ناموجود با strict، mapping تکراری بعد از normalization، کلید typo و boolean رشته‌ای. **پذیرش:** template معتبر واقعاً رفتار تعریف‌شده بدهد و template نامعتبر پیش از نوشتن خروجی خطای روشن ایجاد کند.

### F09 — bidi بافت‌محور، metadata و متن فنی

**اولویت:** P1؛ هر خرابی ترتیب معنی‌دار P0 محسوب شود. **وابستگی:** F03/F06.

- [ ] تقدم جهت محتوا مشخص باشد: override محلیِ پشتیبانی‌شده، metadata صریح ورودی، سپس default قالب؛ code/URL نقش LTR مستقل داشته باشند. اندازه/رنگ قالب از metadata محتوا تغییر نکند.
- [ ] `dir` و `lang` ورودی در AST مصرف و validation شوند؛ متن عددی خنثی مثال خوبی برای تشخیص نادیده‌گرفتن metadata است. نبود metadata رفتار سازگار و مستند بدهد.
- [ ] split bidi با اطلاع از بافت paragraph/ظرف انجام شود؛ پردازش جداگانهٔ هر Str نتواند پرانتز، عبارت لاتین چندکلمه‌ای، path و مرز rich inline را از هم جدا کند. قالب‌بندی inline و target لینک حفظ شوند.
- [ ] جهت paragraph، alignment، run و ترتیب table جدا بمانند؛ حذف RTL والد را با LTR صریح اشتباه نگیر. paragraph-only punctuation، عدد و link-only هم آزموده شوند.
- [ ] شمارهٔ فنی، URL، `D:\SQLData`، `DOMAIN\svc_sqlengine`، `SQL Server Database Engine`، نیم‌فاصله و علامت درصد بعد از copy/search سالم بمانند.
- [ ] رشته‌ها معکوس نشوند، حروف به presentation form تبدیل نشوند و نامرئی‌های جهت برای پوشاندن خطا به کل متن تزریق نشوند. هر کنترل جهتِ محدود نیازمند تست content و Word است.

**تست قرمز:** S01، paragraph عددی با `dir:ltr` در قالب RTL، inline mixed در link/bold و code فارسی. **پذیرش:** هم ترتیب منطقی منبع و هم خواندن بصری در Word صحیح باشد؛ XML RTL به‌تنهایی دلیل پایان نیست.

### F10 — عرض ظرف، جدول، فهرست، کد و شکست صفحه

**اولویت:** P0 برای پذیرش بصری. **وابستگی:** F05/F06/F09.

- [ ] قرارداد واحد عرض از section تا cell/list/callout/footnote برقرار باشد؛ margin، padding و indent از عرض قابل استفاده کم شوند. مقادیر عددی جدول بعد از گردکردن از ظرف تجاوز نکنند.
- [ ] header و دادهٔ جدول از نظر ترتیب ستون با هم بمانند؛ جدول انگلیسی در سند RTL طبق قرارداد روشن عمل کند، نه تصمیم متناقض در مسیرهای root/nested.
- [ ] تخصیص عرض ستون صریح/خودکار و حداقل عرض، مجموع عرض را خراب نکند. جدول واقعاً غیرقابل نمایش خارج corpus diagnostic روشن بگیرد؛ جدول‌های مثبت S05 در همهٔ قالب‌ها باید خوانا شوند.
- [ ] marker فهرست از جهت ظرف بیاید و hanging indent/continuation ثابت باشد. paragraph دوم آیتم، list جدید یا marker تکراری نگیرد؛ ordered style/start/delimiter حفظ شود.
- [ ] کد با syntax highlighting، tab، خطوط خالی، trailing newline و خط بلند عیناً حفظ شود. wrap نمایشی نباید newline/space واقعی به دستور اضافه کند؛ زبان ناشناخته متن کامل ساده بدهد.
- [ ] تیتر با شروع بلوک بعدی بماند؛ header کادر با شروع بدنه و تصویر با caption مربوط نگه داشته شود. keepNext سراسری، cantSplit برای کادر بلند و spacerهای خالی زنجیرهٔ ناممکن نسازند.
- [ ] code/table/callout بلند مجاز به شکست سالم باشند؛ ردیف سرستون داده در صفحهٔ بعد تکرار شود و آخرین خط/ردیف حذف نشود. هدف نثر، حداقل دو خط پس از تیتر است؛ شروع جدول/کد سیاست مستقل دارد.
- [ ] جای title/badge چندخطی، متن فارسی با اعراب، جدول ۶۰ردیفی، code ۱۰۵خطی و مرزهای S14 با همهٔ صفحات مجاور بررسی شود.

**تست قرمز:** oracle سلول جابه‌جا، S08 چندصفحه‌ای و S14 در چند ارتفاع آغاز، و عرض محدود A5. **پذیرش:** بدون بریدگی، هم‌پوشانی، تیتر یتیم، فضای خالی غیرعمدی یا تغییر متن برای جاگیری؛ شواهد F13/F14 لازم است.

### F11 — تصویر صحیح، نسبت ابعاد و caption

**اولویت:** P0. **وابستگی:** F04/F10.

- [ ] حداقل اندازه روی دو ضلع مستقل اعمال نشود؛ scaling واحد با حفظ نسبت انجام شود. تصویر کوچک اجازه داشته باشد از ۰٫۲in کوچک‌تر بماند اگر نسبت و خواست نویسنده چنین است.
- [ ] مقدار width/height صریح متناقض قرارداد روشن داشته باشد؛ stretch ناخواسته پیش‌فرض نباشد. aspect ratio پس از EXIF orientation مبنا قرار گیرد.
- [ ] ارتفاع قابل استفاده شامل caption، spacing و context باشد؛ تصویر portrait قدبلند نتواند caption را از صفحه بیرون بیندازد. تصویر inline محدودیت baseline/line height خود را دارد.
- [ ] image resolver فعلی بدون fallback نام‌فایل حفظ شود؛ file/content/stdin و base_dir با فایل هم‌نام در دو پوشه آزموده شوند. اصل asset برای تست پاک نشود.
- [ ] reference image، نام فارسی/فاصله/percent، alt/title، PNG alpha، JPEG و fallback فرمت پشتیبانی‌شده با تصویر واقعی بررسی شوند؛ error فرمت نامعتبر واضح باشد.
- [ ] media در DOCX جاسازی و بعد از کپی سند به محیط مستقل همچنان قابل نمایش باشد؛ پوشهٔ جانبی تولیدی شرط بازشدن سند نباشد.

**تست قرمز:** نسبت ۲۰۰:۱ مثال ممیزی، عکس orientationدار، تصویر کوچک و تصویر در سلول با caption بلند. **پذیرش:** خطای نسبت بیش از ۱٪ بدون crop صریح صفر، تصویر درست در محل درست و بدون سرریز باشد.

### F12 — Mermaid با label کامل، theme قالب و اندازهٔ خوانا

**اولویت:** P0. **وابستگی:** **F01 باید بسته باشد**، سپس F06/F10/F11.

- [ ] label oracle به خروجی واقعی هر occurrence Mermaid در پذیرش متصل شود؛ helper بدون callsite در pipeline/runner تسک بسته نیست.
- [ ] source/AST path، config/CSS/font/runtime hash، media hash و label/node/edge انتظار برای هر نمودار ثبت شود. SVG/DOM میانی باید متعلق به همان فرایند تولید تصویر یا تولید معادل قابل اثبات باشد؛ موفقیت یک SVG مستقل، PNG دیگری را تأیید نمی‌کند.
- [ ] استخراج متن SVG چند tspan، nested HTML/foreignObject، line break و label تکراری را حفظ کند؛ substring ناقص یا متن پنهان در عنصر نامربوط کافی نیست.
- [ ] label موجود در DOM ممکن است در PNG بریده/پنهان/tofu باشد؛ تصویر native و اندازهٔ درج‌شده در Word هم بررسی شوند. histogram فقط sanity check اولیه است.
- [ ] نمونهٔ Mermaid موجود S10/persian_compact تصویر ۲۳۵۲×۱۰۵ دارد و به نوار بسیار باریک تبدیل می‌شود؛ wrap label، padding و فاصلهٔ گره/لبه بر اساس قالب تنظیم شود تا در A5 خوانا بماند. صرف scale=3 و کوچک‌کردن خروجی راه‌حل نیست.
- [ ] متن node و topology و جهت معنی‌دار graph/sequence تغییر نکند. labelها می‌توانند بدون تغییر متن در چند خط نمایش داده شوند؛ گراف مثبت corpus باید در چهار قالب با همین معنا قابل خواندن شود. موارد ذاتاً بزرگ‌تر از ظرفیت قرارداد در تست منفی جدا با diagnostic صریح باشند، نه جایگزین S10 سخت.
- [ ] theme نمودار از palette و font نقش قالب بیاید؛ کپی مستقیم Mermaid بنفش در تمام قالب‌ها اصلاح شود. T03 چاپی و T04 گزارش باید همین هویت را در diagram نیز داشته باشند.
- [ ] font ready و وزن Bold قبل از اندازه‌گیری/عکس‌گرفتن کنترل شوند؛ CSS `overflow:visible` بدون بازسنجی bbox درمان کافی نیست و ممکن است متن را روی لبه/گره بعدی بیندازد.
- [ ] Mermaid در table/footnote/definition/list/Div به drawing همان story ختم شود؛ cache اگر هست به قالب/فونت/نسخه نیز وابسته باشد.

**تست قرمز:** حذف label از خروجی واقعی، گره بی‌متنِ دارای border، crop label در PNG با SVG سالم، reuse نمودار یک منبع برای منبع دیگر و theme اشتباه قالب. **پذیرش:** همهٔ نمودارهای corpus با اجزای کامل و خوانایی نهایی کنترل شوند؛ تعداد media یا موفقیت mmdc کافی نیست. [مرجع رسمی Mermaid CLI](https://github.com/mermaid-js/mermaid-cli)

### F13 — رندر واقعی همهٔ صفحات و ثبت محیط

**اولویت:** P0 پذیرش. **وابستگی:** F02، محیط فونت F06؛ نتیجهٔ نهایی پس از اصلاحات layout.

- [ ] adapter رندر DOCX به PDF و تمام PNGها اضافه شود؛ تولید مستقیم PDF از Markdown/HTML جای آن نیست.
- [ ] در محیط عامل اسناد، قبل از درخواست نصب، runtime همراه را پیدا کن. در این ممیزی مسیر bundled `soffice` برابر `/Users/moeini/.cache/codex-runtimes/codex-primary-runtime/dependencies/bin/override/soffice` بود. بیرون این محیط، renderer باید configurable باشد؛ این مسیر شخصی در کد محصول hardcode نشود.
- [ ] «executable پیدا شد»، «رندر اجرا شد»، «صفحات سالم ساخته شدند» و «صفحات مرور شدند» چهار وضعیت جدا باشند. پیام نبود LibreOffice را بدون تشخیص واقعی ننویس.
- [ ] فونت‌ها در همان محیط واقعی renderer نصب/قابل مشاهده باشند، نه فقط روی میزبان. raster size/DPI، نسخهٔ LO، خانواده‌های فونت واقعی و page count ثبت شوند.
- [ ] هر worker profile موقت مستقل، زمان محدود و پوشهٔ خروجی پاکِ اختصاصی داشته باشد؛ فایل PDF قدیمی موفقیت run جدید محسوب نشود. cancel فقط پردازش‌های همین run را پاک‌سازی کند.
- [ ] ابتدا B00 و S11 یک قالب برای اندازه‌گیری زمان/صفحات/فضا، بعد هر ۲۴۴ زوج رندر شود. محدودیت هم‌زمانی از اندازه‌گیری بیاید؛ برای سرعت، صفحه‌های سخت حذف نشوند.
- [ ] همهٔ صفحات و شمارهٔ ترتیبی‌شان در page manifest با hash ثبت شوند؛ metadata `Pages` داخل DOCX شمار واقعی صفحات نیست.
- [ ] parserهای ساختاری و بصری نشانه‌های overflow، blank page، فونت جایگزین، شکست caption و متن/شکل بریده را flag کنند؛ مثبت‌شدن detector به معنای review انسانی نیست.

**تست قرمز:** PNG صفر‌بایتی، صفحهٔ وسط حذف‌شده، PDF کهنه، font غایب و worker timeout. **پذیرش:** ۲۴۴ مجموعهٔ رندر کامل با ابزار/فونت معلوم؛ هر مورد اجرا‌نشده blocked باشد. نصب executable تنها F13 را نمی‌بندد.

### F14 — مرور AI، Word واقعی و golden معتبر

**اولویت:** P0 پذیرش. **وابستگی:** F13 و تمام اصلاحات مؤثر بر ظاهر.

- [ ] هر صفحه و مرز میان صفحات یک رکورد review داشته باشد: fixture، template، engine/version، page، hash، reviewer، وضعیت، زمان و issueهای دارای محل/شدت/اطمینان.
- [ ] AI تصویر خوانا، قالب مرجع، عناصر منبع و صفحات مجاور را ببیند؛ accepted/uncertain/issues_found جدا باشد. screenshot کوچک contact sheet برای سنجش خوانایی کافی نیست.
- [ ] همهٔ صفحات LO و Word هدف مرور شوند؛ می‌توان AI یا انسان را برای پوشش کامل به‌کار برد، ولی تعداد واقعی هرکدام ثبت شود. نبود ظرفیت مرور، مجوز auto-accepted نیست.
- [ ] Word هدف/نسخه/سیستم‌عامل را با کاربر تعیین و همان ۲۴۴ DOCX را در آن باز و PDF صادر کن. تفاوت pagination با LO به‌خودی‌خود باگ نیست؛ دو baseline مستقل باشند.
- [ ] چهار خروجی B00، حالت‌های S02/S07/S08/S09/S10–S13، شروع/پایان فصل و تمام issueهای شدید یا مبهم انسانی مرور شوند. حداقل ۱۰٪ صفحات AI-accepted در هر قالب/موتور با seed مشخص انسانی نمونه‌گیری شود؛ کشف ایراد دامنهٔ مرور الگوی مربوط را گسترش دهد.
- [ ] بسته‌های ۳ تا ۵ صفحه/ناحیه برای کاربر با source و لینک DOCX آماده کن. پاسخ کاربر به نسخه/hash مشخص وصل باشد؛ تایید یک نمونه، همهٔ صفحات را تأیید نمی‌کند.
- [ ] issue تأییدشده به fixture و regression test وصل شود. خروجی پس از هر اصلاح layout با hash تازه دوباره رندر و موارد متاثر مرور شود؛ review قدیمی به خروجی تازه منتقل نشود.
- [ ] golden تنها پس از content/structure/font/render و مرور تصویب شود؛ نمونه‌های `raw/` و artifactهای قدیمیِ بدون review golden مطلوب نیستند. baseline/update-all بی‌مرور ممنوع باشد.

**پذیرش:** صفر صفحهٔ بدون وضعیت، صفر issue شدید یا مبهم باز و تأیید انسانی لازم. Word موجود نیست یا کاربر هنوز مرور نکرده است → «آمادهٔ مرور»، نه «پروژه بسته شد».

### F15 — CI و wheel واقعاً مستقل از checkout

**اولویت:** P1؛ مسیر پذیرش الزامی. **وابستگی:** F01/F02.

- [ ] Node CI با engines قفل‌شده سازگار شود؛ Python/Pandoc/Node/Puppeteer/browser نسخه‌های آزموده‌شده داشته باشند و نصب از lockfile انجام شود.
- [ ] تست بدون marker که S10 را تبدیل می‌کند split/mark شود. job واحد باید در محیط عمداً فاقد Node/browser موفق باشد؛ job integration با runtime سالم نباید external skip را بپذیرد.
- [ ] job wheel که تبدیل واقعی اجرا می‌کند Pandoc مورد نیاز را نصب کند. مرورگر فقط اگر smoke Mermaid دارد طبق F01 نصب/استفاده شود.
- [ ] wheel در temp ساخته و در virtualenv تازه نصب شود؛ subprocess با cwd خارج checkout و بدون PYTHONPATH پروژه CLI/API را اجرا کند. `md_to_docx.__file__` و مسیر template باید داخل محیط نصب‌شده باشند.
- [ ] هر چهار قالب، config، فونت Regular/Bold، CSS/JSON، shell در محل لازم و تصویر header بسته‌بندی شوند. smoke شامل Persian + custom-style + code + image و در مسیر integration Mermaid باشد.
- [ ] نمونهٔ نام قالب و مسیر پوشهٔ سفارشی دارای فاصله/فارسی، stdin با output اجباری و API با base_dir مستقل آزموده شوند؛ اعتبارسنجی .docx و رفتار overwrite فعلی رگرسیون نشود.
- [ ] PR یک زیرمجموعهٔ سریع داشته باشد، nightly/release همهٔ ۲۴۴ زوج و artifactهای واقعی. نتیجهٔ runner با کد خروج F02 واقعاً job را fail کند؛ گزارش سبزِ job بدون رندر release کافی نیست.

**تست قرمز:** wheel فاقد shell/فونت، واردشدن از checkout، unit job بدون browser و Node ناسازگار. **پذیرش:** محیط پاک همان نتیجهٔ قابل مصرف و قابل مرور را بدهد؛ تغییر محل فایل ورودی به‌تنهایی «خارج checkout» نیست.

### F16 — TOC، anchor و پوستهٔ تمام صفحات

**اولویت:** P1. **وابستگی:** F05/F06/F08؛ پذیرش با F14.

- [ ] TOC opt-in و قابل تنظیم باشد؛ با کامنت `--toc` یا وجود تیتر «فهرست مطالب» فعال نشود. S15 صریح این قابلیت را فعال کند و body اصلی B00 تغییر نکند.
- [ ] field واقعی TOC با headingهای صحیح/outline و link مقصد ساخته شود؛ شمارهٔ صفحهٔ ساختگی یا متن ثابت شبیه TOC تولید نشود.
- [ ] قبل/بعد update fields در Word آزموده شود. pipeline بدون layout engine نمی‌تواند شمارهٔ صفحهٔ نهایی را حدس بزند؛ حالت update لازم واضح و field سالم باشد، و نسخهٔ تحویلِ مرورشده پس از update بررسی شود.
- [ ] heading شماره‌دار داخل طرح تزئینی در navigation/TOC Word ظاهر شود؛ اگر table باعث محدودیت است، ساختار heading با حفظ طراحی و semantics اصلاح شود.
- [ ] header/footer/logo/PAGE در اولین، زوج/فرد، میانه و آخر صفحات هر قالب طبق قرارداد باقی بمانند. متن خنثی shell به محتوای SQL هارد نشود؛ فونت و geometry پیروی کنند.
- [ ] بخشی از سند چندبخشی اعلام پشتیبانی نشود؛ shell چندبخشی همچنان خطای روشن بدهد. page break شروع فصل با section جدید غیرضروری پیاده نشود.

**تست قرمز:** S15 بدون TOC واقعی، anchor شکسته و تیتر بدون entry در navigation؛ تست متن PAGE فقط «1» نباشد. **پذیرش:** همهٔ موارد در Word واقعی قابل استفاده و طراحی صفحه یک‌دست باشد.

### F17 — دارایی‌ها، مستندات و سازگاری راهنما با کد

**اولویت:** P1. **وابستگی:** تصمیم‌های runtime و template نهایی.

- [ ] S09 دو عکس واقعی افقی/عمودی با منبع و مجوز قابل استناد داشته باشد؛ crop صفحات نمونه «عکس واقعی» یا CC0 فرض نشود. برای asset خودساخته، سازنده/روش/تاریخ و hash ثبت شود؛ برای asset خارجی، منبع واقعی ثبت شود.
- [ ] notice کامل فونت از بستهٔ upstream همراه نسخهٔ صحیح آن نگهداری شود؛ `OFL.txt` فعلی تنها دو خط اشاره به مجوز است. برچسب مجوز بی‌منشأ در PROVENANCE اصلاح شود؛ این کار به معنی تغییر مجوز مرجع کاربر نیست.
- [ ] README و AGENTS دربارهٔ runtime اختصاصی، توقف پس از launch error، Node سازگار، چهار قالب، فونت نصب‌شده، roleها، warnings و فرمان‌های matrix/render/review دقیق شوند.
- [ ] دو توصیف قدیمی AGENTS اصلاح شوند: resolver فعلی fallback نام‌فایل ندارد؛ پاورقی فعلی dispatch غنی دارد. محدودیت‌های واقعیِ آزموده‌نشده باقی بمانند و قابلیت تاییدنشده ادعا نشود.
- [ ] وضعیت تاریخی دو پلن قبلی حفظ شود؛ ادعای «کامل» با یادداشت dated به وضعیت واقعی دروازه‌ها ارجاع دهد. شمار تست قدیمی نتیجهٔ اجرای جدید نیست.
- [ ] اسکریپت‌های setup_templates و fixture generation عملیات صریح و قابل diff باشند؛ قالب سفارشی یا artifact مرور کاربر خودکار بازنویسی/پاک نشود. `.gitignore` از شواهد حفاظت کند، نه اینکه وجود شواهد را حذف کند.
- [ ] نمونهٔ راه‌اندازی پاک و استفادهٔ MD + نام قالب و MD + مسیر قالب، با خروجی سالم و راه فونت، برای کاربر تازه‌وارد آماده شود.

**پذیرش:** هر ادعای قابلیت به تست/شاهد ارجاع داشته باشد و هر asset منشأ درست داشته باشد. گزارش نهایی واقعیت را بیان کند، نه تکمیل فرضی همهٔ checklistها.

### F18 — اجرای نهایی و بستن قابل ممیزی پروژه

**اولویت:** آخرین دروازه؛ الزامی. **وابستگی:** تمام F00–F17.

- [ ] snapshot release candidate را ثابت و گزارش dependency/font/runtime را قفل کن؛ تغییر کد/قالب پس از آن run نهایی تازه می‌خواهد.
- [ ] ۲۴۴ زوج کامل با oracle مستقل و warnings دقیق اجرا شود؛ سپس رندر LO و Word و مرور F14 تکمیل شود. ۲۴۴ تبدیل × دو موتور = ۴۸۸ مشاهدهٔ رندر؛ تعداد صفحات پس از اجرا محاسبه شود.
- [ ] رگرسیون‌های crash، شمارهٔ heading، فونت heading، aspect ratio، relationship، exact code و table semantics با تست منفی مستقل هم سبز باشند.
- [ ] خروجی artifact و بستهٔ نصب در محیط مستقل آزموده شوند؛ اصل MD، template و فایل‌های نامرتبط بدون تغییر باشند و process/staging یتیم باقی نماند.
- [ ] گزارش تحویل بخش ۷ را تکمیل و همهٔ issueها را با شاهد ببند؛ exception جزئی تنها با تأیید روشن کاربر ثبت شود. missing Word/font/review وضعیت pending است و با waiver خودکار بسته نشود.

**پذیرش:** تمام دروازه‌های بخش ۷ PASS و تمام مدارک روی همان snapshot باشند. هیچ مرحله با «فعلاً بعداً انسان چک می‌کند» done نشود.

## ۶. نگاشت کامل پلن‌های قبلی به این پلن

این جدول مانع گم‌شدن کار میان سه سند می‌شود. «موجود» یعنی پیاده‌سازی دیده شده، نه اینکه پذیرش بصری انجام شده باشد.

| مرجع قبلی | وضعیت و مقصد نهایی |
| --- | --- |
| Q00/Q01 | snapshot و corpus → F00؛ صحت oracle استخراج → F03 |
| Q02 | فونت و محیط → F01/F06/F13 |
| Q03 | runner و حاصل‌ضرب → F02/F15 |
| Q04 | semantics/custom-style تا حدی موجود → F05/F07/F08 |
| Q05 | bidi مشترک ناقص → F09 |
| Q06 | role/font بخشی موجود → F06/F08 |
| Q07 | شکست و عرض → F05/F10/F11 |
| Q08/Q09 | list/table/code → F03/F10 |
| Q10 | تصویر → F04/F11 |
| Q11 | Mermaid → F01/F12 |
| Q12 | nesting/footnote → F00/F03/F04/F06/F09 |
| Q13 | TOC/shell → F16/F14 |
| Q14 | سه قالب موجود → F06/F12/F15/F17 |
| Q15 | oracle ناقص → F03/F04/F07 |
| Q16 | review انجام‌نشده → F13/F14 |
| Q17 | CI/تحویل → F15/F17/F18 |
| G01/G02 | pending اضافه شده؛ گزارش/محیط هنوز ناقص → F02/F13 |
| G03/G05 | header عمومی و T04 موجود → رگرسیون F16 و مرور F14 |
| G04 | mapping موجود؛ role و Span ناقص → F08 |
| G06/G08 | کلیدها موجود؛ نقش‌ها و nested typography → F06/F08/F10 |
| G07 | فایل Bold موجود و Courier New انتخاب فعلی → F06/F12/F17 |
| G09/G10 | شمارش و extractor موجود ولی وفاداری ناکافی → F03/F04 |
| G11 | مقایسهٔ warning سطحی → F07 |
| G12 | helper SVG موجود ولی بی‌اتصال به پذیرش → F12 |
| G13 | نوشتهٔ فنی افزوده شده؛ عکس/provenance/نسبت ناقص → F00/F11/F17 |
| G14 | رندر تمام صفحات هنوز انجام‌نشده → F13/F14 |
| G15 | ZIP چهار قالب بررسی می‌شود؛ نصب مستقل نه → F15 |
| G16 | مستندات افزوده اما بخشی نادرست → F17 |
| G17 | بهداشت بدون حذف/commit اجباری تغییرات دیگران → F00/F17 |
| G18 | ۴۵ fixture واقعی بدون تصویر، محدودیت واقعی منبع است → ثبت در F00/F17؛ تکمیل syntheticها بدون جعل تصویر کتاب |
| درخواست تازهٔ crash Chrome | F01 و تست جلوگیری از تکرار + اجرای سالم همان macOS |

## ۷. معیار بسته‌شدن؛ «چیزی نمانده» دقیقاً یعنی چه؟

### ۷.۱. شروط فنی و بصری

| دروازه | شرط PASS | آنچه PASS نیست |
| --- | --- | --- |
| runtime Mermaid | نسخهٔ سازگار، render واقعی موفق، بدون پنجره crash یا child یتیم، failure bounded | فقط suppress پنجره یا skip تست |
| corpus | ۶۱ fixture معتبر و همهٔ قابلیت‌های وعده‌داده‌شده واقعاً در AST | شمار ۶۱ فایل یا فقط عنوان «پیچیده» |
| matrix | دقیقاً ۲۴۴ زوج مورد انتظار، exit/status درست | صفر زوج با exit0 یا missing/skip پنهان |
| حفظ محتوا | متن، شماره، کد، cell، link، image و footnote مطابق منبع | counts یکسان یا سه substring |
| DOCX | رابطه/part/type/schema سالم و بدون Word Repair | XML قابل parse به‌تنهایی |
| template | چهار طراحی، font/role/geometry/diagram theme مطابق قالب | چهار نام با نمودار بنفش یکسان |
| فارسی | متن منطقی و خواندن بصری درست با فونت واقعی | صرف w:bidi یا وجود نام Vazirmatn |
| تصویر/کد/Mermaid | خوانایی در ابعاد Word، aspect سالم، exact code و تمام labelها | PNG موجود، SVG مستقل سالم یا DPI بالا |
| pagination | بدون بریدگی/overlap/عنوان یتیم/caption اشتباه/صفحهٔ خالی غیرعمدی | چند صفحهٔ اول خوب |
| رندر | تمام صفحات LO و Word هدف با hash و نسخه | executable نصب‌شده یا metadata Pages |
| مرور | تمام صفحات دارای review و داوری موارد مبهم و شدید + مرور انسانی نمونه‌ای | pending یا accepted ساخته‌شده از oracle XML |
| بستهٔ نصب | import از wheel نصب‌شده در محیط تمیز، چهار قالب قابل مصرف | input بیرون checkout با import داخل src |
| تحویل | نمونهٔ قابل بازتولید، گزارش و محدودیت‌های روشن روی snapshot واحد | نوشتهٔ «همه انجام شد» در بالای پلن |

### ۷.۲. حداقل‌های کیفیت

- خطای محتوایی، شمارهٔ حذف‌شده، سلول جابه‌جا، رابطهٔ شکسته، label مفقود، crash محیط سالم، فایل repairشونده، font fallback ناشناخته و clipping: **صفر مورد باز**.
- body برای corpus فارسی حداقل اندازهٔ توافق‌شدهٔ قالب، code حداقل حدود ۹pt و label نمودار در اندازهٔ نهایی هدف حداقل حدود ۹pt باشد؛ کوچک‌کردن زیر حد برای پاس‌شدن تست ممنوع است. اعداد نهایی در مشخصات قالب پس از مرور نمونه تثبیت شوند.
- تفاوت نسبت تصویر بیشتر از ۱٪ بدون crop صریح رد شود. تلرانس هندسی کوچک برای گردکردن، مجوز بریدگی متن نیست.
- تفاوت تعداد صفحات Word/LO به‌تنهایی شکست نیست؛ baselineها engine-specific باشند. تغییر page count در engine ثابت نیازمند بررسی، نه snapshot update خودکار است.
- OCR و image diff کمک‌کننده‌اند؛ خطای OCR فارسی نباید متن منبع را تغییر دهد و mask ناحیهٔ مشکل‌دار برای سبزکردن diff مجاز نیست.
- تنها ایراد جزئی فاقد اثر بر خوانایی/یک‌دستی می‌تواند با شاهد و موافقت صریح کاربر پذیرفته شود؛ موارد الزامی «later» نشوند.

### ۷.۳. بستهٔ شواهد و تحویل

برای هر run نهایی این artifactها در مسیر مستقل و قابل حمل تحویل شوند:

| فایل/پوشهٔ پیشنهادی | محتوا |
| --- | --- |
| `run.json` | snapshot، ابزارها، فونت‌ها، hashes، command و state تمام مراحل |
| `matrix.csv` | ۲۴۴ ردیف fixture/template و وضعیت مجزای تبدیل/oracle/render/review |
| `runtime-health.json` | browser منتخب، compatibility، preflight، attempts، failure و cleanup |
| `semantic-diff.json` / `package-checks.json` | تطبیق محتوا و سلامت همهٔ part/relationshipها |
| پوشهٔ هر زوج | DOCX اصلی، media/دادهٔ میانی لازم، PDF/PNG هر موتور، page manifest |
| `elements.json` | نگاشت عنصر منبع به story/عنصر خروجی/صفحه در حد قابل اثبات؛ ابهام مشخص باشد |
| `issues.json` / `reviews.json` | issueها و وضعیت تک‌تک صفحات و مرزها، reviewer و hash |
| `index.html` | مرور محلی با لینک به Word، تصویر صفحه، source و issue؛ بدون HTML تزریق‌شده |
| `release-summary.md` | شمار واقعی pass/fail/skip/blocked، نقش AI/انسان، runtime و محدودیت‌ها |
| بستهٔ کاربر | چهار DOCX کتاب B00 و نمونه‌های غنی S09–S13، قالب‌ها و دستور بازتولید |

گزارش پایانی کوتاه ولی دقیق بگوید: چه نقص‌هایی رفع شدند، چه تست‌هایی اجرا شدند، چند زوج و چند صفحه بررسی شدند، Word کدام نسخه بود، آیا crash روی همان macOS برطرف شد و آیا چیزی هنوز pending است. اگر دروازه‌ای pending است، نام آن صریح بیاید و پروژه «بسته» اعلام نشود.

## ۸. موارد خارج از این دور و قواعد حفظ کار کاربر

این پلن برای بستن همین محصول و قرارداد آزموده‌شده است. خروجی `.doc` قدیمی، پوستهٔ چندبخشی دلخواه، HTML/CSS کامل، همهٔ LaTeX، دانلود خودکار عکس اینترنتی، افزودن وب‌سرویس و توسعهٔ تبدیل معکوس لازم نیستند. قابلیت معکوس موجود فقط در صورت مفیدبودن ابزار کمکی بررسی است؛ جای oracle مستقل قرار نگیرد.

هیچ runtime، برنامهٔ شخصی، profile مرورگر، crash report، `raw/`، artifact مرور کاربر یا فایل‌های دیگری که در git status دیده می‌شوند برای «تمیزشدن پروژه» خودکار حذف نشوند. برنامهٔ اصلاح باید کاربر را به خروجی قابل استفاده برساند، نه اینکه با حذف سناریوی سخت، ساکت‌کردن پیام خطا یا تغییر ادعاها عدد موفقیت را بالا ببرد.

**وضعیت این نوبت:** تنها این فایل پلن ساخته شد؛ هیچ‌یک از F00–F18 به‌عنوان رفع‌شده تحویل داده نمی‌شود و هیچ اصلاح محصول در این نوبت پیاده‌سازی نشده است.
