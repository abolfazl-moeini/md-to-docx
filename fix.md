# برنامهٔ اصلاحات بازبینی کد

تاریخ: 2026-09-05
دامنه: بازبینی پیاده‌سازی موجود در `src/md_to_docx` و تست‌های `tests`؛ این سند فقط برنامهٔ اصلاح است و هیچ کدی در این مرحله تغییر نمی‌کند.

## یافته‌های بحرانی

### F-01 — تبدیل واقعی Mermaid در محیط فعلی قابل اتکا نیست (P0)

تست `test_convert_all_fixtures_and_retain_on_disk` با `mmdc` موجود شکست می‌خورد چون Chromium/Puppeteer قابل اجرا نیست. `_find_mmdc_binary` صرفاً وجود shim را کافی می‌داند و تنظیم `--no-sandbox`/مسیر مرورگر را تضمین نمی‌کند؛ پیام خطا نیز راه تشخیص executable را کامل نشان نمی‌دهد.

**اصلاح:** bootstrap مستند و قابل تکرار برای Chromium، تشخیص executable پیش از اجرای mmdc، پشتیبانی از تنظیم `puppeteer_config` قالب و fallback کنترل‌شده به `npx`؛ خطا باید مسیر مرورگر و دستور رفع مشکل را چاپ کند. تست integration باید با fixture محیطی جدا و در CI قابل اجرا باشد و تست‌های واحد هرگز به نصب محلی وابسته نباشند.

### F-02 — مسیر فایل تصویر Mermaid به فایل موقت وابسته است (P0)

تصویرها در `TemporaryDirectory` ساخته و مسیر مطلق آن‌ها در Markdown/AST درج می‌شود. این در همین pipeline پیش از `save` کار می‌کند، اما API عمومی `process_mermaid_blocks` خروجی‌ای می‌دهد که بعد از پایان عمر پوشهٔ موقت غیرقابل استفاده است و هر مصرف‌کنندهٔ جداگانهٔ AST با فایل گمشده روبه‌رو می‌شود.

**اصلاح:** قرارداد مالکیت artifact را مشخص کنید: یا renderer باید همان‌جا تصویر را embed کند، یا خروجی‌ها به media directory پایدار کنار خروجی DOCX کپی شوند و فقط مسیرهای پایدار وارد AST شوند. تستی برای عمر مسیر پس از خروج از context اضافه شود.

### F-03 — بلوک Mermaid ناقص بی‌صدا پذیرفته می‌شود (P1)

`extract_mermaid_blocks` در صورت نبودن fence پایانی exception نمی‌دهد و تا انتهای فایل را بلوک تلقی می‌کند؛ ممکن است محتوای بعدی حذف یا جایگزین شود.

**اصلاح:** خطای نحوی مشخص با شمارهٔ خط برای fence ناقص، و تست برای fence باز، fenceهای تو در تو و چند بلوک پشت‌سرهم.

### F-04 — shell.docx پاک‌سازی نمی‌شود (P1)

وقتی `shell.docx` وجود دارد، `Document(shell)` باز می‌شود اما بدنهٔ قبلی پاک نمی‌شود؛ در نتیجه محتوای نمونه/placeholder همراه خروجی باقی می‌ماند، در حالی که قرارداد پلن حفظ sectPr/header/footer و جایگزینی بدنه است.

**اصلاح:** قبل از render، body را با حفظ `sectPr` خالی کنید؛ header/footer و section properties را نگه دارید. تست OOXML باید نبود placeholder و باقی‌ماندن header/footer را اثبات کند.

## یافته‌های مهم کیفیت محتوا

### F-05 — calloutها محتوای Markdown را تخت و ناقص می‌کنند (P1)

`render_single_block` برای Divهای note/warning همهٔ child blockها را با `blocks_to_text` به رشته تبدیل می‌کند و `render_callout` فقط پاراگراف ساده می‌سازد. Strong/Emph، لینک، فهرست، جدول، کد و چندپاراگرافی‌بودن کادر از بین می‌رود.

**اصلاح:** renderer کادر باید child AST را بازگشتی render کند یا یک renderer محدود ولی صریح برای Para/Plain/Code/List/Table داشته باشد؛ برای هر نوع، تست حفظ formatting و ترتیب اضافه شود.

### F-06 — AST adapter بخشی از ساختار Pandoc را حذف می‌کند (P1)

پشتیبانی واقعی برای RawInline/RawBlock، Note، Strikeout، Superscript/Subscript، DefinitionList، چند نوع جدول و تصویرهای دارای عنوان وجود ندارد و برخی inlineها به متن ساده تبدیل می‌شوند. این با هدف «Markdown فنی با کیفیت عالی» فاصله دارد.

**اصلاح:** ماتریس پشتیبانی اعلام‌شده در README، خطای صریح برای node ناشناخته به‌جای حذف خاموش، و fixture/test برای عناصر پشتیبانی‌شده و ردشده. ساختار Table باید با چند body/header و captionهای Pandoc 3 تست شود.

### F-07 — شماره/ترتیب ستون‌های RTL فقط با `bidiVisual` فرض شده است (P1)

کد `cell(0,0)` را badge/ستون اول می‌گیرد و سپس `w:bidiVisual` می‌گذارد، اما ترتیب بصری Word به نسخه/renderer وابسته است و تست فعلی فقط وجود attribute را می‌سنجد، نه ترتیب راست‌به‌چپ واقعی.

**اصلاح:** یک DOCX مرجع تولید و در Word/LibreOffice بررسی شود؛ ترتیب XML و `tblGrid` را بر اساس نتیجه تثبیت کنید. تست محتوایی باید اثبات کند badge و ستون «مفهوم» در سمت راست دیده می‌شوند، و برای حالت LTR نیز تست جدا باشد.

### F-08 — splitter دوطرفه اسکریپت‌ها کامل نیست (P1)

`split_bidi_runs` ارقام فارسی/عربی، علائم Unicode، slash، underscore و پرانتزهای مجاور لاتین را دقیق دسته‌بندی نمی‌کند؛ بعضی neutralها به run فارسی merge می‌شوند و ممکن است در Word جابه‌جا شوند.

**اصلاح:** جدول آزمون برای فارسی/عربی/لاتین/اعداد فارسی و لاتین/URL/نسخه‌ها/مسیرها/پرانتز و mixed-run؛ تعیین راهبرد صریح برای neutralها و افزودن تست XML/تصویری در Word.

## شکاف‌های پیکربندی و CLI

### F-09 — اعتبارسنجی قالب ناقص است (P1)

فقط وجود چند section و font/color بررسی می‌شود. نوع رنگ و hex، اندازه/حاشیه، direction، مقادیر heading، وجود فایل‌های ارجاع‌شده و سازگاری `schema_version` بررسی نمی‌شود؛ `_resolve_path` برای مسیر گمشده نیز exception نمی‌دهد.

**اصلاح:** schema validation کامل با خطای مسیر‌دار، بررسی فایل‌های theme/CSS/Puppeteer و font، محدودیت عددی برای page/margins/scale، و تست برای هر خطای پیکربندی. مسیرها همیشه نسبت به پوشهٔ قالب resolve شوند.

### F-10 — CLI قرارداد خطا و ورودی را کامل enforce نمی‌کند (P1)

وجود مسیر بررسی می‌شود اما directory می‌تواند از بررسی دستی عبور کند؛ `output == input`، overwrite ناخواسته و خطای permission نیز مدیریت قراردادی ندارند. دستور `templates validate` فقط YAML را load می‌کند.

**اصلاح:** الزام regular file، جلوگیری از خروجی برابر ورودی، گزینهٔ صریح overwrite، exit codeهای مستند، و اجرای contract validation همانند pipeline در CLI.

### F-11 — وابستگی‌ها و محیط اجرا reproducible نیستند (P1)

تست واقعی به pandoc، Chromium و فونت محلی وابسته است؛ نصب Python و Node در دو محیط جدا تعریف نشده و CI matrix وجود ندارد.

**اصلاح:** lock/constraint برای Python و npm، دستور bootstrap واحد، CI برای unit و integration جدا، و تستی که نبود هر binary را با پیام قابل فهم verify کند. artifactهای تولیدی در تست‌ها نباید روی فایل‌های tracked نوشته شوند.

## شکاف‌های تست و پذیرش

### F-12 — پوشش رفتار واقعی DOCX کافی نیست (P1)

بیشتر تست‌ها وجود XML را می‌سنجند و snapshot بصری/بازشدن واقعی سند ندارند. شکست فعلی integration نشان می‌دهد مسیر طلایی بدون محیط کامل قابل تکرار نیست.

**اصلاح:** تست unzip/XPath برای همهٔ اجزای مهم، بازکردن با parser مستقل، render به PDF/PNG در محیط CI و بررسی چشمی نمونه؛ خروجی تست در temp ساخته شود. معیار پذیرش برای page break، caption، جدول چندصفحه‌ای و فونت fallback اضافه شود.

### F-13 — تست TDD و fixtureها با قرارداد plan هم‌راستا نیستند (P2)

fixtureهای متعدد خارج از fixture طلایی اضافه شده‌اند، اما برای formatهای پشتیبانی‌نشده یا خطاهای ساختاری تست قراردادی وجود ندارد؛ یک تست integration به‌صورت ناخواسته در اجرای پیش‌فرض fail می‌شود.

**اصلاح:** markerها را درست اعمال کنید، integration را به محیط آماده محدود کنید، تست‌های منفی را اضافه کنید و fixture policy را مستند کنید.

### F-14 — امنیت و پاک‌سازی منابع بررسی نشده است (P2)

متن Markdown و عنوان‌های YAML مستقیماً وارد command arguments/OOXML می‌شوند؛ فایل‌های موقت و خروجی‌های شکست‌خورده policy مشخصی ندارند.

**اصلاح:** همهٔ commandها با argv ساختاری اجرا شوند، طول/کاراکترهای عنوان و مسیر validate شوند، cleanup در `finally` تضمین شود، و تست عناوین دارای quote/newline و مسیرهای Unicode اضافه شود.

## ترتیب اجرای پیشنهادی

1. F-01 تا F-04: پایدارسازی pipeline، Mermaid و shell template.
2. F-05 تا F-08: حفظ محتوای Markdown و اصلاح RTL/ترتیب بصری.
3. F-09 تا F-11: قرارداد قالب، CLI و bootstrap قابل تکرار.
4. F-12 تا F-14: تست بصری، fixture policy، امنیت و مستندسازی.

هر مورد باید با تست قرمز شروع شود، سپس حداقل اصلاح لازم انجام و در پایان unit/integration/visual checks مرتبط اجرا شود. این فایل برنامهٔ اصلاح است و اجرای موارد بالا باید در تغییر جداگانه انجام شود.
