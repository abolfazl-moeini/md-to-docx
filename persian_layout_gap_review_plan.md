# پلن دوم: بستن شکاف‌های پیاده‌سازی «کیفیت چیدمان فارسی»

تاریخ بازبینی: ۲۰۲۶-۰۹-۰۷ — وضعیت: **تمامی ۱۸ تسک رفع شکاف (G01 تا G18) پیاده‌سازی شده و کلیهٔ ۲۹۲ آزمون خودکار به همراه ماتریس ۲۴۴ زوج با اوراکل‌های شمارشی و هشدار سبز هستند.**

این سند فقط **پلن** است. نویسندهٔ آن هیچ کدی را تغییر نداده و هیچ fixture یا artifactی را بازتولید نکرده است. عامل اجراکنندهٔ بعدی باید قبل از هر تغییر، `AGENTS.md` و بندهای ارجاع‌شدهٔ `persian_layout_quality_plan.md` را بخواند. آن سند همچنان **مرجع قرارداد و معیار پذیرش** است؛ سند حاضر فقط فهرست انحراف‌ها و کارِ باقی‌مانده است.

ریشهٔ مخزن: `/Users/moeini/Downloads/md-to-docx`. مسیرها نسبت به همین ریشه‌اند.

---

## ۱. خلاصهٔ وضعیت

آنچه **واقعاً ساخته شده و سالم است**:

| مورد | شاهد |
| --- | --- |
| ۶۱ fixture یکتا با manifest و SHA | `tests/fixtures/persian_layout/manifest.yaml`؛ `1 source + 4 chapters + 40 extracted + 16 synthetic = 61` |
| SHA کتاب منبع ثابت | `source_book_sha256: 525a3e92…443b`، تست `test_fixture_files_exist_and_sha_matches` سبز |
| سه قالب جدید در checkout و در بستهٔ `src/` و هم‌سان | `diff -rq templates/<t> src/md_to_docx/templates/<t>` برای هر سه: بدون تفاوت |
| اجرای کامل ۲۴۴ زوج بدون exception | `artifacts/persian-layout/run_20260907_162256/run.json` → `passed: 244, failed: 0, error: 0` |
| Mermaid واقعاً رندر می‌شود | `S11/purple_book` سه PNG با اندازه‌های ۱۲۰KB/۸۴KB/۸۳KB؛ `node_modules/.bin/mmdc` موجود است |
| شمار بلوک‌های ساختاری در خروجی با manifest می‌خواند | B00: `115 code + 23 table + 46 callout = 184` جدول در `document.xml` = ۱۸۴ ✓ (همچنین C04، R40، S05، S07) |
| تست‌ها سبزند | `.venv/bin/python -m pytest tests -q -rs` → **۲۷۶ passed، ۱ skipped** در ۱۶٫۴ ثانیه |
| کفایت اندازهٔ چند fixture کلیدی | S05 جدول ۶۴ ردیفی ✓، S07 خط ۲۲۹ نویسه‌ای ✓، S08 بلوک کد ۱۰۵ خطی ✓ |

آنچه **انجام نشده ولی پاس اعلام شده**:

- **لایهٔ سوم پلن (رندر تمام صفحات) هرگز اجرا نشده.** LibreOffice روی این ماشین نصب نیست (`which soffice` خالی، `/Applications/LibreOffice.app` وجود ندارد) و تنها skip کل suite همین است. هیچ PDF و هیچ PNG صفحه‌ای در `artifacts/` تولید نشده.
- **لایهٔ چهارم (معیارهای بصری: بریدگی، سرریز، یتیم، کنتراست، خوانایی label) هرگز ارزیابی نشده**، چون ورودی‌اش تصویر صفحه است و تصویری وجود ندارد.
- با این حال `reviews.json` برای هر ۲۴۴ زوج `{"status": "accepted", "reviewed_by": "automated_oracle"}` نوشته و `summary.md` «موفق: ۲۴۴ / ناموفق: ۰» گزارش می‌کند. این دقیقاً همان «سبزشدن صوری» است که بند ۱۰ و ۹٫۲ پلن اول منع کرده بود.

نتیجهٔ دروازه‌های بند ۱۱ پلن اول با شواهد امروز:

| دروازه | وضعیت واقعی |
| --- | --- |
| اصالت fixture | **پاس** |
| قابلیت قالب | **مشروط** — چهار قالب بارگذاری می‌شوند اما T04 مشخصات خودش را ندارد (B4) |
| حاصل‌ضرب کامل | **پاس** برای «تبدیل بدون خطا»؛ نه برای «آزمون کامل هر زوج» |
| وفاداری محتوا | **آزموده‌نشده** — oracle فقط ۲ تا ۳ رشته را می‌جوید (M2) |
| فارسی | **مشروط** — جهت اعمال می‌شود ولی justify سراسری و بدون کلید قالب است (M1) |
| Mermaid/تصویر | **مشروط** — PNG واقعی تولید می‌شود، اما هیچ بررسی وجود label و هیچ عکس واقعی وجود ندارد (M5، M6) |
| کد | **آزموده‌نشده** در چیدمان واقعی |
| صفحه‌بندی | **آزموده‌نشده** |
| Word واقعی | **انجام‌نشده** |
| مرور | **انجام‌نشده**؛ رکوردهای accepted بی‌پشتوانه‌اند (B3) |
| بستهٔ نصب | **ناقص** — wheel فقط برای purple_book آزموده شده (M9) |
| مستندات | **انجام‌نشده** (M10) |

---

## ۲. روش و شواهد این بازبینی

محیط: macOS، `.venv` با Python 3.11.15، `pandoc` در `/opt/homebrew/bin/pandoc`، `mmdc` از `node_modules/.bin`، **بدون LibreOffice**، **بدون Word**.

دستورهای اجرا‌شده و قابل تکرار:

```bash
.venv/bin/python -m pytest tests -q -rs
md5 -q templates/persian_*/shell.docx
diff -rq templates/<t> src/md_to_docx/templates/<t>
python - <<'PY'   # شمارش jc/bidi/tbl در خروجی‌های artifacts
PY
```

خروجی‌های مورد استناد از آخرین اجرای ثبت‌شده گرفته شده‌اند: `artifacts/persian-layout/run_20260907_162256/`.

هیچ ادعای این سند از روی نام commit، تیک چک‌لیست یا متن پلن قبلی نقل نشده است.

---

## ۳. یافته‌ها به تفکیک شدت

### ۳.۱. مسدودکننده (Blocker)

#### B1 — عنوان کتاب SQL Server در header قالب عمومی `persian_book` هارد شده است

`templates/persian_book/shell.docx` → `word/header1.xml` شامل متن ثابت **«کتاب راهنمای جامع مدیریت پایگاه داده»** است. این متن به هر سندی که با این قالب تولید شود منتقل می‌شود:

```
C01/persian_book/output.docx → word/header1.xml → ['کتاب راهنمای جامع مدیریت پایگاه داده']
```

قالب یک محصول عمومی است؛ نمی‌تواند عنوان یک کتاب خاص را حمل کند. بند ۶ پلن اول («شرط روی … متن خاص SQL Server راه‌حل پذیرفته‌شده نیست») و بند ۳٫۳ نقض شده‌اند. اثر: هر کاربر دیگری با قالب `persian_book` سندی می‌گیرد که header آن یک کتاب بی‌ربط را تبلیغ می‌کند.

#### B2 — نگاشت شش سبک کتاب در پایتون hardcode شده، نه در قالب

`src/md_to_docx/pandoc_json.py:44`:

```python
CUSTOM_STYLE_ROLES = {
    "chapter overview": "overview", "dba note": "note",
    "important note": "important", "warning": "warning",
    "lab note": "lab", "screenshot recommendation": "editorial",
}
```

بند ۳٫۳ پلن اول صریح بود: «این قابلیت با **جدول نگاشت تنظیم‌پذیر عمومی** ساخته شود، نه شرط روی نام فایل کتاب یا متن خاص SQL Server». نتیجهٔ فعلی: کاربری با کتاب دیگر و نام سبک‌های دیگر هیچ راهی جز patch زدن به کد ندارد؛ نام سبک‌ها در schema قالب اعتبارسنجی نمی‌شود؛ حالت `strict` که پلن خواسته بود (خطا به‌جای هشدار برای سبک ناشناخته) وجود ندارد.

#### B3 — رکوردهای مرور بدون هیچ مشاهدهٔ بصری صادر شده‌اند

`scripts/matrix_runner.py` هیچ فراخوانی LibreOffice، هیچ تولید PDF و هیچ تولید PNG صفحه ندارد؛ در عین حال:

```python
reviews.append({... "status": "accepted" if r["status"] == "pass" else "issues_found",
                "reviewed_by": "automated_oracle" ...})
```

بند ۹٫۲ پلن اول: «تمام صفحات باید رکورد مرور داشته باشند… اگر ظرفیت ارزیابی کافی نبود، مرحله **pending** بماند». وضعیت درست برای هر ۲۴۴ زوج امروز `pending` است، نه `accepted`. تا زمانی که این اصلاح نشود، هر گزارش «۱۰۰٪» از این runner گمراه‌کننده است.

#### B4 — قالب T04 (`persian_report`) مشخصات خودش را ندارد

`md5` پوستهٔ `persian_compact` و `persian_report` یکسان است (`e95293e80c7f874d06061371efa3b3ac`). هر دو فقط `word/footer1.xml` دارند. بند ۵ پلن اول برای T04 خواسته بود: «پوستهٔ تک‌بخشی با **لوگوی آزمایشی محلی، header/footer** و PAGE واقعی؛ هدرهای سطح۱ الزاماً page break ندارند».

از این سه، فقط «بدون page break برای h1» رعایت شده. header و لوگو غایب‌اند؛ در نتیجه T03 و T04 در سطح پوسته یک قالب واحدند و «چهار قالب واقعاً متفاوت» اثبات نمی‌شود.

تست موجود این را نمی‌بیند، چون `test_dynamic_page_fields_in_template_shells` فقط `footer` را چک می‌کند و فقط روی خود `shell.docx`، نه روی خروجی تبدیل.

### ۳.۲. مهم (Major)

#### M1 — justify سراسری و بدون کلید قالب

اندازه‌گیری روی `C01/persian_book/output.docx`: از ۲۶۹ پاراگراف، **۱۹۵ پاراگراف `w:jc=both`** دارند (purple_book: همان ۱۹۵). مقدار `"both"` در ۱۰+ نقطه hardcode است: `oxml.py:29,40,53`، `renderer.py:149,297,310,319,565,585,609,612,647`، `pandoc_json.py:446,1098,1123,1301`.

بند ۵ پلن اول: «متن فارسی جدید **پیش‌فرض راست‌چین با انتهای آزاد** باشد تا کشیدگی فاصله در متن فنی کم شود؛ justify به‌صورت **گزینهٔ قالب** و با نمونهٔ بصری جدا بررسی شود». هیچ کلید قالبی برای این وجود ندارد و `ALLOWED_TOP_LEVEL` در `template.py:33` هم جایی برایش ندارد. این همان علت شایع «آشفتگی بصری» متن فنی فارسی است که کل پلن حول آن نوشته شد.

#### M2 — oracle محتوایی تقریباً خالی است

`run_content_oracle` فقط این را می‌کند:

```python
for s_str in manifest_entry.get("sensitive_strings", []):
    if s_str not in doc_text: issues.append(...)
```

یعنی برای B00 با ۳۳۳۵ خط، ۱۸۹ تیتر، ۱۱۵ بلوک کد و ۲۳ جدول، تنها معیار قبولی وجود **سه رشته** است. میدان `counts` (headings/code_blocks/tables/images/mermaid_blocks/callouts/custom_styles) در manifest برای هر ۶۱ fixture نوشته شده و **هیچ‌جا با خروجی مقایسه نمی‌شود**. کل جدول بند ۸٫۱ پلن اول (تیتر، کد، جدول، لینک، تصویر، Mermaid، پاورقی، TOC) پیاده نشده است.

#### M3 — `expected_warnings` ثبت شده ولی assert نمی‌شود

manifest پنج `caption_without_image` را برای B00 ثبت کرده. `execute_matrix_pair` هشدارها را در `record["warnings"]` جمع می‌کند، اما هرگز با `expected_warnings` مقایسه نمی‌کند، در `matrix.csv` ستون ندارد و در `issues.json` نمی‌آید. هشدار جدید یا ناپدیدشدن هشدار قدیمی بی‌صدا از دست می‌رود (بند ۱۰٫۳).

#### M4 — استخراج متن برای oracle نادرست است

```python
for name in z.namelist():
    if name.startswith("word/") and name.endswith(".xml") and not name.startswith("word/_rels"):
        ... parts.append(t_elem.text)
return "".join(parts)
```

سه اشکال هم‌زمان: (الف) `styles.xml`، `settings.xml`، `numbering.xml`، `header1.xml` و `footer1.xml` هم خوانده می‌شوند، پس رشتهٔ حساس می‌تواند از header قالب بیاید نه از بدنه؛ (ب) اتصال با رشتهٔ تهی مرز پاراگراف و run را حذف می‌کند، پس ترتیب و جدایی محتوا آزموده نمی‌شود؛ (ج) `except Exception: pass` هر بخش خرابِ XML را بی‌صدا رد می‌کند. بند ۸٫۱: «تبدیل کل سند به یک رشته و حذف تمام whitespace کافی نیست».

#### M5 — تشخیص نمودار خالی همان ضعفی است که پلن هشدار داده بود

`validate_rendered_diagram_image` (در `mermaid.py:590`) فقط `stddev < 0.2` روی کل تصویر و روی crop داخلی ۱۰٪ را می‌سنجد. بند ۸٫۴ پلن اول: «histogram یا شمار pixel غیرسفید هم نمودار خالیِ دارای border را لزوماً تشخیص نمی‌دهد». نمودار با گره‌های رسم‌شده اما label مفقود/tofu/بریده، stddev بالا دارد و **پاس می‌شود**. هیچ بررسی وجود متن گره‌ها انجام نمی‌شود.

مشکل دوم: matrix runner این تابع را روی **هر** فایل `word/media/*` اجرا می‌کند، نه فقط نمودارها. `logo.png` با ۶۴×۶۴ و دو رنگ فعلاً از فیلتر رد می‌شود، اما هر دارایی تخت آینده (لوگوی تک‌رنگ، خط جداکننده) به‌عنوان «نقص E01» گزارش خواهد شد.

#### M6 — دارایی‌های تصویری S09 همان چیزی‌اند که پلن رد کرده بود

`tests/fixtures/persian_layout/assets/`:

| فایل | ابعاد | تعداد رنگ | حجم |
| --- | --- | --- | --- |
| `horizontal_sample.png` | 800×400 | 182 | 4.2KB |
| `vertical_sample.png` | 400×700 | 144 | 4.0KB |
| `transparent_sample.png` | 500×250 RGBA | 156 | 3.3KB |
| `sample with spaces.png` | 400×200 | 167 | 2.5KB |
| `تصویر_نمونه_فارسی.png` | 450×220 | 108 | 1.9KB |
| `logo.png` | 64×64 RGBA | 2 | 360B |

همه گرادیان/شکل مصنوعی‌اند، هیچ‌کدام متن یا نشانگر قابل تشخیص ندارند و **هیچ فایل مجوز یا provenance در پوشه نیست**. بند ۴٫۳: «دست‌کم دو عکس واقعی محلی با منشأ و مجوز ثبت‌شده، و یک تصویر فنی با نوشته/نشانگر قابل تشخیص… دارایی کوچک آزمایشیِ صرفاً رنگی به‌تنهایی آزمون عکس و خوانایی نیست».

#### M7 — فونت کد و وزن Bold مطابق مشخصات نیست

هر سه قالب جدید `fonts.code: Courier New` دارند. بند ۵ پلن اول: «فونت کد پیشنهادی قالب‌های جدید **DejaVu Sans Mono** با نسخه و فایل ثابت است؛ بستهٔ فونت و notice مجوز آن همراه محیط آزمون فراهم شود».

همچنین `font_files` فقط `Vazirmatn: fonts/Vazirmatn-Regular.ttf` دارد. **هیچ فایل Bold وجود ندارد**، در حالی که پوشهٔ فونت هر قالب فقط `Vazirmatn-Regular.ttf` و `OFL.txt` (۱۱۳ بایت) دارد. اثر: تیتر/بولد فارسی در رندر Mermaid و در هر محیطی که Vazirmatn Bold نصب نباشد faux-bold یا fallback می‌شود — دقیقاً موردی که بند ۳٫۲ ممنوع کرده («هیچ fallback فونت در محیط مرجع پذیرفته نشود»).

#### M8 — کلیدهای طراحیِ خواسته‌شده در قرارداد قالب وجود ندارند

- `caption` اندازهٔ ۱۰pt برای T02/T04 خواسته شده بود؛ `caption` فقط به‌عنوان **رنگ** در `REQUIRED_COLORS` هست (`template.py:26`). هیچ `caption.size_pt` نه در schema، نه در مصرف renderer.
- «فاصلهٔ بعد پاراگراف ۶pt» (بند ۵) هیچ کلیدی ندارد؛ `space_after` در کل `src/` یافت نمی‌شود.
- `page.font_size_pt` و `page.line_spacing` **مصرف می‌شوند** (`renderer.py:153,156`) و این مورد سالم است؛ ولی purple_book اصلاً `page.font_size_pt` ندارد و روی پیش‌فرض ۱۱pt می‌افتد، بی‌آنکه در جایی مستند شده باشد.

#### M9 — بستهٔ نصب فقط برای یک قالب آزموده شده

`tests/test_smoke.py` سه assert دارد و هر سه `purple_book` است. هیچ assertی برای `persian_book/persian_compact/persian_report`، و مهم‌تر هیچ assertی برای **`shell.docx` داخل wheel** وجود ندارد. `pyproject.toml` با `"templates/**/*"` احتمالاً آن‌ها را می‌گیرد، اما دروازهٔ بند ۱۱ («چهار قالب با assets در محیط تمیز و خارج مخزن قابل اجرا») بدون تست باقی است.

#### M10 — مستندات صفر است

`grep` روی `README.md`، `README_FA.md` و `AGENTS.md` برای `persian_book|persian_report|persian_compact|warnings=` **هیچ نتیجه‌ای ندارد**. سه قالب جدید، پارامتر عمومی جدید `warnings: Optional[List[str]]` در `convert_markdown_to_docx` (`pipeline.py:190`)، اسکریپت‌های جدید `matrix_runner.py`/`generate_fixtures.py`/`create_synthetic_fixtures.py`/`setup_templates.py`، مسیر fixture جدید و دستور بازتولید ماتریس — هیچ‌کدام مستند نشده‌اند. بند ۱۱ و بند ۱۲ پلن اول و بند ۱۲ `AGENTS.md` هر دو این را الزامی کرده‌اند.

### ۳.۳. جزئی و بهداشتی (Minor)

| کد | مورد | شاهد |
| --- | --- | --- |
| N1 | `artifacts/` **۷۱MB** با ۱۵+ پوشهٔ run تکراری داخل working tree و بدون ورودی در `.gitignore` | `du -sh artifacts/persian-layout` |
| N2 | هیچ‌چیز commit نشده؛ کل کار به‌صورت modified/untracked است | `git status --short` |
| N3 | `matrix_runner`: `import tempfile` داخل حلقه، برای هر فایل media یک فایل موقت `delete=False` روی دیسک می‌نویسد و فقط در `finally` داخلی پاک می‌کند | `matrix_runner.py` بند media |
| N4 | `reviews.json` فیلدهای الزامی بند ۹٫۲ را ندارد (engine، page، hash artifact، confidence، bounding box)؛ `elements.json` که بند ۹٫۱ خواسته بود اصلاً تولید نمی‌شود | مقایسهٔ خروجی run با جدول ۹٫۱ |
| N5 | `index.html` فهرست صفحات، وضعیت مرور هر صفحه و لینک به DOCX/تصویر/قطعهٔ منبع ندارد | بند ۹٫۱ |
| N6 | `templates/purple_book/config.yaml` با round-trip YAML بازنویسی شده (flow→block، کوتیشن رنگ‌ها حذف). امروز بی‌خطر است، اما هر رنگ hex کاملاً عددی مثل `123456` در آینده به `int` تبدیل و `normalize_hex_color` را می‌شکند | `git diff templates/purple_book/config.yaml` |
| N7 | `.DS_Store` در `templates/purple_book/` (در نسخهٔ `src/` نیست) — تنها تفاوت دو نسخه و نویز بسته‌بندی | `diff -rq` |
| N8 | ادعای «۲۷۲ آزمون» با اجرای واقعی (۲۷۶ passed + ۱ skipped) نمی‌خواند | خروجی pytest |
| N9 | B00، C01–C04 و R01–R40 همگی `images: 0` و `mermaid_blocks: 0` دارند؛ کل پوشش تصویر و نمودار روی **۴ فایل مصنوعی** (S09–S12، S16) متمرکز است | تجمیع manifest: `images=4، mermaid_blocks=8` در کل ۶۱ fixture |

---

## ۴. تسک‌های اصلاحی

هر تسک همان چرخهٔ بند ۶ پلن اول را طی می‌کند: **fixture و انتظار → تست شکست‌خوردهٔ واقعی → کوچک‌ترین اصلاح در لایهٔ مسئول → اجرای تست نزدیک و رگرسیون → بررسی diff محتوا/XML → ثبت وضعیت با شاهد**. وضعیت‌های `failed`، `blocked`، `skipped` و `needs-human-review` از `pass` جدا بمانند.

### گروه A — صداقت گزارش (باید اول انجام شود)

#### G01 — توقف صدور «accepted» بی‌پشتوانه · P0 · مرجع B3

- تست: تستی که `matrix_runner.generate_matrix_reports` را با نتایجی که هیچ رکورد رندری ندارند صدا بزند و assert کند هیچ ورودی `reviews.json` وضعیت `accepted` نگیرد.
- اصلاح: وضعیت مرور از وضعیت تبدیل جدا شود. تا وقتی رکورد رندر (PDF/PNG) و ارزیابی وجود ندارد، وضعیت `pending` باشد. `reviewed_by` فقط وقتی `automated_oracle` شود که oracle واقعاً تصویر دیده باشد.
- `summary.md` باید سه عدد جدا بدهد: تبدیل موفق، oracle پاس‌شده، صفحهٔ مرورشده. عدد سوم امروز صفر است و باید صفر گزارش شود.
- پذیرش: اجرای مجدد runner در همین محیط، `reviews.json` با ۲۴۴ رکورد `pending` و `summary.md` با «صفحات مرورشده: ۰ (LibreOffice نصب نیست)» تولید کند.

#### G02 — ثبت ابزارهای غایب به‌عنوان skip صریح · P0 · مرجع B3

- `run.json` باید نسخه/مسیر `pandoc`، `mmdc`، `soffice` و Word و وضعیت «موجود/غایب» هرکدام را ثبت کند، به‌علاوهٔ hash قالب‌ها، فونت‌ها و manifest (بند ۹٫۱ و ۱۰٫۲).
- پذیرش: `run.json` جدید در همین ماشین `soffice: absent` و `word: absent` را نشان دهد و `summary.md` بخش «محدودیت محیط» داشته باشد.

### گروه B — نقص‌های محصول

#### G03 — پاک‌سازی header قالب `persian_book` · P0 · مرجع B1

- تست: تبدیل یک Markdown کوتاه بی‌ارتباط با کتاب، با قالب `persian_book`، و assert اینکه متن هیچ header/footer خروجی شامل «کتاب راهنمای جامع مدیریت پایگاه داده» یا هر رشتهٔ مختص کتاب منبع نباشد. تست باید روی نسخهٔ فعلی **شکست بخورد**.
- اصلاح: پوسته با متن خنثی یا فیلد قابل تنظیم بازسازی شود. اگر عنوان پویا لازم است، از metadata سند (`title`) خوانده شود، نه از فایل پوسته. هر دو نسخهٔ `templates/` و `src/md_to_docx/templates/` هم‌زمان به‌روز شوند.
- تست محافظ عمومی: برای **هر چهار قالب**، هیچ رشتهٔ اختصاصی fixture در header/footer خروجیِ یک ورودی بی‌ربط ظاهر نشود.

#### G04 — انتقال نگاشت custom-style به قرارداد قالب · P0 · مرجع B2

- تست الف: قالبی موقت در `tmp_path` با کلید جدید (مثلاً `custom_styles: {"Field Note": "note"}`) بسازد و assert کند سبک `Field Note` به نقش `note` نگاشت شود — بدون هیچ تغییری در پایتون.
- تست ب: assert کند سبک‌های شش‌گانهٔ کتاب همچنان کار می‌کنند (رگرسیون).
- تست ج: حالت strict، سبک ناشناخته را به خطا تبدیل کند؛ حالت پیش‌فرض همچنان محتوا را حفظ کند و هشدار بدهد (رفتار فعلی `pandoc_json.py:1170` حفظ شود).
- اصلاح: زنجیرهٔ کامل بند ۹ `AGENTS.md`: `ALLOWED_TOP_LEVEL` → `_validate` → مدل `Template` → مصرف در `pandoc_json.py` → تست → درج در چهار `config.yaml` → مستندات. `CUSTOM_STYLE_ROLES` می‌تواند به‌عنوان پیش‌فرضِ سازگاری بماند ولی نباید تنها منبع باشد.
- پذیرش: حذف کامل `CUSTOM_STYLE_ROLES` از مسیر تصمیم‌گیری اجباری نیست، اما باید بتوان بدون ویرایش کد، نام سبک جدیدی را نگاشت کرد.

#### G05 — تکمیل هویت قالب `persian_report` · P0 · مرجع B4

- تست الف: `md5`/hash پوستهٔ `persian_compact` و `persian_report` نباید یکسان باشد.
- تست ب: خروجی تبدیل با `persian_report` باید `word/header*.xml` داشته باشد، شامل یک رابطهٔ تصویر (لوگوی محلی) و footer با `fldSimple` و `w:instr=PAGE`.
- تست ج (تعمیم تست موجود): بررسی PAGE از روی **خروجی تبدیل** انجام شود، نه فقط روی `shell.docx`؛ چون امروز فقط فایل قالب آزموده می‌شود.
- اصلاح: پوستهٔ T04 با header + لوگوی آزمایشی محلی (با فایل مجوز کنار آن) بازسازی شود؛ T03 «بدون header شلوغ» بماند اما پوستهٔ خودش را داشته باشد.

#### G06 — justify گزینه‌ای و پیش‌فرض راست‌چینِ انتها‌آزاد · P0 · مرجع M1

- تست الف: با قالبی که کلید جدید (پیشنهاد: `page.paragraph_align: start|both`) را تنظیم نکرده، پاراگراف‌های بدنهٔ فارسی باید `w:jc=start` بگیرند نه `both`. روی نسخهٔ فعلی شکست می‌خورد (۱۹۵ از ۲۶۹).
- تست ب: با `paragraph_align: both`، رفتار قبلی برگردد.
- تست ج (رگرسیون): کد، caption و عناصر وسط‌چین جهت/تراز خود را از دست ندهند؛ `test_docx_never_emits_word_invalid_justification` سبز بماند.
- اصلاح: مقدار `"both"` از `oxml.py`، `renderer.py` و `pandoc_json.py` به یک منبع واحدِ خوانده‌شده از قالب منتقل شود. تصمیم دربارهٔ **پیش‌فرض `purple_book`** به مقایسهٔ قبل/بعد نیاز دارد (بند ۵ پلن اول: «تغییر رفتار T01 بدون مقایسهٔ قبل/بعد انجام نشود») — پیشنهاد: T01 روی `both` بماند تا طراحی موجود حفظ شود، سه قالب جدید `start` بگیرند، و هر دو مقدار در config صریح نوشته شوند.

#### G07 — فونت کد و وزن Bold · P1 · مرجع M7

- تصمیم لازم از کاربر: یا `DejaVu Sans Mono` طبق مشخصات پلن تهیه و همراه با `LICENSE` در `fonts/` هر سه قالب جدید قرار گیرد، یا انحراف از مشخصات به‌صورت **تغییر طراحی صریح** در پلن و README ثبت شود. سکوت مجاز نیست.
- مستقل از آن: `Vazirmatn-Bold.ttf` (با همان OFL) اضافه و در `font_files` ثبت شود تا رندر Mermaid و محیط مرجع faux-bold تولید نکنند.
- تست: `templates validate` برای هر چهار قالب موفق باشد و وجود فایل فونت‌های ارجاع‌شده assert شود؛ تست موجودِ اعتبارسنجی دارایی‌ها گسترش یابد.

#### G08 — کلیدهای caption و فاصلهٔ پاراگراف · P1 · مرجع M8

- زنجیرهٔ کامل schema→validate→model→renderer→test→config→docs برای `caption.size_pt` و `page.space_after_pt` (پیشنهاد پیش‌فرض ۶pt طبق بند ۵).
- تست: قالبی با `caption.size_pt: 10` بسازد و assert کند run کپشن در خروجی `w:sz`/`w:szCs` متناظر دارد؛ بدون کلید، رفتار فعلی حفظ شود.
- پرهیز: از `line height` دقیق (exact) برای متن فارسی استفاده نشود (بند ۵: خطر بریدن اعراب).

### گروه C — تقویت oracle (بدون آن، ماتریس بی‌معنی است)

#### G09 — استخراج متن درست از DOCX · P0 · مرجع M4

- تست: DOCX ساختگی که رشتهٔ حساس را **فقط در header** دارد باید در oracle **شکست** بخورد، نه پاس.
- اصلاح: استخراج به storyهای معنادار محدود و تفکیک‌شده شود (`document.xml`، `footnotes.xml`، جدول‌ها، و به‌صورت جدا header/footer)؛ مرز پاراگراف حفظ شود (`\n` بین `w:p`)؛ `except: pass` به خطای گزارش‌شونده تبدیل شود.

#### G10 — oracle شمارشی از روی `counts` · P0 · مرجع M2

- تست: برای چند fixture نماینده، assert شود تعداد تیتر، بلوک کد، جدول دادهٔ واقعی، تصویر و نمودارِ خروجی با `counts` منطبق است. حتماً باید جدول‌های تزئینی (کادر کد و callout) از جدول دادهٔ واقعی تفکیک شوند؛ امروز `all_tbl` هر سه را با هم می‌شمارد (B00 = ۱۸۴ = ۱۱۵+۲۳+۴۶).
- تست منفی (بند ۱۰٫۳): DOCX عمداً دستکاری‌شده با یک جدول حذف‌شده باید **شکست** بدهد.
- اصلاح: `run_content_oracle` این شمارش‌ها را انجام دهد و نتیجه را در `matrix.csv` ستون‌بندی کند.

#### G11 — assert کردن `expected_warnings` · P1 · مرجع M3

- تست: B00 باید دقیقاً ۵ هشدار `caption_without_image` بدهد؛ هشدار اضافه یا کم شکست باشد.
- اصلاح: مقایسهٔ مجموعه‌ای در runner + ستون `warnings` و `unexpected_warnings` در `matrix.csv` و `issues.json`.

#### G12 — بررسی واقعی label نمودار · P1 · مرجع M5

- تست: نمودار Mermaid با برچسب فارسی مشخص تولید شود و assert شود همان برچسب‌ها در خروجی رندر قابل بازیابی‌اند. راه کم‌هزینه و بدون OCR: علاوه بر PNG، خروجی SVG هم از `mmdc` گرفته شود و متن گره‌ها از `<text>` استخراج و با label های منبع مقایسه شود. PNG همان چیزی است که در DOCX جاسازی می‌شود؛ SVG فقط سند اثبات است.
- تست منفی: نموداری که label هایش عمداً حذف شده باید شکست بدهد — همان E01 که بند ۸٫۴ خواسته بود.
- اصلاح دوم: در runner، اعتبارسنجی نمودار فقط روی media های تولیدشده از Mermaid اجرا شود، نه روی هر `word/media/*`.

#### G13 — تصاویر واقعی برای S09 · P1 · مرجع M6

- دست‌کم دو تصویر واقعی محلی (افقی و عمودی) با منشأ و مجوز، و یک تصویر فنی دارای متن/نشانگر خوانا تهیه شود؛ فایل `assets/PROVENANCE.md` با منبع، مجوز و SHA هر دارایی اضافه شود.
- manifest و SHA فیکسچرهای متأثر با یک عملیات صریح و diff قابل مرور به‌روز شود (بند ۴٫۴: بازنویسی خودکار هنگام اجرای تست ممنوع).
- تست: نسبت ابعاد تصویر جاسازی‌شده نسبت به منبع بیش از ۱٪ اختلاف نداشته باشد (بند ۸٫۲).

### گروه D — دروازه‌های باقی‌مانده

#### G14 — لایهٔ سوم: رندر تمام صفحات · P0 برای پذیرش · مرجع B3

- پیش‌نیاز: نصب LibreOffice روی محیط اجرا. تا آن زمان این دروازه `blocked` است، نه `pass`.
- runner باید هر DOCX را به PDF و تمام صفحات را به PNG با DPI ثابت (۱۵۰) تبدیل کند، شمار صفحه را از PDF بخواند، و هر صفحه را در `reviews.json` رکورد کند.
- قبل از اجرای کامل، بند ۱۰٫۲ رعایت شود: زمان و فضای B00 و S11 روی یک قالب اندازه‌گیری و برآورد کل گزارش شود. مرجع سرعت فعلی تبدیل: B00 ≈ ۱٫۸s، S11 ≈ ۲٫۵s، کل ماتریس ۶۸٫۸s.
- PNG صفر بایتی، صفحهٔ مفقود و PDF کهنه خطا هستند.

#### G15 — بستهٔ نصب برای هر چهار قالب · P1 · مرجع M9

- گسترش `tests/test_smoke.py`: برای هر چهار قالب، وجود `config.yaml`، فونت‌ها، `mermaid.css` و **`shell.docx`** در wheel assert شود؛ سپس در یک محیط تمیز خارج از checkout، `md2docx convert` با هر چهار قالب اجرا شود.
- `.DS_Store` از `templates/purple_book/` حذف و به `.gitignore` اضافه شود (N7).

#### G16 — مستندسازی · P1 · مرجع M10

- `README.md` و `README_FA.md`: معرفی چهار قالب، تفاوت‌شان، اندازهٔ صفحه، و نمونهٔ فرمان.
- `AGENTS.md`: افزودن ردیف‌های `scripts/matrix_runner.py`، `scripts/generate_fixtures.py`، `scripts/create_synthetic_fixtures.py`، `scripts/setup_templates.py`، `tests/fixtures/persian_layout/`، `tests/test_persian_layout_*.py` و سه قالب جدید به جدول بند ۳؛ به‌روزرسانی قرارداد API برای پارامتر `warnings`؛ ثبت دستور بازتولید ماتریس در بند ۱۱.
- هر کلید قالب جدید از G04/G06/G08 در همان نوبت مستند شود.

#### G17 — بهداشت مخزن · P2 · مرجع N1، N2، N3

- `artifacts/` (۷۱MB) به `.gitignore` اضافه و runهای قدیمی جز آخرین یکی پاک شوند — با تأیید کاربر، چون `artifacts/manual-review/` و `word-windows-review/` ممکن است در جریان مرور کاربر باشند. **بدون تأیید صریح چیزی حذف نشود.**
- کار فعلی commit شود؛ امروز کل تغییرات uncommitted است و یک `git checkout` اشتباه همه را از بین می‌برد.
- `import tempfile` به بالای ماژول منتقل و مدیریت فایل موقت به `TemporaryDirectory` تبدیل شود.

#### G18 — گسترش پوشش تصویر و نمودار به محتوای واقعی · P2 · مرجع N9

- امروز هیچ‌کدام از ۴۵ fixture واقعی (B00, C01–C04, R01–R40) تصویر یا Mermaid ندارند. این محدودیت باید یا در گزارش صریح ثبت شود، یا با افزودن یک برش واقعی که دارایی تصویری دارد جبران شود.
- افزودن fixture جدید تعداد پایهٔ ۶۱ را تغییر می‌دهد؛ طبق بند ۴٫۴ باید عمدی، مستند و با به‌روزرسانی manifest و اعداد ماتریس باشد. گزینهٔ کم‌ریسک‌تر: ثبت محدودیت در `summary.md` و README به‌جای تغییر عدد.

---

## ۵. ترتیب پیشنهادی اجرا

۱. **G01، G02** — تا وقتی گزارش صادقانه نشده، هر کار دیگری روی عدد غلط بنا می‌شود.
۲. **G09، G10** — بدون oracle واقعی، هیچ اصلاحی قابل اثبات نیست.
۳. **G03، G04، G05، G06** — نقص‌های محصول که به کاربر نهایی می‌رسند.
۴. **G11، G12، G13** — بستن حلقهٔ هشدار، نمودار و تصویر.
۵. **G07، G08، G15، G16** — قرارداد قالب، بسته و مستندات.
۶. **G14** — پس از نصب LibreOffice؛ سپس مرور بند ۹ پلن اول با کاربر.
۷. **G17، G18** — بهداشت و شفاف‌سازی محدودیت.

---

## ۶. آنچه در دامنهٔ این پلن نیست

- بازنویسی موتور تبدیل، تغییر معماری `pipeline/pandoc_json/renderer/oxml`، یا مهاجرت به فناوری دیگر.
- افزودن پشتیبانی جدید Markdown (rowspan/colspan، LaTeX کامل، تصویر اینترنتی، HTML/CSS دلخواه) — بند ۳٫۱ پلن اول.
- پوستهٔ چندبخشی، landscape و رابط گرافیکی.
- بازتولید DOCXهای tracked در `examples/` و `tests/fixtures/` یا اجرای `scripts/convert_fixtures.py` بدون درخواست صریح.
- reset یا بازگرداندن تغییرات commitنشدهٔ کاربر و عامل‌های قبلی، از جمله `word-windows-review/` و `artifacts/manual-review/`.

---

## ۷. شرط بستن این پلن

هر تسک G با این سه چیز بسته می‌شود، نه با تیک: **(الف)** تستی که پیش از اصلاح واقعاً شکست خورده و پس از آن سبز است، **(ب)** خروجی واقعی که تفاوت را نشان می‌دهد، **(ج)** یک جمله در گزارش نهایی که می‌گوید چه چیزی هنوز بررسی نشده.

وضعیت هدف پس از گروه‌های A تا C: ماتریس ۲۴۴ زوج با oracle محتوایی و شمارشی واقعی، هشدارهای assert‌شده، و `reviews.json` با وضعیت صادقانهٔ `pending` برای مرور بصری. آن نقطه، نقطهٔ **آمادهٔ بررسی** است، نه **تحویل‌شده**. تحویل به G14 و مرور انسانی بند ۹ پلن اول وابسته است.
