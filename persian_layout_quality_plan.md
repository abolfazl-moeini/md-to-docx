# پلن رفع ریشه‌ای آشفتگی خروجی فارسی Markdown → Word

تاریخ بررسی: ۲۰۲۶-۰۹-۰۷ — وضعیت: **پیاده‌سازی کامل و راستی‌آزمایی با TDD؛ تمامی ۲۷۲ آزمون خودکار و ماتریس کامل ۲۴۴ تبدیل با موفقیت ۱۰۰٪ پاس شدند.**

این پلن برای عاملی نوشته شده که باید از روی ورودی واقعی، خطا را بازتولید کند، با TDD اصلاح کند و کیفیت تمام صفحات را نشان دهد. هدف، افزودن چند تست صوری یا تغییر رنگ قالب نیست؛ خروجی باید از نظر محتوا، جهت خواندن، اندازهٔ عناصر و صفحه‌بندی قابل استفاده باشد.

ریشهٔ پروژه: `/Users/moeini/Downloads/md-to-docx`. مسیرهای پیشنهادی کوتاه در این سند نسبت به همین ریشه‌اند و تا زمان اجرای پلن لزوماً وجود ندارند. این سند جایگزین بازنویسی `finalize.md` نیست؛ تغییرات قبلی کاربر و عامل‌های دیگر باید حفظ شوند.

## ۱. نتیجه‌ای که باید تحویل داده شود

- فایل یا متن Markdown همراه نام/مسیر یک قالب معتبر، به `.docx` تبدیل شود؛ پسوند قدیمی `.doc` در این برنامه جزو دامنه نیست.
- کتاب واقعی بدون دستکاری متن، حذف بخش‌ها یا تعمیر دستی خروجی Word تبدیل شود.
- `purple_book` حفظ و اصلاح شود و سه قالب واقعاً متفاوت نیز ارائه شوند.
- **۶۱ ورودی مستقل × ۴ قالب = ۲۴۴ ترکیب تبدیل** آزموده شوند؛ هر ترکیب خروجی و گزارش مستقل داشته باشد.
- فارسی، عبارت‌های لاتین در متن فارسی، جدول، فهرست، کد رنگی، تصویر و Mermaid در ظرف و صفحهٔ خود درست قرار گیرند.
- فونت فارسی پیش‌فرض `Vazirmatn` باشد؛ تغییر فونت از قالب روی تمام نقش‌های مربوط اثر کند و در محیط مشاهده هم کنترل شود.
- نتیجه با بررسی ساختاری، رندر تمام صفحات، ارزیابی تصویری و مرور انسانی در Word تأیید شود. صرف بازشدن DOCX یا وجود چند PNG کافی نیست.

قالب فعلی یک پوشه با `config.yaml` و دارایی‌های همراه و در صورت نیاز `shell.docx` تک‌بخشی است. این پلن وعدهٔ تبدیل هر عکس یا هر Word دلخواه به قالب اجرایی، حفظ تمام placeholderهای پوسته یا پشتیبانی از پوستهٔ چندبخشی نمی‌دهد. قالب‌های جدید در همین قرارداد ساخته شوند؛ کاربرد «دادن template همراه md» با مثال اجرایی همین قرارداد اثبات شود.

## ۲. شواهد این بررسی و حدود اطمینان

### ۲.۱. ورودی اصلی و اصالت داده‌ها

ورودی اصلی: `/Users/moeini/Downloads/md-to-docx/SQL_Server_DBA_Book_Chapters_1_4_FIXED.md`

SHA-256:

`525a3e92016a946bf36fdd5b3a2a8c8829a6e71e3e433fe52b6d9e9f3352443b`

| ویژگی | مقدار مشاهده‌شده |
| --- | --- |
| حجم و طول | ۱۷۴٬۷۰۳ بایت؛ ۳۳۳۵ خط |
| تیتر | ۱۸۹ Header در AST |
| کد بلوکی | ۱۱۵: SQL برابر ۴۷، text برابر ۵۴، PowerShell برابر ۸، console برابر ۶ |
| طول کد | بلندترین خط ۱۹۰ نویسه؛ بلندترین بلوک ۴۴ خط |
| جدول داده | ۲۳: دوازده جدول سه‌ستونی، ده جدول دو‌ستونی، یک جدول چهارستونی |
| Div دارای custom-style | ۴۶، با شش نام سبک متفاوت |
| فهرست بومی AST | ۷ BulletList و ۱ OrderedList؛ نشانه‌های گلوله‌ای متنی دیگری هم وجود دارند |
| سایر inlineها | ۱۲ Link و ۲۱ Code |
| تصویر و Mermaid | هیچ Image و هیچ CodeBlock با زبان mermaid وجود ندارد |
| metadata | `lang: fa-IR` و `dir: rtl` |

پنج پاراگراف شبیه زیرنویس شکل در خطوط ۱۱۵، ۱۲۵، ۲۶۲، ۴۰۱ و ۴۳۵ وجود دارد، اما فایل تصویر یا نمودار برای آن‌ها در Markdown ارجاع نشده است. این کمبود ورودی را نباید با ساخت تصویر فرضی، حذف زیرنویس یا قراردادن کادر خالی پنهان کرد. کتاب اصلی باید با همین واقعیت حفظ شود؛ پوشش تصویر و Mermaid از fixtureهای تکمیلی تأمین می‌شود.

### ۲.۲. دو خروجی نامطلوب ارائه‌شده توسط کاربر

| فایل | SHA-256 | یافتهٔ ساختاری | رندر تشخیصی این بررسی |
| --- | --- | --- | --- |
| `/Users/moeini/Downloads/raw/output.docx` | `9ae4e6d9dfbc0ff33b546513842093aa15f6c8daff05cb9a6e08dd273494d3d3` | ۱۲۲ جدول XML، ۵ drawing و ۵ PNG متمایز | ۳۲ صفحه در LibreOffice همراه محیط اسناد |
| `/Users/moeini/Downloads/raw/output2.docx` | `f4225253e73d9cc407a494732e0f73aec94dc8d3c19538b17228f82e58a79502` | ۳۲۵ جدول XML، ۵ drawing ولی فقط یک PNG مشترک | ۸۵ صفحه در همان محیط |

این جدول‌ها شامل کادر کد، کادر یادداشت و تیتر نیز هستند؛ نباید با تعداد جدول داده در Markdown مقایسهٔ مستقیم شوند. مقدار `Pages=1` در metadata هر دو فایل، شمار واقعی صفحات نیست.

**این دو DOCX خروجی اثبات‌شدهٔ همان نسخهٔ Markdown و همان checkout نیستند.** ورودی معرفی‌شده هیچ تصویر ندارد، در حالی که هر دو DOCX پنج تصویر دارند؛ متن و ساختارشان هم متفاوت است. بنابراین از تفاوت تعداد صفحات یا طول متن نمی‌توان حذف محتوا توسط نسخهٔ فعلی را نتیجه گرفت. فرمان تبدیل، نسخهٔ ابزار، ورودی دقیق، قالب و برنامه‌ای که کاربر در آن مشکل دیده هنوز باید ثبت شوند. کاربر فعلاً فقط مسیر فایل‌ها را مشخص کرده است.

تمام صفحات دو فایل به تصویر تبدیل شد، ولی **مرور تصویری این بررسی نمونه‌ای بود**: صفحات ۱، ۶، ۱۶ و ۱۷ از فایل اول و صفحات ۱، ۵، ۳۹ و ۵۲ از فایل دوم، به‌علاوهٔ تصاویر داخلی منتخب. هیچ بررسی مستقیم Microsoft Word و هیچ اجرای ماتریس جدید در این نوبت انجام نشده است.

### ۲.۳. ایرادهای قابل مشاهده در نمونه‌های ارسالی

| شناسه | شاهد | نتیجه برای پلن |
| --- | --- | --- |
| E01 | هر پنج drawing فایل دوم به PNG یکسان ۹۰۰×۴۵۰ وصل‌اند؛ تصویر فقط مستطیل بنفش خالی است. صفحهٔ ۵ این حالت را نشان می‌دهد. | شمارش media کافی نیست؛ نمودار باید محتوای مورد انتظار داشته باشد و stub/placeholder به خروجی پذیرفته‌شده راه نیابد. |
| E02 | در فایل اول، صفحهٔ ۶ برچسب نمودار افقی بسیار ریز و بریده است؛ در صفحهٔ ۱۷ انتهای `VMware ESXi Hypervisor` داخل گره قطع دیده می‌شود. | اندازهٔ بوم و برچسب Mermaid پیش از Word، و اندازهٔ فیزیکی نمودار بعد از جاسازی، هر دو بررسی شوند. افزایش DPI به‌تنهایی متن بریده را بازنمی‌گرداند. |
| E03 | صفحهٔ ۱ فایل دوم شناسهٔ مربوط به `chapter-1` را با آکولاد در متن تیتر نمایش می‌دهد. | مسیر parsing/escaping شناسهٔ تیتر و bookmark بازتولید شود؛ علت را بدون ورودی متناظر به کد فعلی نسبت نده. |
| E04 | عنوان «نکتهٔ DBA» انتهای صفحهٔ ۱ فایل دوم است و بدنه به صفحهٔ بعد می‌رود. | عنوان کادر باید با شروع بدنه بماند؛ کادر طولانی باید امکان شکست سالم داشته باشد. |
| E05 | صفحهٔ ۵۲ فایل دوم آیتم‌های انگلیسی و مخلوط یک مجموعه را گاهی چپ و گاهی راست نشان می‌دهد. | جهت ظرف فهرست از جهت run و زبان هر آیتم جدا شود؛ نشانهٔ گلوله هم‌ستون بماند. |
| E06 | صفحهٔ ۱ فایل اول، متن مرور فصل در نیمهٔ چپ کادر فشرده است و نیمهٔ راست خالی می‌ماند. | عرض و نقش ستون‌های کادر، محتوای استخراج‌شده و flatten شدن بلوک‌ها بررسی شود. |
| E07 | در PDFهای تشخیصی نام Vazirmatn دیده نمی‌شود؛ فونت‌هایی مانند DejaVu Sans، Linux Libertine و Liberation Mono استفاده شده‌اند، با وجود ارجاع‌های Vazirmatn در XML اصلی. | این رندرها دچار جانشینی فونت‌اند و golden نهایی نیستند؛ محیط رندر باید پیش از قضاوت نهایی فونت، کنترل شود. |

نمونه‌های خام «شاهد بد» هستند، نه golden مطلوب. صفحات دارای نمودار کامل ممکن است عمداً تمام‌صفحه باشند؛ صرف بزرگ‌بودن شکل یا اختلاف تعداد صفحات بین موتورهای Word و LibreOffice خطا محسوب نمی‌شود.

### ۲.۴. شکاف‌های تأییدشده در کد و آزمون فعلی

۱. شاخهٔ `Div` در `src/md_to_docx/pandoc_json.py`، حوالی خط ۱۰۴۸، کلاس‌های callout را بررسی می‌کند اما مقدار `custom-style` را برای انتخاب سبک مصرف نمی‌کند. شش سبک واقعی این کتاب در AST کلاس ندارند. در نتیجه برای حفظ ظاهر معنایی ۴۶ بلوک ورودی، مسیر مشخصی وجود ندارد. این یافته از کد و AST است، مستقل از منشأ دو DOCX خام.

۲. reader و adapter اختصاصی استفاده می‌شوند: Pandoc در مسیر اصلی DOCX نمی‌نویسد. افزودن `--reference-doc` یا اتکا به قابلیت custom-style در writer بومی Pandoc، این adapter را خودبه‌خود اصلاح نمی‌کند. رفتار writer رسمی صرفاً مرجع معنای syntax است. [راهنمای سبک‌های سفارشی Pandoc](https://pandoc.org/MANUAL.html#custom-styles)

۳. آزمون `tests/test_rtl_quality.py:test_libreoffice_visual_render` وجود PDF غیرخالی و در صورت فراهم‌بودن ابزار، حداقل یک PNG را کنترل می‌کند. شکست بصری متن، شکل، صفحه‌بندی یا فونت را ارزیابی نمی‌کند.

۴. تیترهای شماره‌دار، کد و بعضی کادرها با جدول پیاده شده‌اند؛ سیاست عرض و شکست یک helper می‌تواند چند نوع عنصر را خراب کند. وجود `outlineLvl` در نسخهٔ فعلی باید حفظ و در Word آزموده شود؛ تست XML به‌تنهایی navigation و TOC سالم را اثبات نمی‌کند.

۵. metadata فارسی در ورودی، دستور کامنت‌شدهٔ `--toc` و نشانه‌های `•` در نثر، الزاماً قابلیت اجرایی معادل تولید نمی‌کنند. برای هرکدام قرارداد ورودی، هشدار و آزمون لازم است؛ بازنویسی بی‌صدای متن راه‌حل نیست.

سایر علت‌ها، از جمله خطای اندازه‌گیری badge، شکل‌دهی فونت، تنظیمات compatibility و زنجیرهٔ keep، **فرضیهٔ نیازمند بازتولید** هستند. تیک‌های `finalize.md` یا تعداد تستی که عامل قبلی گزارش کرده، شاهد رفع این موارد محسوب نمی‌شوند.

## ۳. قراردادهای محصول که پیش از اصلاح باید ثابت شوند

### ۳.۱. محتوا و Markdown

- dialect اصلی همان reader فعلی Pandoc باشد؛ نسخه و فهرست extensionهای مؤثر در گزارش ثبت شوند. ویژگی جدید با تغییر روشن قرارداد و آزمون اضافه شود.
- منبع کتاب immutable باشد. ساخت fixture از آن به معنی ویرایش کتاب برای سازگارشدن با renderer نیست.
- متن منطقی فارسی، نیم‌فاصله، ترتیب کلمات، URL، مسیر Windows، اعداد فنی و کد حفظ شوند. حذف ZWNJ یا افزودن فاصله/علامت جهت برای بهترشدن تصویر بدون آزمون وفاداری متن ممنوع است.
- دادهٔ SQL/PowerShell/console فقط متن آموزشی است؛ هیچ فرمانی از fixture اجرا نشود.
- موارد پشتیبانی‌نشده صریح گزارش شوند؛ تبدیل موفق همراه حذف بی‌صدای محتوا قابل قبول نیست.
- raw HTML/CSS دلخواه، rowspan/colspan، تصویر اینترنتی و LaTeX کامل در این پلن به‌طور ناگهانی جزو وعدهٔ محصول نشوند. برای قراردادهای فعلی آزمون خطا/محدودیت وجود داشته باشد.

### ۳.۲. تقدم جهت، فونت و ظاهر

- هندسه، رنگ، فاصله و نقش‌های فونت از قالب بیایند؛ markup محتوا معنای bold/italic/code/heading را مشخص کند.
- `dir` و `lang` ورودی زبان/جهت پیش‌فرض محتوا را تعیین کنند، نه رنگ و طراحی قالب را. نبود آن‌ها به پیش‌فرض قالب برگردد. override صریح محلیِ پشتیبانی‌شده بالاتر از پیش‌فرض سند باشد؛ کد و آدرس فنی سیاست LTR اختصاصی داشته باشند.
- این تقدم، یک قرارداد پیشنهادی برای پیاده‌سازی است؛ پیش از تغییر رفتار فعلی در تست API و README ثبت شود. ترکیب قالب RTL با metadata لاتین و عکس آن هم آزموده شود.
- جهت یک فهرست از ظرف آن به ارث برسد؛ عبارت انگلیسی در یک آیتم نباید جای نشانگر همان فهرست را عوض کند. جدول نیز جهت ستون‌ها و جهت محتوای هر سلول را جدا مدیریت کند.
- `Vazirmatn` برای متن و تیتر فارسی پیش‌فرض بماند. انتخاب نام فونت در XML با موجودبودن یا جاسازی فونت برابر نیست. ضمانت ظاهر به مجموعهٔ فونت و برنامهٔ نمایش آزموده‌شده مربوط است.
- هیچ fallback فونت در محیط مرجع پذیرفته نشود؛ در محصول، فونت غایب باید راهنمای روشن ایجاد کند. عدم دسترسی به Word مقصد، مجوز ادعای تأیید ظاهر آن نیست.

### ۳.۳. شش سبک واقعی کتاب

نام‌های زیر از attribute ورودی به نقش معنایی نگاشت شوند؛ رنگ، حاشیه و اندازهٔ هر نقش از قالب بیاید. این قابلیت با جدول نگاشت تنظیم‌پذیر عمومی ساخته شود، نه شرط روی نام فایل کتاب یا متن خاص SQL Server.

| نام custom-style | تعداد در کتاب | نقش پیشنهادی | رفتار ضروری |
| --- | --- | --- | --- |
| Chapter Overview | ۲ | overview | کادر مرور با عرض کامل، حفظ ساختار بدنه؛ نشانه‌های متنی خودکار تبدیل نشوند |
| DBA Note | ۱۸ | note | یادداشت عادی؛ عنوان تکراری به متن افزوده نشود |
| Important Note | ۱۲ | important | تأکید مستقل از هشدار، بدون رنگ‌آمیزی تمام بدنه به شکل عنوان |
| Warning | ۱۰ | warning | هشدار با خوانایی و کنتراست کافی |
| Lab Note | ۲ | lab | یادداشت تمرین/آزمایش |
| Screenshot Recommendation | ۲ | editorial | توصیهٔ درج اسکرین‌شات؛ خودش تصویر یا دستور ساخت تصویر نیست |

عنوان موجود در بدنه حفظ شود. استخراج خودکار عبارت «نکتهٔ DBA» از آغاز پاراگراف فقط با قرارداد و آزمون مستقل مجاز است؛ پیش‌فرض امن برای custom-style، عنوان اضافهٔ تولیدی ندارد. عنوان صریح در syntax callout مسیر جداگانهٔ خود را حفظ کند.

برای style ناشناخته، پیش‌فرض «حفظ کامل محتوا + هشدار دارای مسیر AST» باشد و حالت strict بتواند آن را خطا کند. Span سفارشی هم با همین قرارداد بررسی شود؛ نقش inline نباید ناخواسته کل پاراگراف را به کادر تبدیل کند.

## ۴. طراحی مجموعهٔ fixtureها؛ m = ۶۱

### ۴.۱. کتاب کامل و چهار فصل: ۵ fixture

| ID | محدودهٔ منبع | توضیح |
| --- | --- | --- |
| B00 | کل فایل، خطوط ۱–۳۳۳۵ | همان بایت‌های کتاب اصلی، بدون افزودن TOC، تصویر یا اصلاح نثر |
| C01 | ۷–۳۶۸ | فصل اول |
| C02 | ۳۶۹–۵۶۲ | فصل دوم |
| C03 | ۵۶۳–۲۲۸۹ | فصل سوم؛ تنش اصلی طول، کد و تیتر |
| C04 | ۲۲۹۰–۳۳۳۵ | فصل چهارم؛ جدول، کد و نمودار متنی |

metadata لازم برای فصل‌ها از منبع کپی و به‌عنوان wrapper استخراج در manifest مشخص شود؛ در B00 هیچ wrapper اضافه نشود.

### ۴.۲. چهل برش واقعی: R01 تا R40

شماره‌خط‌های زیر متعلق به hash ثبت‌شده‌اند. استخراج‌کنندهٔ آینده باید مرز بلوک‌ها، fenceها، جدول‌ها، Divها و referenceها را بررسی کند؛ بریدن کورکورانه بر اساس شماره‌خط کافی نیست. اگر مرز به اصلاح جزئی نیاز داشت، محدودهٔ نهایی و دلیل آن در manifest ثبت شود و محتوای بلوک کامل بماند.

| ID | خطوط پیشنهادی | محتوای انتخابی و شکست هدف |
| --- | --- | --- |
| R01 | ۱۵–۴۲ | تعریف SQL Server، متن مخلوط، SQL کوتاه و دو سبک یادداشت |
| R02 | ۷۳–۱۰۰ | مرزهای Instance/Database، مسیر بک‌اسلش و جدول سه‌ستونی |
| R03 | ۱۰۱–۱۲۸ | نقش مؤلفه‌ها، دو زیرنویس بدون تصویر و DBA Note |
| R04 | ۱۲۹–۱۵۲ | پایگاه‌های سیستمی، سلول پرمتن و هشدار |
| R05 | ۱۵۳–۱۷۴ | Login/User و نام‌های فنی مخلوط |
| R06 | ۱۷۵–۱۹۷ | Edition و Lab Note؛ حفظ تفاوت نقش‌ها |
| R07 | ۲۱۸–۲۴۹ | Storage، Data/Log و مسیرهای فنی |
| R08 | ۳۰۹–۳۶۲ | تمرین پایان فصل، تیترهای تو‌در‌تو و کد |
| R09 | ۳۹۵–۴۰۶ | منابع مجازی و زیرنویس بدون تصویر |
| R10 | ۴۰۷–۴۱۸ | CPU/vCPU و جهت عبارت‌های لاتین در نثر |
| R11 | ۴۲۹–۴۳۶ | لایه‌های تنظیم عملکرد و زیرنویس مستقل |
| R12 | ۴۵۷–۴۶۸ | Storage و واحدها و اصطلاحات فنی |
| R13 | ۵۰۱–۵۲۰ | طراحی Volume، جدول و هشدار |
| R14 | ۵۳۳–۵۶۲ | چک‌لیست پایان فصل دوم، Lab/Important Note |
| R15 | ۵۶۳–۶۳۶ | مقدمهٔ فصل سوم، گلوله‌های literal و جریان متنی عمودی |
| R16 | ۶۳۷–۸۲۸ | برنامهٔ نصب، زیرتیترهای متوالی و جدول |
| R17 | ۸۲۹–۹۶۰ | PowerShell، خروجی چندخطی و یادداشت‌ها |
| R18 | ۹۶۱–۱۰۳۲ | Volume و مسیر Windows |
| R19 | ۱۰۳۳–۱۰۹۹ | console، واحد ظرفیت و هشدار |
| R20 | ۱۱۴۸–۱۱۸۵ | Named Instance و بک‌اسلش در متن/کد |
| R21 | ۱۲۲۱–۱۲۹۹ | حساب سرویس، Permission و تیترهای مخلوط بلند |
| R22 | ۱۳۰۰–۱۳۴۱ | احراز هویت، هشدار و کد |
| R23 | ۱۳۴۲–۱۴۱۹ | Collation، underscore و شناسهٔ لاتین بلند |
| R24 | ۱۴۲۰–۱۵۲۷ | نصب و Screenshot Recommendation؛ بدون جعل تصویر |
| R25 | ۱۵۶۸–۱۶۳۱ | شبکه، TCP و پورت/مسیر در متن مخلوط |
| R26 | ۱۶۹۲–۱۷۵۵ | محاسبهٔ حافظه، اعداد، واحد و DBA Note |
| R27 | ۱۷۵۶–۱۷۸۷ | MAXDOP، کد کوتاه و پاراگراف دوزبانه |
| R28 | ۱۸۱۰–۲۲۷۱ | اعتبارسنجی نصب؛ برش بلند عمدی برای شکست چندصفحه‌ای |
| R29 | ۲۳۳۶–۲۳۸۱ | معماری منطقی/فیزیکی و درخت متنی |
| R30 | ۲۳۸۲–۲۴۲۵ | شیت طراحی و جدول اطلاعات |
| R31 | ۲۴۶۰–۲۵۰۵ | Recovery Model، جدول چهارستونی، SQL و Warning |
| R32 | ۲۵۰۶–۲۵۹۶ | ساخت Database، SQL بلند و توصیهٔ اسکرین‌شات |
| R33 | ۲۵۹۷–۲۶۳۱ | نام فایل منطقی/فیزیکی و علائم مسیر |
| R34 | ۲۶۴۴–۲۷۱۴ | Filegroup و نویسه‌های درخت `├──`، `│` و `└──` |
| R35 | ۲۷۱۵–۲۷۴۰ | فهرست و ظرفیت‌گذاری اولیه |
| R36 | ۲۷۴۱–۲۷۸۲ | Autogrowth، درصد و مقادیر SQL |
| R37 | ۲۷۹۱–۲۸۲۸ | درخت متنی و تشخیص خطا |
| R38 | ۲۸۷۰–۲۹۵۴ | Query بلند، هشدار و زیرتیترهای متوالی |
| R39 | ۲۹۵۵–۳۰۹۲ | Queryهای پایش چندخطی |
| R40 | ۳۰۹۳–۳۲۳۵ | Query با بلوک ۴۴خطی و جدول تحویل با سلول‌های خالی |

این ۴۰ برش جای B00 و C01–C04 را نمی‌گیرند؛ مسئله‌ای که فقط در مجاورت چند بخش و شکست صفحه رخ می‌دهد با تست برش کوتاه کشف نمی‌شود.

### ۴.۳. شانزده Markdown مصنوعی و فارسی: S01 تا S16

هر فایل باید متن فارسی واقعی و قابل خواندن داشته باشد، نه تکرار «تست» و lorem ipsum. تعداد عناصر، رشته‌های حساس و انتظار معنایی آن پیشاپیش در manifest نوشته شود.

| ID | طرح فایل | انتظار ویژه |
| --- | --- | --- |
| S01 | متن مخلوط با پرانتز، گیومه، درصد، تاریخ، IP، URL، دامنه و مسیر Windows؛ عبارت لاتین چندکلمه‌ای میان bold/link/italic | ترتیب منطقی و بصری عبارت، نیم‌فاصله و نشانه‌گذاری حفظ شود |
| S02 | تیترهای ۱ تا ۶، شماره‌های فارسی/عربی/لاتین، شمارهٔ چندبخشی بلند، عنوان دو تا چهارخطی، شناسه و لینک داخلی | عدم نمایش syntax شناسه، badge سالم، bookmark و navigation قابل استفاده |
| S03 | هر شش custom-style، calloutهای class-based، عنوان صریح/ضمنی، دو سطح nesting و نمونهٔ literal در code fence | نگاشت مستقل از نام قالب، بدون حذف یا تکرار عنوان؛ style ناشناخته در زیرآزمون هشدار |
| S04 | فهرست bullet و ordered، شروع عدد غیر۱، alpha/roman، tight/loose، سه سطح nesting، آیتم فقط انگلیسی کنار فارسی؛ literal bullet جدا | ستون نشانگر ثابت، تورفتگی درست و حفظ توالی؛ تفاوت literal و list واقعی روشن |
| S05 | جدول‌های دو، سه و شش‌ستونی؛ یک جدول حداقل ۶۰ردیفی با متن فارسی بلند، عدد، سرستون چندخطی و سلول خالی | ترتیب ستون، عرض قابل خواندن، تکرار سرستون و شکست ردیف سالم |
| S06 | جدول داخل کادر، فهرست/تصویر/کد داخل سلول، quote و جدول تو‌در‌تو در syntax معتبر Pandoc | محاسبهٔ عرض بر اساس ظرف واقعی و حفظ تمام فرزندان AST |
| S07 | SQL، PowerShell، console، Python و text؛ tab، indentation، خط خالی، توضیح فارسی و خط بیش از ۱۹۰ نویسه | کد رنگی و قابل کپی؛ هیچ تغییر در رشتهٔ کد برای جاگیری |
| S08 | بلوک کد بیشتر از دو صفحه، بلوک دارای خطوط خالی ابتدا/انتها، درخت ASCII و box drawing | شکست قابل کنترل، عدم حذف انتهای کد و عدم ریزکردن افراطی فونت |
| S09 | عکس افقی و عمودی واقعی با مجوز، PNG شفاف، تصویر کوچک inline، reference image، نام فایل فارسی/فاصله/percent-encoding و caption | تصویر درست انتخاب شود، نسبت ابعاد و alt حفظ و تصویر واقعاً در DOCX جاسازی شود |
| S10 | Mermaid flowchart با جهت TB و LR، برچسب فارسی و لاتین، متن بلند داخل گره، لبهٔ دارای عنوان | تمام گره‌ها/لبه‌ها و برچسب‌ها خوانا باشند؛ RTL متن، جهت گراف را معکوس نکند |
| S11 | Mermaid sequence، state و ER با فارسی؛ نمودار عریض و بلند دارای تعداد عناصر معلوم | سالم‌بودن تصویر قبل از جاسازی و خوانایی در ابعاد نهایی؛ عدم بریدن label |
| S12 | Mermaid و تصویر داخل list، quote، Div، definition list، جدول با syntax مناسب و footnote؛ fenceهای تودرتو | هر محل اعلام‌شده باید مسیر کامل تا Word داشته باشد؛ ساختن media بدون درج در سند قبول نیست |
| S13 | پاورقی چندپاراگرافی، لینک، فهرست، inlineهای غنی و فرمول‌های محدودهٔ پشتیبانی‌شدهٔ OMML | شماره و مقصد پاورقی، محتوا و ترتیب مستقل از متن بدنه حفظ شود |
| S14 | مرز صفحه: تیتر، شروع کادر، تصویر/caption، جدول و کد پس از متن با طول کنترل‌شده | عنوان یتیم و caption جدا نشود؛ نسخه‌های طول متفاوت در زیرآزمون‌های مشخص اجرا شوند |
| S15 | metadata، فهرست مطالب، چهار شروع فصل، appendix، heading anchor و ارجاع داخلی | TOC واقعی مطابق سیاست قالب، لینک درست و شماره صفحه پس از update کنترل شود |
| S16 | کتابچهٔ بلند فارسی ترکیبی از همهٔ موارد با تصاویر و Mermaid؛ آن‌قدر محتوا که در محیط مرجع بیش از ده صفحه شود | یک‌دستی ابتدا/میانه/انتها و شکست‌های چندصفحه‌ای، نه فقط صفحهٔ نمونه |

fixtureهای S12/S13 قابلیت فعلی را قطعی فرض نمی‌کنند. اگر nesting اعلام‌شده محتوا را از دست می‌دهد، اصلاح مسیر adapter/footnote جزو کار لازم برای سبزشدن همین fixture است؛ حذف آن مورد از fixture برای سبزکردن ماتریس مجاز نیست. از نحو معتبر Pandoc برای بلوک داخل سلول استفاده شود؛ pipe table چندخطی نامعتبر نوشته نشود.

برای S09 دست‌کم دو عکس واقعی محلی با منشأ و مجوز ثبت‌شده، و یک تصویر فنی با نوشته/نشانگر قابل تشخیص تهیه شود. دارایی کوچک آزمایشیِ صرفاً رنگی به‌تنهایی آزمون عکس و خوانایی نیست. خطاهای remote/data URI و فرمت پشتیبانی‌نشده زیرمجموعهٔ منفی جدا هستند.

### ۴.۴. manifest و استخراج قابل اعتماد

- مسیر پیشنهادی: `tests/fixtures/persian_layout/` با زیرپوشه‌های `source/`، `chapters/`، `extracted/`، `synthetic/` و `assets/`؛ فهرست در `manifest.yaml`.
- برای هر fixture: ID، مسیر، SHA، منشأ، بازه/anchor، wrapper افزوده، ویژگی‌ها، تعداد عناصر، رشته‌های حساس، warnings مورد انتظار، دارایی‌ها و معیارهای بصری ثبت شود.
- reference definition، footnote definition و metadata لازم همراه برش بیاید؛ افزودن آن‌ها باید provenance داشته باشد. استخراج نباید یک link معتبر در کتاب را در برش خراب کند.
- AST کتاب پیش از استخراج، AST برش پس از استخراج و ساختار بلوک منتخب مقایسه شوند. line range و AST path هر دو ذخیره شوند؛ Pandoc JSON را دارای source position تضمین‌شده فرض نکن.
- B00 باید دقیقاً hash اصلی را داشته باشد. fixtureهای استخراجی به‌طور خودکار هنگام اجرای تست بازنویسی نشوند؛ بازتولیدشان عملیات صریح با diff قابل مرور باشد.
- full matrix شامل دقیقاً ۶۱ ID یکتا است. طول‌های مختلف S14 و حالت‌های منفی، زیرآزمون اضافه‌اند و تعداد پایه را به‌شکل گمراه‌کننده افزایش نمی‌دهند.

## ۵. چهار قالب؛ n = ۴

طرح‌های زیر مشخصات اولیهٔ پیاده‌سازی‌اند. پیش از ساخت golden، چند صفحهٔ نماینده از هرکدام برای مرور کاربر آماده شود. استفاده از A5 تعمدی است تا فرض‌های ثابتِ عرض A4 آشکار شوند.

| ID / نام | صفحه و حاشیه | طراحی و متن | کد و تصویر | سیاست صفحات |
| --- | --- | --- | --- | --- |
| T01 / `purple_book` | A4، حاشیهٔ فعلی ۲cm | هویت بنفش، badge، کادرهای فعلی و مرجع `sample-template/` حفظ شود؛ اصلاح عرض/فونت/فاصله قابل مرور باشد | اندازهٔ پایهٔ کد فعلی ۹٫۵pt؛ اندازه‌گذاری وابسته به ظرف | نبود header/footer در قالب فعلی به‌خودی‌خود باگ نیست؛ ظاهر موجود مبنای مقایسهٔ طراحی باشد |
| T02 / `persian_book` | A4، حاشیهٔ چهارطرف ۲٫۲cm | آبی تیره `17324D`، متن `222222`، بدنه ۱۲pt، تیترهای ۲۲/۱۸/۱۵/۱۳/۱۲/۱۲pt؛ بدون badge | کد ۹٫۵pt، تصویر وسط‌چین و caption ۱۰pt | شروع فصل سطح۱ در صفحهٔ تازه، footer شمارهٔ صفحه، header ثابت نام کتاب |
| T03 / `persian_compact` | A5، حاشیهٔ چهارطرف ۱٫۶cm | طرح چاپی خاکستری/مشکی، بدنه ۱۱٫۵pt، تیترهای ۱۸/۱۶/۱۴/۱۲٫۵/۱۲/۱۱٫۵pt؛ کادر کم‌رنگ | کد ۹pt؛ آزمون جدی wrap و جدول شش‌ستونی، بدون کوچک‌کردن متن برای عبور از تست | فصل سطح۱ صفحهٔ تازه، شمارهٔ صفحه؛ بدون header شلوغ |
| T04 / `persian_report` | Letter عمودی، حاشیهٔ چهارطرف ۲cm | سرمه‌ای `17324D` و سبزآبی `006D77`، بدنه ۱۱٫۵pt، تیترهای ۲۰/۱۷/۱۴/۱۲٫۵/۱۲/۱۱٫۵pt؛ بدون badge | کد ۹٫۵pt، caption ۱۰pt، جدول با سرستون رنگی | پوستهٔ تک‌بخشی با لوگوی آزمایشی محلی، header/footer و PAGE واقعی؛ هدرهای سطح۱ الزاماً page break ندارند |

قواعد مشترک قالب‌های جدید:

- `fonts.body` و `fonts.heading` برابر Vazirmatn؛ برای هماهنگی نمونه‌های جدید `fonts.latin` نیز Vazirmatn باشد. نقش‌ها همچنان جدا و قابل تغییر بمانند.
- فونت کد پیشنهادی قالب‌های جدید `DejaVu Sans Mono` با نسخه و فایل ثابت است؛ بستهٔ فونت و notice مجوز آن همراه محیط آزمون فراهم شود. [مجوز رسمی DejaVu](https://dejavu-fonts.github.io/License.html)
- T01 نقش‌های فعلی Segoe UI و Courier New را دارد؛ محیط مرجع باید خود فونت‌های مجاز را واقعاً داشته باشد. اگر تهیهٔ آن‌ها ممکن نبود، تغییر پیش‌فرض لاتین/کد T01 یک تغییر طراحی صریح و قابل مرور باشد، نه fallback پنهان renderer.
- متن فارسی جدید پیش‌فرض راست‌چین با انتهای آزاد باشد تا کشیدگی فاصله در متن فنی کم شود؛ justify به‌صورت گزینهٔ قالب و با نمونهٔ بصری جدا بررسی شود. تغییر رفتار T01 بدون مقایسهٔ قبل/بعد انجام نشود.
- فاصلهٔ خطوط بدنه ابتدا ۱٫۳۵ و فاصلهٔ بعد پاراگراف ۶pt پیشنهاد می‌شود؛ پس از نمونهٔ واقعی، مقدار نهایی ثابت و در قرارداد قالب ثبت شود. برای متن فارسی از ارتفاع exact که ممکن است اعراب را ببرد اجتناب شود.
- شش نقش custom-style، quote، جدول، caption، code و پاورقی در هر چهار قالب ظاهر تعریف‌شده داشته باشند؛ تفاوت قالب‌ها فقط تعویض palette نباشد.
- همه تک‌ستونی و portrait باشند؛ رفع نمودار عریض به توسعهٔ فوری sectionهای landscape و پوستهٔ چندبخشی وابسته نشود.
- شروع فصل تازه با page break پیاده شود؛ section جدید غیرضروری تولید نشود. هدر/footer و نسبت به first/even/default به‌صراحت برای T04 آزموده شود.
- حدود فونت پذیرش: بدنه کمتر از ۱۱pt و کد کمتر از ۹pt بدون تصمیم طراحی صریح پذیرفته نشود؛ label نمودار در اندازهٔ نهایی حداقل حدود ۹pt هدف باشد. اعداد بالا مشخصات طراحی‌اند، نه اندازه‌گیری فعلی Word.

برای هر قالب جدید، config و دارایی‌های checkout و بستهٔ نصب‌شده هماهنگ شوند. اگر کلیدهای تازهٔ styles، typography یا pagination لازم شد، فقط نوشتن YAML کافی نیست: schema/validator، مدل، مصرف‌کننده، تست اثر واقعی، مستندات و نسخه‌پذیری همگی تکمیل شوند.

## ۶. روش کار برای عامل اجراکننده

ابتدا `AGENTS.md`، وضعیت تغییرات و کد مرتبط را بخوان. این مخزن در زمان بررسی تغییرات commitنشدهٔ فراوان داشته است؛ HEAD ثبت‌شده `eea802710a68b480e97578f3d6873589323370c1` به‌تنهایی نسخهٔ بررسی‌شده را مشخص نمی‌کند. از بازگرداندن فایل‌های کاربر یا بازتولید تمام DOCXهای tracked خودداری کن.

هر تسک Q زیر این چرخه را طی کند:

۱. fixture و انتظار مشخص را انتخاب کن و گزارش قبلی همان مورد را بخوان.
۲. آزمون رفتار مطلوب را بنویس و شکست واقعی آن را روی نسخهٔ قبل از اصلاح ثبت کن؛ اگر مشکل قبلاً رفع شده، به‌جای تغییر دوباره شواهد سلامت و آزمون محافظ اضافه کن.
۳. کوچک‌ترین اصلاح عمومی را در لایهٔ مسئول انجام بده. شرط روی ID تست، متن SQL کتاب، نام قالب خاص یا تعداد صفحه راه‌حل پذیرفته‌شده نیست.
۴. تست نزدیک، رگرسیون مرتبط و رندر صفحهٔ مشکل‌دار با صفحات مجاور را اجرا کن.
۵. diff محتوا، XML و تصویر را با انتظار مستقل بررسی کن و اثر روی قالب‌های دیگر را بسنج.
۶. فقط پس از شواهد، وضعیت تسک را done کن. failed، blocked، skipped و needs-human-review وضعیت‌های جدا از pass هستند.

مسیر معماری پایه حفظ شود: `pipeline.py` هماهنگی؛ `pandoc_json.py` معنا و پیمایش؛ `renderer.py` تولید/ظاهر؛ `oxml.py` قواعد XML؛ `bidi.py` قطعه‌بندی؛ `template.py` قرارداد قالب؛ `mermaid.py` نمودار؛ `footnotes.py` پاورقی. بازنویسی کل موتور یا مهاجرت به Haskell در این پلن نیست.

## ۷. تسک‌های اجرا به ترتیب وابستگی

### Q00 — ثبت نسخه و بازتولید قابل ردیابی؛ اولویت P0

**ورودی:** کتاب اصلی، دو DOCX خام، working tree فعلی. **وابستگی:** ندارد.

- [ ] hash هر سه فایل را با بخش ۲ تطبیق بده؛ اختلاف را ثبت و بررسی کن، فایل جدید را خودکار جای نسخهٔ مبنا نگذار.
- [ ] علاوه بر commit، وضعیت dirty و hash فایل‌های مؤثر کد/قالب/lockfile را ثبت کن؛ baseline باید به snapshot واقعی اشاره کند.
- [ ] از کاربر برنامه و نسخه‌ای که در آن مشکل دیده و در صورت دسترسی فرمان/قالب/MD دو خروجی خام را دریافت کن. نبود این اطلاعات مانع بررسی مستقل کتاب نیست، اما نسبت‌دادن قطعی raw به کد فعلی را محدود می‌کند.
- [ ] B00 را با CLI عمومی و T01 فعلی در پوشهٔ خروجی مستقل تبدیل کن؛ AST، log، warnings، media و DOCX را نگه دار. کتاب و rawها بازنویسی نشوند.
- [ ] رندر rawها را با رندر این خروجی تازه جدا نگه دار و برای E01–E07 ستون «در نسخهٔ فعلی بازتولید شد/نشد/نامشخص» پر کن.

**تست و پذیرش:** دوباره‌اجرای همان ورودی با همان snapshot ساختار و محتوای یکسان بدهد؛ مسیرها و فرمان ثبت شده باشند. اختلاف timestamp/شناسهٔ غیرمعنایی ZIP دلیل تغییر محتوا نیست. این مرحله baseline بد را ثبت می‌کند و نباید انتظار تصویر سالم را سبز اعلام کند.

### Q01 — ساخت مجموعهٔ واقعی و manifest؛ اولویت P0

**وابستگی:** Q00. **فایل‌های هدف:** fixtureها و ابزار استخراج/اعتبارسنجی مستقل.

- [ ] B00، چهار فصل و چهل برش دقیق بخش ۴ را ایجاد کن؛ metadata/referenceهای لازم را با provenance اضافه کن.
- [ ] برای هر برش، بسته‌بودن fence/Div و حفظ blockهای منتخب در AST را بررسی کن. برای R28 و R40 محدودیت «همه باید کوتاه باشند» اعمال نکن.
- [ ] S01–S16 و دارایی‌های محلی را بنویس؛ تعداد Image، Mermaid، heading، table، note و رشته‌های حساس مورد انتظار را پیش از اجرای renderer ثبت کن.
- [ ] manifest نهایی باید ۶۱ ID و مسیر یکتا داشته باشد. نبود فایل/دارایی، hash نامعتبر، ID تکراری یا fixture بدون assertion، خطای suite باشد.
- [ ] پنج caption بدون تصویر B00 را با هویت پاراگراف و متن ثبت کن؛ در برش‌ها warning متناظر انتظار رود. از تولید چند warning تکراری برای یک caption جلوگیری کن.

**تست قرمز:** fixture قطع‌شده در وسط fence، reference حذف‌شده و فایل گم‌شده باید validator را قرمز کنند. **پذیرش:** corpus مستقل از خروجی renderer قابل اعتبارسنجی باشد و تمام counts با parse واقعی سازگار باشند.

### Q02 — تثبیت فونت و محیط مرجع؛ اولویت P0

**وابستگی:** Q00؛ پیش از تصویب هر golden.

- [ ] نسخهٔ Python، python-docx، Pandoc و API آن، Node، Mermaid CLI، Chromium، LibreOffice، ابزار PDF/PNG و Word مورد استفاده ثبت شود.
- [ ] فایل‌های فونت Regular/Bold لازم، نسخه، hash و مجوز مشخص شوند؛ نام family داخلی و نقش body/heading/latin/code با فایل واقعی تطبیق داده شود. وجود یک TTF در پوشهٔ template به معنی نصب آن در Word یا محیط رندر نیست.
- [ ] صفحهٔ کنترل فونت شامل فارسی با نیم‌فاصله/اعراب، فارسی bold، لاتین و کد بساز؛ در XML، PDF و تصویر کنترل شود. فقط مقایسهٔ نام family در XML کافی نیست.
- [ ] فونت‌های مورد انتظار در محیط واقعی Chromium و محیط واقعی LibreOffice حاضر باشند؛ نصب روی host وقتی renderer در محیط دیگری اجرا می‌شود کافی نیست.
- [ ] بارگذاری فونت Mermaid قبل از عکس‌گرفتن تکمیل و با نمونهٔ glyph/عرض متن کنترل شود؛ timeout به تصویر fallback موفق تبدیل نشود.
- [ ] آزمون override برای body و heading با یک فونت دومِ واقعاً نصب‌شده اجرا و تفاوت هدفمند خروجی مشاهده شود؛ نقش code/latin مستقل بماند.
- [ ] محیط مرجع در غیاب فونت مورد نیاز fail شود. برای کاربر راهنمای تشخیص/نصب فونت با قرارداد غیرجاسازی فعلی مستند شود؛ embedding پروژهٔ جدا و شرط پنهان این پلن نیست.

**تست قرمز:** حذف دسترسی به Vazirmatn در محیط مرجع باید خطای preflight بدهد، نه golden تازه. **پذیرش:** شواهد استفادهٔ واقعی از فونت ثبت شده، و رندر E07 به‌عنوان مرجع طراحی کنار گذاشته یا با فونت درست دوباره ساخته شده باشد.

### Q03 — runner ماتریس و ثبت شکست‌ها، پیش از رفع ظاهر؛ اولویت P0

**وابستگی:** Q01 و Q02؛ در ابتدا فقط T01 موجود است و سه قالب دیگر planned ثبت شوند.

- [ ] runner مجموعهٔ fixture و قالب را از manifest بخواند و حاصل‌ضرب کامل را بسازد؛ حلقهٔ دست‌نویس با حذف موارد سخت پذیرفته نیست.
- [ ] برای هر زوج یک پوشهٔ اختصاصی با DOCX، media، AST، log و گزارش ایجاد شود؛ از فایل‌های tracked کنار fixtureها برای خروجی استفاده نشود.
- [ ] از API یا CLI عمومی و Mermaid واقعی استفاده شود. تزریق `render_mermaid_fn` یا monkeypatch تولید PNG در آزمون پذیرش ممنوع است؛ mock فقط برای آزمون واحد خطا مجاز است.
- [ ] خروجی هر مرحله وضعیت مجزا داشته باشد؛ خطای تبدیل با ادامهٔ بقیهٔ زوج‌ها ثبت شود و خروج کلی matrix ناموفق بماند.
- [ ] timeout و سقف هم‌زمانی Chromium/LibreOffice تنظیم و به گزارش اضافه شود؛ هر اجرای LibreOffice profile مستقل داشته باشد تا خروجی کهنه یا تداخل پردازش نتیجه را سبز نکند.
- [ ] خروجی‌های یک run جدید در پوشهٔ خالی همان run ساخته شوند؛ وجود PDF قدیمی از اجرای قبل قبول نشود.

**تست قرمز:** یک زوج عمداً حذف یا renderer خراب شود؛ summary باید missing/failed را نشان دهد. **پذیرش:** runner هیچ planned/skip/missing را pass نمی‌شمارد و با اضافه‌شدن T02–T04 خودکار به ۲۴۴ زوج می‌رسد.

### Q04 — حفظ semantics ورودی، custom-style و تشخیص کمبود منبع؛ اولویت P0

**وابستگی:** Q01، Q03. **محل اصلی:** adapter، template و preprocess مرتبط.

- [ ] نگاشت شش style بخش ۳.۳ را با schema معتبر، resolution روشن و حفظ فرزندان Div پیاده کن. aliasهای class-based قبلی رگرسیون نکنند.
- [ ] آزمون مستقل برای custom-style بدون class، class همراه attribute، nested Div و unknown style بنویس؛ نوع نقش inline/block را تفکیک کن.
- [ ] خطاهای نشت `{#id}` و `custom-style` را از ورودی متناظر یا S02 بازتولید کن. stripping عمومی آکولاد ممنوع است؛ literal داخل کد و مثال Markdown باید عیناً بماند.
- [ ] پیش‌پردازش admonition نباید code fence، HTML comment یا literal Markdown را تغییر دهد؛ parse دوبارهٔ متن escaped تنها برای پنهان‌کردن E03 انجام نشود.
- [ ] زیرنویس بدون image warning مشخص با source anchor ایجاد کند؛ caption عادی که در نثر از یک شکل حرف می‌زند بی‌دلیل مصرف نشود. adjacency/ساختار معتبر و انتظار پنج مورد کتاب را آزمون کن.
- [ ] گلوله‌های literal و کامنت `--toc` به‌عنوان semantics تضمین‌شده برداشت نشوند. اصلاح محتوایی پیشنهادی می‌تواند در گزارش lint بیاید، اما B00 تغییر نکند.

**تست قرمز:** R01/R03 و S03 باید قبل از نگاشت، فقدان نقش را نشان دهند. **پذیرش:** همهٔ ۴۶ بلوک B00 نقش معتبر و متن کامل داشته باشند، بدون تکرار عنوان یا تولید شکل فرضی؛ S02 syntax شناسه را به متن خروجی نریزد.

### Q05 — سیاست یکپارچهٔ فارسی و متن مخلوط؛ اولویت P0

**وابستگی:** Q02، Q04. **محل اصلی:** `bidi.py`، emit_inlines، renderer و ابزارهای OOXML.

- [ ] جهت پایهٔ paragraph/container را یک‌بار بر اساس قرارداد بخش ۳ تعیین کن؛ روش‌های پراکندهٔ تشخیص فارسی در callout، جدول، list و quote به سیاست مشترک برسند.
- [ ] عبارت لاتین چندکلمه‌ای را با آگاهی از بافت کل پاراگراف segment کن؛ مرز `Str`، `Space`، bold یا hyperlink نباید context نشانه‌گذاری را بشکند. هم‌زمان مرزهای قالب‌بندی و target لینک حفظ شوند.
- [ ] `w:bidi`، alignment و `w:rtl` را جدا آزمون کن. LTR صریح برای code/URL لازم است؛ پاک‌کردن یک ویژگی وقتی style والد RTL است لزوماً کافی نیست.
- [ ] فونت complex script، اندازه و bold/italic متناظر (`szCs`، `bCs`، `iCs` و موارد لازم) در کنار تنظیمات لاتین کنترل شود.
- [ ] رشته‌های واقعی `SQLPROD01\REPORTING`، مسیر `D:\SQLData`، عبارت `SQL Server Database Engine`، underscore، علامت درصد و شمارهٔ چندبخشی را هم در متن و هم کد/جدول بررسی کن.
- [ ] از معکوس‌کردن رشته، reshaping به presentation form، تبدیل کلی اعداد و درج انبوه علامت نامرئی برای آرایش ظاهری خودداری کن. هر کاراکتر کمکی احتمالی نیازمند دلیل محدود و آزمون copy/search است.

`w:bidi` به‌تنهایی ترتیب متن درون پاراگراف را تعیین نمی‌کند؛ مستند رسمی هم جهت پاراگراف و ترتیب متن را جدا می‌داند. این مرجع برای تنظیمات XML است، نه دلیل افزودن وابستگی .NET. [Microsoft: BiDi](https://learn.microsoft.com/en-us/dotnet/api/documentformat.openxml.wordprocessing.bidi?view=openxml-3.0.1)

**تست قرمز:** S01 و R20/R21/R26 با assertion متن منطقی و مرور تصویر عبارت‌های علامت‌دار. **پذیرش:** متن قابل کپی مطابق منبع و خواندن بصری صحیح باشد؛ زیبایی تصویر با تغییر محتوای فنی خریداری نشود.

### Q06 — نقش‌های سبک و حذف اندازه‌گذاری پراکنده؛ اولویت P0

**وابستگی:** Q04، Q05. **محل اصلی:** template، renderer، adapter و پوسته.

- [ ] نقش‌های body، heading1–6، inline code، code block، table body/header، list، quote، callout، caption، footnote و header/footer مشخص و از قالب resolve شوند.
- [ ] اعداد ثابت مانند اندازهٔ ۱۰٫۵pt در مسیرهای nested را فهرست کن؛ هر مقدار باید یا default مستند نقش باشد یا از قالب بیاید. یک template جدید نباید فقط بدنهٔ اصلی را تغییر دهد.
- [ ] سبک‌های Word و inheritance مشخص بساز/استفاده کن و direct formatting را به موارد لازم محدود کن. برای تیترها در صورت لزوم سبک Heading و outline سازگار حفظ شود؛ ادعای TOC بر اساس وجود تنها یک tag نباشد.
- [ ] roleهای فارسی/لاتین/code در table، hyperlink، footnote، caption و callout نیز کامل اعمال شوند. تغییر body نباید code را ناخواسته Vazirmatn کند.
- [ ] کلید ناشناخته خطا دهد؛ مقدار معتبر ولی بی‌اثر با آزمون اثر بصری/ساختاری آشکار شود. تقدم shell و YAML طبق قرارداد فعلی روشن بماند.

**تست قرمز:** یک قالب آزمون با اندازه/رنگ/فونت متمایز برای نقش‌ها بده و نشت مقدار ثابت را پیدا کن. **پذیرش:** تغییر تنظیم هر نقش، تمام محل‌های همان نقش را پوشش دهد و هیچ role بی‌صاحب نباشد.

### Q07 — عرض ظرف، تیتر و شکست صفحه؛ اولویت P0

**وابستگی:** Q06. **محل اصلی:** renderer/oxml و dispatch ظرف‌ها.

- [ ] یک قرارداد عرض/ارتفاع قابل استفاده برای section، سلول، callout و list ایجاد کن: عرض صفحه منهای حاشیه‌ها، سپس عرض سلول منهای padding و indent؛ عرض ثابت ۶٫۳in مرجع همهٔ ظرف‌ها نباشد.
- [ ] `tblW`، `tblGrid`، `tcW`، autofit، indent و cell margin متناقض تولید نشوند؛ واحدهای twip، EMU، point و inch در مرزهای مشخص تبدیل شوند.
- [ ] عرض badge با شمارهٔ بلند و عنوان چندخطی آزموده شود. گسترش title باید ارتفاع را افزایش دهد، نه متن را ببرد یا badge را روی عنوان بیندازد. ترتیب بصری RTL دو سلول جدا کنترل شود.
- [ ] keepNext فقط بین عناصر لازم باشد: تیتر و شروع محتوای بعدی، عنوان کادر و شروع بدنه، تصویر و caption مربوط. تمام پاراگراف‌های فصل یا کد طولانی در زنجیرهٔ keep قرار نگیرند.
- [ ] جدول تک‌سلولی چندصفحه‌ایِ code/callout مجاز به شکست کنترل‌شده باشد؛ `cantSplit` یا keepLines سراسری باعث فضای سفید عظیم و گم‌شدن محتوا نشود.
- [ ] برای heading، حداقل شروع معنی‌دار بلوک بعدی در همان صفحه بماند؛ در نثر هدف دو خط است. برای heading پشت سر heading یا پیش از جدول، تست اختصاصی تعریف شود.
- [ ] پاراگراف اجباری انتهای سلول و پاراگراف بین جدول‌ها فاصلهٔ ناخواسته تولید نکند؛ قبل/بعد بلوک از سیاست نقش بیاید، نه چند paragraph خالی.

زنجیرهٔ keepNext می‌تواند چند پاراگراف را به هم متصل کند؛ اعمال وسیع آن باید با متن بزرگ‌تر از یک صفحه آزموده شود. [Microsoft: KeepNext](https://learn.microsoft.com/en-us/dotnet/api/documentformat.openxml.wordprocessing.keepnext?view=openxml-3.0.1)

**تست قرمز:** E04 با S14، E06 با overview، تیتر بلند S02 و کد چندصفحه‌ای S08. **پذیرش:** بدون سرریز/بریدگی، تیتر و عنوان کادر یتیم، یا صفحهٔ خالی غیرعمدی در چهار اندازه/طرح؛ تمام صفحات مجاور شکست دیده شوند.

### Q08 — فهرست و جدول داده؛ اولویت P0

**وابستگی:** Q05–Q07.

- [ ] جهت marker، hanging indent و tab stop فهرست از context فهرست بیاید؛ جهت انگلیسیِ runهای آیتم مستقل باشد. E05 در S04 بازتولید شود.
- [ ] start/style/delimiter فهرست ordered و nesting حفظ شود؛ tight/loose و continuation paragraph با عنصر جدید اشتباه نشود. numbering بومی Word در صورت انتخاب، با تست ادامه/شروع مجدد انجام شود و پیش‌شرط پنهان رفع RTL نباشد.
- [ ] literal bullet source حفظ و طبق paragraph context نمایش داده شود؛ تبدیل آن به list بومی فقط قابلیت opt-in با تست محتوایی مستقل است.
- [ ] جهت جدول و ترتیب منطقی ستون‌ها مستقل از direction سلول باشند؛ عدد/مسیر/کد در جدول RTL بتوانند LTR باشند، بدون جابه‌جا شدن سرستون با داده.
- [ ] عرض ستون از مشخصات AST و ظرفیت قابل استفاده بیاید؛ حالت auto بر اساس دادهٔ واقعی بررسی شود و ستون پرمتن بی‌دلیل به اندازهٔ ستون عددی نشود.
- [ ] سرستون جدول بلند در صفحهٔ بعد تکرار و سلول چندپاراگرافی با تمام inlineها حفظ شود. ردیف بلندتر از صفحه نباید با keep/cantSplit غیرقابل نمایش شود.
- [ ] برای جدول شش‌ستونی A5، محتوای fixture قابل جاگیری با wrap و padding مناسب طراحی شود؛ کوچک‌کردن فونت زیر حد مجاز راه خروج نیست. جدول واقعاً ناممکن در آزمون منفی باید diagnostic روشن بدهد.

**تست قرمز:** S04/S05/S06، R04/R30/R31/R40. **پذیرش:** متن هر cell زیر header درست قرار گیرد؛ نشانگرهای یک سطح فهرست هم‌ستون، indentها ثابت و جدول چندصفحه‌ای خوانا باشد.

### Q09 — کد رنگی، بلند و قابل کپی؛ اولویت P0

**وابستگی:** Q05–Q07.

- [ ] متن خروجی کد را با متن CodeBlock AST تطبیق بده: indentation، tab، خط خالی، بک‌اسلش و trailing newline طبق قرارداد روشن حفظ شوند؛ whitespace را برای پاس‌شدن oracle حذف نکن.
- [ ] رنگ tokenهای Pygments برای SQL و PowerShell با نمونهٔ دارای keyword/string/comment واقعاً متفاوت باشد؛ وجود یک رنگ دلخواه روی تمام کد اثبات syntax highlighting نیست.
- [ ] زبان ناشناخته به کد سادهٔ خوانا با متن کامل برگردد؛ نبود lexer نباید حذف بلوک یا font نامناسب ایجاد کند.
- [ ] خط طولانی با wrap نمایشی حل شود؛ افزودن newline واقعی یا فاصله به دستور مجاز نیست. اگر راه‌حل Word برای آن مورد متن را تغییر می‌دهد، محدودیت به diagnostic تبدیل شود و راه‌حل دیگر آزموده شود.
- [ ] بلوک بلند از مرز صفحه عبور کند و پس‌زمینه/حاشیه هر قسمت سالم بماند. caption یا متن پس از آن با آخرین خط کد ادغام نشود.
- [ ] box drawing و توضیح فارسی در کد جدا بررسی شوند؛ جهت اصلی کد LTR است، اما glyph فارسی نباید مربع/جداجدا شود. آزمون متن فنی با `w:tab`/`w:br` درست decode شود.

**تست قرمز:** S07/S08، R17/R34/R40. **پذیرش:** رنگ‌بندی، فونت، خطوط خالی، پایان بلوک و متن قابل کپی سالم؛ بیش از ۱۹۰ نویسه و چند صفحه با آزمون مستقل پوشش یابد.

### Q10 — تصویر واقعی و caption در هر ظرف؛ اولویت P0

**وابستگی:** Q07؛ با Q09 قابل انجام مستقل پس از زیرساخت مشترک.

- [ ] برای فایل، stdin و content API با base_dir آزمون بگذار؛ دو پوشه با تصویر هم‌نام اما محتوای متفاوت باید انتخاب نادرست fallback را آشکار کنند.
- [ ] اندازهٔ تصویر بر اساس عرض ظرف و ارتفاع قابل استفاده محدود شود و نسبت ابعاد حفظ شود؛ ابعاد EXIF و orientation عکس نیز آزموده شود.
- [ ] تصویر کوچک بی‌دلیل به عرض کل صفحه کشیده نشود. تصویر بزرگ به‌همراه caption محاسبه شود؛ اندازهٔ تصویر به تنهایی باعث بیرون‌رفتن caption نشود.
- [ ] alt، title، caption و پاراگراف بعدی نقش جدا دارند؛ متن عادی پس از تصویر خورده نشود و caption دوباره درج نشود.
- [ ] media و relationship در DOCX معتبر باشند و بازشدن سند از پوشه‌ای دیگر پس از حذف نسخهٔ کپی media جانبی همچنان تصویر را نمایش دهد. اصل asset کاربر برای این تست حذف نشود.
- [ ] فرمت پشتیبانی‌نشده و فایل خراب خطای روشن بدهند؛ وجود یک فایل با پسوند PNG یا ابعاد مثبت موفقیت نیست.

**تست قرمز:** S06/S09/S14 و دارایی هم‌نام اشتباه. **پذیرش:** عکس درست، نسبت سالم، عدم crop ناخواسته/سرریز، caption مجاور و استقلال سند از پوشهٔ جانبی.

### Q11 — Mermaid واقعی؛ رفع تصویر خالی، label بریده و نمودار ناخوانا؛ اولویت P0

**وابستگی:** Q02، Q07، Q10. **محل اصلی:** Mermaid renderer، تنظیمات قالب و traversal AST.

- [ ] E01 را با تزریق عمدی تصویر خالی در آزمون منفی oracle بازتولید کن؛ پذیرش نمودار واقعی باید این خروجی را رد کند. تشخیص بر اساس hash همان فایل خالی به‌تنهایی کافی نیست.
- [ ] تصویر قبل از ورود به Word نگه داشته و در اندازهٔ اصلی بررسی شود. تعداد گره/لبه و متن label از fixture معلوم باشد؛ label داخل SVG/DOM میانی یا خروجی معتبر معادل و سپس تصویر بررسی شود.
- [ ] بارگذاری فونت، اندازهٔ label، bounding box، padding گره، viewport و گزینه‌های مؤثر Mermaid مانند HTML label با نسخهٔ نصب‌شده آزمایش شود. وجود key در schema اینترنتی را به نسخهٔ lockشده تعمیم نده.
- [ ] flowchart عریض E02 با نمونهٔ ۶گرهی دارای `Query Processor` و `VMware ESXi Hypervisor` بازتولید شود. هیچ بخشی از label در PNG اولیه و DOCX نهایی بریده نباشد.
- [ ] متن فارسی label درست شکل بگیرد و جهت edges از تعریف نمودار بیاید؛ اعمال CSS عمومی RTL نباید گراف LR یا ترتیب sequence را معکوس کند.
- [ ] scale و raster resolution برای وضوح تنظیم شوند، ولی fit شدن با ریزکردن label زیر حد خوانایی پذیرفته نشود. wrapping label/padding و اندازهٔ رندر قبل از downscale بررسی شوند.
- [ ] برای نمودار ذاتاً بزرگ‌تر از ظرفیت صفحه، پیام صریح با پیشنهاد تنظیم layout/تقسیم توسط نویسنده بده؛ توپولوژی، ترتیب sequence یا جهت LR/TB را بی‌اجازه بازنویسی نکن. نمونه‌های مثبت S10/S11 باید در محدودهٔ قابل ارائهٔ چهار قالب طراحی شده باشند؛ موارد ناممکن به تست منفی مستقل بروند.
- [ ] تمام محل‌های S12، از AST تا drawing نهایی، شمارش و بازبینی شوند؛ Mermaid تبدیل‌شده به Image که در پاورقی flatten می‌شود، همچنان نقص است.
- [ ] cache اگر وجود دارد کلیدش شامل source، نسخه‌های renderer/browser، config/CSS، فایل فونت و پارامترهای مؤثر باشد؛ دو قالب نباید نمودار رنگ/فونت قالب قبلی را بگیرند. بدون cache هم ماتریس باید درست کار کند.

Mermaid CLI تصویر را از نمودار می‌سازد و امکان config/CSS دارد؛ این قابلیت به‌تنهایی تضمین شکل‌دهی فارسی یا اندازهٔ مناسب Word نیست. [مخزن رسمی Mermaid CLI](https://github.com/mermaid-js/mermaid-cli)، [schema رسمی تنظیمات Mermaid](https://mermaid.js.org/config/schema-docs/config.html)

**تست قرمز:** S10–S12، تصویر مستطیل خالی، label ناقص و دو نمودار متفاوت که اشتباهاً یک تصویر می‌گیرند. **پذیرش:** تمام نمودارها در محل درست، با متن و اجزای مورد انتظار و خوانایی نهایی واقعی باشند؛ mock در مسیر پذیرش صفر باشد.

### Q12 — مرزهای پشتیبانی Markdown، nesting و پاورقی؛ اولویت P1

**وابستگی:** Q04–Q11.

- [ ] جدول پوشش block/inline و ظرف‌های body/list/cell/quote/Div/footnote تهیه کن؛ «در سطح اصلی پشتیبانی می‌شود» جای نتیجهٔ nested ننشیند.
- [ ] برای S12/S13، پاورقی غنی و محتوای آن به part صحیح با relationshipهای متعلق به همان part منتقل شوند؛ flatten به متن اگر تصویر/فرمت/بلوک را از بین می‌برد اصلاح شود.
- [ ] لینک خارجی، لینک داخلی، reference، strike، emphasisهای ترکیبی، break، definition list و محدودهٔ math موجود با expected content/structure مستقل کنترل شوند.
- [ ] node ناشناخته، HTML خارج قرارداد و فرمول خارج زیرمجموعهٔ موجود هشدار/خطای مستند داشته باشند. از افزودن پشتیبانی ناقص خاموش به‌عنوان «کامل Markdown» پرهیز کن.
- [ ] تمامی blockهای مثبت در manifest باید حفظ شوند؛ حذف تست nesting دشوار یا تبدیل آن به xfail دائمی قابل قبول نیست.

**تست قرمز:** خروجی nested ناقص S06/S12/S13. **پذیرش:** ماتریس پشتیبانی به آزمون وصل باشد؛ هر محدودیت منتشرشده نمونهٔ منفی و diagnostic داشته باشد و تمام fixtureهای مثبت بدون افت محتوا تبدیل شوند.

### Q13 — فهرست مطالب، تیترها و پوستهٔ همهٔ صفحات؛ اولویت P1

**وابستگی:** Q06، Q07 و Q12.

- [ ] TOC یک قابلیت صریح قالب/تنظیم تبدیل باشد؛ کامنت داخل کتاب آن را خودکار فعال نکند. برای B00 متن ورودی تغییر نکند و generated TOC از محتوای اصلی در oracle جدا شناخته شود.
- [ ] هویت heading و bookmark پایدار و یکتا باشد؛ تیتر داخل جدول تزئینی هم در Word navigation/TOC آزمایش شود. اگر لازم شد نمایش تیتر بازطراحی شود، هویت بصری T01 با کاربر مرور شود.
- [ ] اگر TOC فعال است، field صحیح و راه به‌روزرسانی داشته باشد؛ شمارهٔ صفحهٔ ساختگی و پاراگرافی که فقط شبیه فهرست است پذیرفته نشود. ظاهر پیش از update و پس از update جدا ثبت شود.
- [ ] PAGE، header، footer، فاصله از بدنه و لوگوی T04 در صفحهٔ اول، زوج/فرد و انتها بررسی شوند؛ تنظیم link-to-previous یا single-section تصادفی گم نشود.
- [ ] shell چندبخشی طبق قرارداد صریح رد شود؛ اصلاح این پلن به پشتیبانی ناقص چندبخشی گسترش نیابد. اولویت هندسهٔ YAML نسبت به shell آزموده شود.
- [ ] یک بازکردن/ذخیره‌کردن در Word روی کپی خروجی و update fields نباید متن/تصویر/جهت را خراب کند؛ فایل اصلی تولیدی برای مقایسه حفظ شود.

**تست قرمز:** S02/S15، لینک به heading مخلوط و پوستهٔ T04. **پذیرش:** در Word واقعی navigation، TOC فعال و header/footer مطابق قرارداد تمام صفحات باشند؛ صرف LibreOffice/outlineLvl کافی نیست.

### Q14 — ساخت و بسته‌بندی سه قالب جدید؛ اولویت P1

**وابستگی:** Q06 و Q07؛ تکمیل پذیرش وابسته به Q08–Q13.

- [ ] T02–T04 با طرح بخش ۵، دارایی‌های محلی، فونت‌ها و تمام roleها ساخته و validate شوند.
- [ ] همهٔ کلیدهای اضافه‌شده اثر قابل اندازه‌گیری داشته باشند؛ سه قالب از یک نمونهٔ رنگی یکسان تولید نشوند.
- [ ] از `sample-template/1.jpg` و `2.jpg` و T01 برای حفظ مشخصهٔ طراحی پیشین استفاده شود؛ عکس‌های مرجع، golden محتوایی کتاب نیستند.
- [ ] برای هر قالب چهار صفحهٔ نمایندهٔ body، table/code، mixed heading/callout و image/Mermaid برای مرور اولیه آماده شود؛ ایراد طراحی قبل از تصویب صدها baseline رفع شود.
- [ ] template list/validate، انتخاب نام و مسیر، wheel assets و اجرای خارج از checkout با هر چهار قالب آزموده شود. یک نسخهٔ قابل حمل از قالب سفارشی در مسیر دارای فاصله/فارسی امتحان شود.

**تست قرمز:** wheel فاقد font/CSS/shell یا template با نقش بی‌اثر باید شکست بخورد. **پذیرش:** هر چهار قالب از CLI و بستهٔ نصب‌شده قابل مصرف و تمام ۲۴۴ زوج ثبت‌شده باشند.

### Q15 — oracle محتوایی، ساختاری و تصویری مستقل؛ اولویت P0 برای پذیرش

**وابستگی:** زیرساخت اولیه همراه Q03؛ تکمیل پس از Q14. جزئیات oracle در بخش ۸ آمده است.

- [ ] استخراج متن DOCX شامل بدنهٔ جدول‌های تزئینی، hyperlink، footnote، code و field با نقش درست انجام شود؛ تکرار سرستون/TOC/caption تولیدی به افت محتوا یا تکرار کاذب منجر نشود.
- [ ] شمار تصاویر بر اساس occurrence و relationship در کنار unique media ثبت شود؛ reuse قانونی یک لوگو با اشتباه E01 یکی تلقی نشود.
- [ ] sanity-check ساختاری و layout، صفحهٔ گم‌شده، سند repairشونده، media خالی و font fallback را پیدا کند.
- [ ] برای کنترل خود oracle، روی کپی آزمایشی خروجی حذف یک پاراگراف، قطع آخر کد، جابه‌جایی دادهٔ دو ستون، تعویض نمودار با مستطیل، شکستن link و خروج تصویر از حاشیه را تزریق کن؛ هرکدام باید fail شوند.
- [ ] baseline خراب را برای کاهش failure approve نکن؛ تصویر صرفاً به خاطر ساخته‌شدن توسط نسخهٔ جدید مطلوب نیست.

**پذیرش:** دست‌کم این شش خرابی عمدی به‌وسیلهٔ checks مربوط کشف شوند؛ هیچ golden بدون reviewer و شواهد پذیرفته نشود.

### Q16 — مرور AI و انسانی، بستن ایرادهای تصویری؛ اولویت P0 برای انتشار

**وابستگی:** Q15 و رندر کامل. گردش کار دقیق در بخش ۹.

- [ ] تمام صفحات همهٔ زوج‌ها برای هر موتور هدف در index و وضعیت مرور ثبت شوند؛ نمونه‌برداری از چند صفحه نباید با عنوان «تمام صفحات تأیید شد» گزارش شود.
- [ ] AI برای هر صفحه و مرز صفحه issue با نوع/محل/شدت/اطمینان ثبت کند؛ uncertainty و مورد مشکوک به انسان برسد.
- [ ] کاربر نمونه‌های هر چهار قالب، کتاب کامل روی هر چهار قالب، صفحه‌های مسئله‌دار و تفاوت‌های Word/LibreOffice را با بسته‌های کوچک مرور کند.
- [ ] مشکل تأییدشده به fixture حداقلی و تست رگرسیون وصل و در لایهٔ عمومی اصلاح شود؛ Word نهایی دستی تعمیر نشود.
- [ ] پس از تغییر layout، run تازه و تمام صفحات متاثر دوباره مرور شوند؛ تیک تایید run قبلی به فایل با hash جدید منتقل نشود.

**پذیرش:** issue بازِ مسدودکننده صفر، وضعیت صفحهٔ بدون بررسی صفر و تأیید انسانی موردهای ضروری ثبت شده باشد. نبود دسترسی به Word یا پاسخ کاربر «pending» است، نه pass.

### Q17 — CI، بستهٔ تحویل و معیار بسته‌شدن؛ اولویت P1

**وابستگی:** تمام Qهای لازم بالا.

- [ ] job سریع برای PR، ماتریس کامل برای nightly/release و مسیر Wordِ قابل تکرار یا مرور ثبت‌شده تعریف شود؛ بخش ۱۰ حداقل‌ها را تعیین می‌کند.
- [ ] مثال نهایی `MD + نام قالب` و `MD + مسیر قالب` در محیط تمیز برای هر چهار قالب اجرا شود؛ فایل ورودی، دستور، خروجی، dependency report و راهنمای فونت کنار هم باشند.
- [ ] README و AGENTS دربارهٔ dialect، custom-style، تصاویر محلی، font، template/shell و Word/LibreOffice به‌روز شوند؛ شعار «پشتیبانی کامل» جای جدول قابلیت آزموده‌شده را نگیرد.
- [ ] گزارش نهایی بدون ادعای تست اجرا‌نشده، با pass/fail/skip/pending و لینک artifactها نوشته شود.
- [ ] جدول DoD بخش ۱۱ تکمیل شود؛ تنها رسیدن به پایان فهرست تسک‌ها یا افزایش نمرهٔ ذهنی به معنی بسته‌شدن نیست.

**پذیرش:** شخص تازه‌وارد بتواند با دستور مستند، همان fixture و قالب را به خروجی قابل مرور تبدیل کند و شواهد سلامت آن را پیدا کند.

## ۸. آزمون دقیق هر یک از ۲۴۴ زوج

### ۸.۱. لایهٔ اول: حفظ محتوا و معنی

برای هر زوج، قبل از قضاوت ظاهر این موارد بررسی شوند:

| حوزه | oracle و انتظار |
| --- | --- |
| متن | توالی محتوای blockها و رشته‌های حساس منبع حفظ باشد؛ تبدیل کل سند به یک رشته و حذف تمام whitespace کافی نیست |
| تیتر | تعداد، سطح، متن، شماره و مقصد anchor با انتظار fixture تطبیق کند؛ عنوان تزئینی دو بار شمرده نشود |
| کد | رشتهٔ CodeBlock پس از decode صحیح tab/break در Word با AST ورودی برابر باشد؛ indentation و خط خالی حذف نشود |
| جدول | تعداد ردیف/ستون داده، ترتیب cellها و متن غنی هر cell حفظ شود؛ جدول layout جدا شناسایی شود |
| لینک | متن و مقصد خارجی/داخلی و relationship معتبر باشد؛ رنگ آبی به‌تنهایی لینک نیست |
| تصویر | تمام occurrenceهای مورد انتظار drawing داشته باشند، مقصد media درست باشد و reuse فقط وقتی مجاز باشد که منبع مشترک است |
| Mermaid | هر کد منبع به یک نتیجهٔ واقعی مرتبط شود؛ labelها و ساختار مورد انتظار حاضر باشند؛ تولید PNG بدون درج در مقصد nested شکست است |
| پاورقی/فرمول | reference و part هدف، متن و عناصر غنی و فرمولِ حوزهٔ پشتیبانی‌شده حفظ شود |
| metadata/TOC | دادهٔ ورودی و عناصر تولیدی قالب تفکیک شوند؛ شمارش متن TOC موجب خطای تکرار یا پنهان‌کردن افت محتوا نشود |

انتظارها از ورودی و manifest مستقل بیایند؛ استخراج متن با همان تابع renderer و مقایسه با خروجی خودش oracle مستقل نیست. مقایسهٔ round-trip DOCX→MD کمک‌کننده است، اما افت مشابه در دو مسیر ممکن است یکدیگر را پنهان کند و معیار اصلی نیست.

در prose فقط تفاوت‌های مجاز و مستند مانند نوع شکست خط نمایشی normalization شوند؛ ZWNJ، علائم، ترتیب کلمه و اعداد فنی پاک نشوند. هر استثنا باید محدود به نقش و fixture مشخص باشد. assertion سراسری «متن اصلی substring خروجی است» برای مجموعه کافی نیست.

### ۸.۲. لایهٔ دوم: سلامت Word و اعمال قالب

- DOCX از نظر ZIP، XML، relationship، content type و media سالم باشد؛ Word آن را با پیام Repair باز نکند.
- اندازهٔ صفحه، margin و sectionها به template manifest بخورند؛ header/footer در محدودهٔ مجاز خود قرار گیرند.
- نقش‌های فونت/اندازه/جهت روی تمام storyهای مربوط، شامل footnote، table، caption و header/footer اعمال شوند.
- ستون‌ها و محتوای داخلی از عرض container فراتر نروند؛ علاوه بر عدد عرض در XML، تصویر نهایی بررسی شود.
- نسبت ابعاد تصویر پس از درنظرگرفتن orientation، با نسبت منبع بیش از حدود ۱٪ اختلاف نکند مگر crop صریح و مستند باشد؛ فعلاً crop خودکار درخواست نشده است.
- bookmark ID و relationship ID در scope خود معتبر و یکتا باشند؛ جدول تزئینی به ساختار heading آسیب نزند.
- اندازهٔ font ثبت‌شده و فونت استفاده‌شده در رندر هر دو با انتظار بررسی شوند؛ اختلاف ناشناختهٔ font شکست محیط مرجع است.

### ۸.۳. لایهٔ سوم: رندر واقعی تمام صفحات

برای هر زوج، DOCX با LibreOffice ثابت به PDF و همهٔ صفحات به PNG با DPI ثابت، مثلاً ۱۵۰، تبدیل شود؛ برای ناحیهٔ مشکوک نسخهٔ ۲۰۰–۳۰۰ DPI هم در دسترس باشد. **PDF حاصل از HTML یا PDF تولیدشده مستقیم از Markdown جای رندر DOCX نیست.**

موتور هدف Word جدا باشد: نسخهٔ منتخب Word روی سیستم کاربر یا محیط آزمون واقعی، همان DOCX را باز کند و PDF صادر کند. فایل تولیدی اصلی و کپی بازشده/field-updated با hash جدا نگه داشته شوند؛ export از LibreOffice را «Word test» نام نگذار.

- تمام صفحات خروجی باید در manifest رندر ثبت شوند؛ شمارهٔ مفقود، PNG صفر‌بایتی یا PDF قدیمی خطاست.
- تعداد واقعی صفحات از PDF/renderer بیاید، نه metadata قدیمی DOCX.
- ۲۴۴ زوج × دو موتور برابر **۴۸۸ مشاهدهٔ رندر** است، نه ۴۸۸ fixture یا ۴۸۸ تبدیل مستقل Markdown. تعداد واقعی PNGها مجموع صفحات اسناد است و پس از اجرا محاسبه شود.
- baseline هر موتور و هر قالب جداست؛ انتظار برابری پیکسلی Word با LibreOffice یا T01 با T03 وجود ندارد.
- تغییر تعداد صفحه در یک محیط ثابت trigger بررسی باشد، نه خودکار pass/fail؛ بدون افت محتوا، فونت یا حاشیه ممکن است تغییر درست طراحی باشد.
- رندر تمام‌صفحه فقط crop بدنه نباشد؛ بیرون‌زدگی، footer و header هم دیده شوند. crop مکمل برای خواندن جزئیات مفید است.

### ۸.۴. لایهٔ چهارم: معیارهای قابل مشاهده

| نوع ایراد | شرط رد یا ارجاع |
| --- | --- |
| بریدگی/هم‌پوشانی | هر نویسه/گره/عدد/تصویر لازم که بریده، پوشیده یا خارج محدوده باشد، رد؛ تلرانس اندازه‌گیری تا ۱mm برای گردکردن، مجوز بریدگی نیست |
| جهت و ترتیب | وارونگی عبارت، جابه‌جایی ستون داده، قرارگرفتن marker در سمت نامتناسب با همان فهرست، رد |
| فونت | tofu، گسست حروف فارسی، fallback ناشناخته، اندازهٔ زیر حد توافق‌شده، رد |
| تیتر/کادر | عنوان بدون شروع محتوای وابسته در همان صفحه، badge بریده یا title متداخل، رد |
| کد | خط افتاده، wrap همراه تغییر کد، خروج از کادر یا تکهٔ ناخوانا، رد |
| نمودار | مستطیل/placeholder به جای نمودار، label ناقص، گره/edge مفقود یا متن زیر حد خوانایی، رد |
| caption | انتساب به شکل نادرست یا جداافتادن غیرضروری از تصویر، رد؛ caption بدون asset در B00 فقط warning منبعِ از قبل ثبت‌شده |
| فضای سفید | صفحهٔ ناخواستهٔ خالی، رد؛ استفادهٔ کمتر از حدود ۳۵٪ بدنه در صفحهٔ میانی فقط علامت مرور است، زیرا شروع فصل/شکل/پایان بخش می‌تواند عمدی باشد |
| خوانایی رنگ | برای متن عادی هدف کنتراست حداقل ۴٫۵:۱ و متن بزرگ ۳:۱ به‌عنوان معیار پروژه تعیین شود؛ نسخهٔ grayscale T03 نیز خوانا بماند |
| یک‌دستی | اندازه/رنگ/فاصلهٔ یک نقش در شرایط برابر از قالب منحرف نشود؛ نقش‌های متفاوت لازم نیست ظاهر یکسان داشته باشند |

OCR و استخراج مختصات PDF ابزار کمکی‌اند. OCR فارسی می‌تواند ترتیب حروف را بد تشخیص دهد؛ اختلاف OCR باید به بررسی ناحیه منجر شود، نه اصلاح متن منبع. histogram یا شمار pixel غیرسفید هم نمودار خالیِ دارای border را لزوماً تشخیص نمی‌دهد؛ E01 عمداً برای آزمودن این ضعف به کار رود.

image diff تنها پس از ساخت golden سالم و در همان engine/version/font قابل اتکاست. threshold عمومی SSIM بدون بررسی ناحیهٔ متن/label کافی نیست. mask فقط برای ناحیهٔ متغیر واقعاً لازم و با ثبت علت باشد؛ بدنهٔ متن، caption، header یا ناحیهٔ مشکل‌دار برای سبزشدن تست mask نشوند. زمان/تاریخ تصادفی از fixture حذف یا ثابت شود.

## ۹. گردش کار مرور هوش مصنوعی و انسانی

### ۹.۱. بستهٔ مرور قابل استفاده

مسیر پیشنهادی هر اجرا: `artifacts/persian-layout/<run-id>/`، مستقل از fixtureها. این پوشه بعداً تولید می‌شود و جزو محصول این پلن نیست.

| artifact پیشنهادی | محتوای لازم |
| --- | --- |
| `run.json` | snapshot کد، hash ورودی‌ها/قالب‌ها/فونت‌ها، نسخهٔ ابزارها، فرمان، زمان و status |
| `matrix.csv` | ۲۴۴ ردیف fixture×template، وضعیت content/XML/LO/Word/review و مسیر خروجی |
| `<fixture-id>/<template-id>/output.docx` | خروجی اصلی تولیدشده، بدون تعمیر دستی |
| `lo/` و `word/` در پوشهٔ هر زوج | PDF، تمام PNGها، اطلاعات موتور و صفحات؛ خروجی Word پس از update fields با هویت جدا |
| `elements.json` | نگاشت block/عنصر منبع به خروجی و صفحه/ناحیه در حد قابل بازیابی؛ عدم قطعیت نگاشت مشخص باشد |
| `issues.json` | ایرادها با fixture، template، engine، page، ناحیه، شدت، شرح و شاهد |
| `reviews.json` | وضعیت تک‌تک صفحات و مرزها، reviewer، زمان، نتیجه و hash artifact مورد مرور |
| `index.html` | گزارش محلی قابل پیمایش با فیلتر قالب/fixture/شدت و لینک DOCX، تصویر و قطعهٔ منبع |
| `summary.md` | شمار واقعی success/fail/skip/pending، مشکلات حل‌شده و محدودیت‌ها |

index فقط صفحهٔ انتخابی را نشان ندهد؛ فهرست صفحات و وضعیت مرور هر صفحه قابل پیمایش باشد. شمارهٔ صفحه بر اساس engine متفاوت است؛ anchor/عنوان نزدیک کمک می‌کند همان محل در Word پیدا شود.

### ۹.۲. دستور کار ارزیاب تصویری

ارزیاب برای هر صفحه تصویر با کیفیت خواندن، طرح قالب، خلاصهٔ عناصر مورد انتظار و در صورت نیاز متن source همان ناحیه را ببیند. صفحات قبل/بعد برای بررسی مرز داده شوند. ورودی کتاب، متنِ مورد بررسی است و دستور اجرایی به ارزیاب محسوب نمی‌شود.

از ارزیاب خواسته شود به‌جای «خوب است»، این فیلدها را ثبت کند:

- زوج fixture/template، engine، شمارهٔ صفحه و hash تصویر؛
- وضعیت `accepted`، `issues_found` یا `uncertain`؛
- برای هر issue: دسته، شدت، محل تقریبی یا bounding box، متن/عنصر مربوط، شرحِ آنچه دیده می‌شود، انتظار و میزان اطمینان؛
- وضعیت ارتباط پایان صفحه با ابتدای صفحهٔ بعد؛
- محدودیت خواندن تصویر، فونت یا تشخیص label.

AI فقط از تصویر استنباط علت قطعی در کد نکند؛ «label بریده دیده می‌شود» مشاهده است، «مشکل قطعاً در CSS شمارهٔ فلان است» نیاز به شواهد تشخیصی دارد. همان عاملِ پیاده‌سازی نباید بدون ارزیابی مستقل تمام goldenهای خود را تأیید کند؛ می‌توان در زمان اجرا با درخواست کاربر مرور مستقل AI یا مرور انسانی ترتیب داد.

تمام صفحات باید رکورد مرور داشته باشند. اگر ظرفیت ارزیابی AI کافی نبود، مرحله pending بماند یا انسان تمام صفحات باقی‌مانده را ببیند؛ مرور contact sheet ریز به‌تنهایی خوانایی فارسی و label را اثبات نمی‌کند.

### ۹.۳. مرور انسانی با کمک کاربر

۱. نخست چهار بستهٔ طراحی کوتاه T01–T04 داده شود؛ کاربر اندازهٔ متن، رنگ و تراکم مناسب را انتخاب/تأیید کند. این تأیید طراحی، به معنی تأیید تمام اسناد نیست.
۲. چهار خروجی B00، با نمونه‌های اول/میانه/آخر هر فصل و تمام محل‌های flagged، در Word مرور شوند. برای هر کتاب صفحات شروع/پایان، تیتر بلند، جدول چندصفحه‌ای، بلوک کد بلند و مرز callout/caption در فهرست مرور باشند.
۳. برای S09–S12 تصاویر واقعی و Mermaid در هر چهار قالب به کاربر نشان داده شوند؛ B00 بدون تصویر نمی‌تواند این پذیرش را تأمین کند.
۴. تمام issueهای severe، تمام `uncertain`ها و اختلاف‌های مهم Word/LibreOffice انسانی داوری شوند. نمونه‌ای از صفحات AI-accepted هم بازبینی شود تا false negative دیده شود؛ حداقل ۱۰٪ صفحات پذیرفته‌شده در هر قالب/موتور، با seed نمونه‌گیری ثبت‌شده.
۵. هر بار ۳ تا ۵ ناحیه/صفحهٔ مرتبط با source و گزینهٔ «درست/ایراد دارد/نامشخص» ارائه شود؛ کاربر برای تشخیص یک ایراد مجبور به گشتن در صدها سند نباشد.
۶. اگر در نمونهٔ پذیرفته‌شده ایراد جدی پیدا شد، تمام موارد همان الگو دوباره بررسی و دامنهٔ نمونه‌گیری گسترش یابد؛ صرف اصلاح همان صفحه کافی نیست.
۷. تأیید انسانی روی hash خروجی و نسخهٔ قالب ثبت شود. تأیید قدیمی به نسخهٔ تازه‌ای که شکست صفحه‌اش تغییر کرده منتقل نشود.

هیچ انتظار «بررسی انسانی تمام صفحات» به کاربر نسبت داده نشود مگر واقعاً انجام شود. شرط این پلن **بررسی تمام صفحات با AI یا انسان، به‌علاوهٔ مرور انسانی هدفمند و داوری موارد مبهم** است. در گزارش نهایی دقیقاً تفکیک شود چه تعداد صفحه AI، انسان یا هر دو دیده‌اند.

### ۹.۴. ساخت golden معتبر

- baseline اولیه فقط برای بازتولید است و باید برچسب known-bad/unapproved داشته باشد.
- golden سالم پس از گذر محتوا/XML، نبود fallback فونت، تأیید طراحی و بسته‌شدن ایرادهای تصویری تصویب شود.
- برای هر template و renderer golden جدا با نسخه، hash و reviewer ثبت شود؛ نمونه‌های قدیمی raw هیچ‌وقت خودکار golden نشوند.
- هنگام تغییر فونت/نسخهٔ renderer/page geometry، diffها دوباره مرور شوند؛ اجرای خودکار «update all snapshots» پایان review نیست.
- نتایج AI و نمونه‌گیری انسانی کنار golden بمانند؛ پذیرش فقط یک فایل PNG بدون دلیل نیست.

## ۱۰. اجرای مرحله‌ای و جلوگیری از سبزشدن صوری

### ۱۰.۱. تقسیم اجرا

| مسیر | حداقل پوشش | زمان اجرا و محدودیت |
| --- | --- | --- |
| هر تغییر مرتبط | آزمون واحد همان رفتار + fixture مسئله روی T01 و باریک‌ترین قالب T03، پس از موجودشدن آن | بازخورد سریع TDD |
| PR | fixtureهای کوچک نمایندهٔ متن مخلوط، style، table/list، code، image و Mermaid روی تمام قالب‌های موجود؛ بخشی با ابزار واقعی | جایگزین ماتریس release نیست |
| nightly یا اجرای کامل توسعه | ۶۱×۴ تبدیل، تمام checks ساختاری و تمام صفحات LibreOffice، گزارش تصویری | artifactها حفظ و failureها گزارش شوند |
| release candidate | همان ۲۴۴ زوج روی snapshot ثابت + رندر تمام آن‌ها در Word هدف + مرور بخش ۹ | بدون Word به‌عنوان تأییدشده منتشر نشود |
| بستهٔ نصب | CLI و API خارج checkout، نام/مسیر قالب، فونت و دارایی wheel | حداقل یک fixture پرقابلیت و B00 روی هر چهار قالب |

دستورات تست موجود `AGENTS.md` مبنا هستند؛ نام دستورهای جدید runner پس از پیاده‌سازی مستند شود. وجود `MD2DOCX_REQUIRE_EXTERNAL=1` به‌تنهایی کافی نیست؛ skipهای واقعی و ابزارهای غایب در گزارش ممیزی شوند.

### ۱۰.۲. کنترل منابع و تکرارپذیری

- پیش از اجرای کامل، زمان تبدیل و رندر B00 و S11 را در هر قالب اندازه بگیر و برآورد تعداد صفحه/زمان/فضای دیسک بده؛ عدد دقیقهٔ حدسی معیار قبولی نشود.
- worker محدود و profile/cache مستقل انتخاب شود؛ parallel کردن نامحدود ۲۴۴ Chromium/LibreOffice احتمال failure محیطی و نتیجهٔ کهنه را بالا می‌برد.
- timeout از اندازهٔ fixture و زمان مشاهده‌شده با حاشیهٔ مشخص تعیین شود؛ افزایش timeout برای پنهان‌کردن hang یا کاهش تعداد موارد برای پاس‌شدن suite مجاز نیست.
- retry محدود فقط برای failure زیرساختی با حفظ log اولیه باشد؛ نتیجهٔ بار دوم باعث حذف سابقهٔ خطای اول نشود. failure محتوایی/بصری خودکار retry تا پاس نشود.
- اجرای کامل می‌تواند shard شود؛ تجمیع باید دقیقاً تمام ۲۴۴ زوج مورد انتظار را بدون تکرار/missing تحویل دهد.
- hashهای fixture/template/font/config در کلید نتیجه و cache لحاظ شوند؛ reuse artifact قدیمی با تنظیم جدید ممنوع است.
- DOCX کامل لازم نیست بایت‌به‌بایت یکسان باشد؛ timestamp و IDهای غیرمعنایی جدا شوند، اما متن، رابطه‌ها، media، style و layout در محیط ثابت قابل تکرار باشند.

### ۱۰.۳. آزمون‌های منفی خارج از عدد ۲۴۴

این موارد جدا از مجموعهٔ ورودی‌های مثبت اجرا و انتظار خطا/هشدار دقیق برایشان نوشته شود:

| دسته | نمونهٔ لازم |
| --- | --- |
| دارایی | تصویر گم‌شده/خراب، فایل هم‌نام در base_dir دیگر، remote/data URI خارج قرارداد |
| Mermaid | syntax نامعتبر، executable/browser غایب، timeout، تصویر خالیِ دارای border، label ناقص، نمودار بسیار بزرگ و غیرقابل جاگیری |
| فونت | فونت ناموجود، family اشتباه، Bold غایب، فونت در host حاضر ولی در renderer غایب |
| Markdown | style ناشناخته، node خارج قرارداد، HTML نامعتبر، syntax نمایشی داخل fence که باید literal بماند |
| قالب | کلید ناشناخته، مقدار اندازه نامعتبر، margin ناممکن، shell چندبخشی، asset گم‌شده در wheel |
| oracle | حذف/جابه‌جایی/قطع محتوا، link شکسته، blank media و عنصر بیرون صفحه |
| اجرا | زوج ماتریس حذف‌شده، تصویر صفحهٔ گم‌شده، artifact کهنه، worker شکست‌خورده، مورد بدون review |

warningهای مثبتِ از قبل مشخص‌شده مانند captionهای بی‌تصویر B00 با ID و anchor در manifest مجازند؛ هشدار جدید یا مبهم نباید بی‌دلیل ignore شود. «تمام خروجی‌ها بدون warning» برای منبعی که پنج caption بی‌تصویر دارد انتظار نادرستی است.

## ۱۱. شرط واقعی اتمام و تحویل

عامل اجراکننده باید این جدول را با شاهد تکمیل کند، نه فقط علامت بزند:

| دروازهٔ پذیرش | معیار لازم | شاهد نهایی |
| --- | --- | --- |
| اصالت fixture | hash کتاب اصلی ثابت؛ ۴ فصل، ۴۰ برش و ۱۶ نمونهٔ جدید؛ ۶۱ ID یکتا | manifest و گزارش validation |
| قابلیت قالب | T01 اصلاح‌شده + T02/T03/T04 مستقل، نام و مسیر قابل استفاده | خروجی validate/list و اجرای واقعی |
| حاصل‌ضرب کامل | ۲۴۴ زوج، بدون missing/skip پنهان؛ تمام تبدیل‌های مثبت موفق | matrix.csv و شمارش مستقل |
| وفاداری محتوا | صفر افت/جابه‌جایی متن، کد، table cell، link و media مورد انتظار | گزارش oracle و آزمون‌های خرابی عمدی |
| فارسی | جهت و نشانه‌گذاری درست، Vazirmatn واقعی و override فونت آزموده | صفحهٔ کنترل فونت و review موارد حساس |
| Mermaid/تصویر | نمودار واقعی، label کامل، عکس درست، نسبت و ظرف سالم، nesting کامل اعلام‌شده | native media + صفحات Word/LibreOffice |
| کد | syntax highlighting واقعی، رشتهٔ قابل کپی، long-line و چندصفحه سالم | oracle کد و صفحات S07/S08/R40 |
| صفحه‌بندی | صفر بریدگی/هم‌پوشانی/عنوان یتیم/سرریز شناخته‌شده؛ template در تمام صفحات رعایت شود | review تمام صفحات و مرزها |
| Word واقعی | نسخهٔ هدف و export واقعی مشخص؛ header/footer/TOC/anchorهای لازم آزموده | گزارش Word و تأیید کاربر |
| مرور | تمام صفحات reviewشده؛ severe و uncertain حل‌شده؛ نمونه‌گیری انسانی انجام‌شده | reviews.json و issues بسته‌شده |
| بستهٔ نصب | چهار قالب با assets در محیط تمیز و خارج مخزن قابل اجرا | گزارش wheel/CLI/API |
| مستندات و محدودیت | قالب اجرایی، dialect، font، تصاویر محلی و محدودهٔ پشتیبانی روشن | README/راهنمای نهایی و نمونهٔ ورودی/خروجی |

شدت‌بندی برای بستن issueها:

- **مسدودکننده:** حذف/تحریف محتوا، نمودار خالی/ناقص، متن ناخوانا، ترتیب غلط معنی‌دار، فایل repairشونده یا از دست‌دادن asset. هیچ مورد باز مجاز نیست.
- **مهم:** تیتر/کادر یتیم، marker جابه‌جا، سرریز، font fallback، label ریز یا اختلاف جدی نقش قالب. پیش از اتمام رفع شود.
- **جزئی:** اختلاف جزئی زیبایی بدون افت خوانایی/یک‌دستی؛ فقط با ثبت شاهد و تأیید صریح کاربر قابل پذیرش است، نه با تصمیم یک‌طرفهٔ عامل.

اگر Word، فونت یا مرور انسانی در دسترس نیست، پروژه از نظر همان دروازه **آمادهٔ بررسی/منتظر شاهد** است. هیچ نمرهٔ عددی، نسبت pass تست واحد یا رندر چند صفحه جای این دروازه‌ها را نمی‌گیرد.

بستهٔ تحویل باید شامل چهار DOCX کتاب اصلی، نمونه‌های پرقابلیت تصویر/Mermaid روی چهار قالب، گزارش ۲۴۴ زوج، مسیر fixtureها و templateها، دستور بازتولید، اطلاعات فونت/محیط و فهرست محدودیت‌های باقی‌مانده باشد. خروجی‌های ارسالی raw برای مقایسه نگه داشته شوند و به‌جای آن‌ها فایل ترمیم‌شده ذخیره نشود.

## ۱۲. مرز این نوبت با اجرای آینده

در این نوبت فقط منبع، کد مرتبط و دو DOCX کاربر بررسی و برای تشخیص، نسخه‌های موقت PDF/PNG از DOCXهای موجود ساخته شد. **قالب جدید، fixture جدید، تست اجرایی جدید یا اصلاح کد محصول پیاده‌سازی نشده است.** ساخت تمام آن‌ها کارهای آیندهٔ همین پلن است.

تبدیل معکوس DOCX→Markdown، رابط گرافیکی، سرویس آنلاین، دانلود خودکار عکس اینترنتی و بازنویسی موتور تبدیل برای این مسئله لازم نیستند و در این برنامه گسترش نمی‌یابند. مسیر معکوس موجود فقط می‌تواند ابزار تشخیصی کمکی باشد؛ هدف اصلی همین دور، رفع قابل اثبات کیفیت فارسی در Word است.
